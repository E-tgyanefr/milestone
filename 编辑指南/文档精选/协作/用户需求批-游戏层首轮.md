# 用户需求批-④游戏层首轮实施（coder-vis）

## 已实现（零 X ABI 依赖——数据平面/输入现有 ABI/性能基准/映射表）
1. **设置 UX 重构**（App/SettingsPage.cs）：SettingItem/Section 模型——9 区全显（标题行全可见+聚焦区展开明细）+↑↓分区/区内滚动（底顶切换）+←→选值（Options+OnChange→AppConfig）+Enter 确定保存+Esc 返回+焦点高亮（▶/●/Accent 标记）；数据平面全量（绘制=X3 ms_text_* 后 Widgets 渲染）。
2. **主菜单精简**（App/MainMenuPage.cs）：三项（开始游戏/设置/退出——eng-design 裁定基准）；FolderRequested 通道保留（曲库管理并入设置=待裁定后定位；菜单不暴露）。
3. **性能测试与画质建议**（Play/PerfBench.cs + Bench.cs + --bench [seconds]）：Stopwatch 帧时统计（avg/p50/p95）+19/9ms 自适应模型端口（p95<9ms=高/9~19ms=中/>19ms=低——原版 FpsGovernor 口径）；**本机实测（native）**：--bench 5s → avg 12.04ms/p50 12.09ms/p95 12.90ms → **中档建议**（特效减半+UI 简化）。
4. **每模式大击特效映射表**（Play/FxCatalog.cs）：FxKind 8 模式映射（ManiaClick/PhigrosLineBurst/ArcaeaTrace/IidxScratch/CytusScanPulse/MaimaiRing/AdofaiShock/OsustdRipple——ms_rnd_* 原语组合描述；X2 就绪后 OnRender 检 FxKind 经原语生效）；selftest 断言（fx-catalog: 8 mode mapping）加入。
5. **osustd 游玩**（用户 bug 修复候选——末段显示问题待试玩取证）：InputBridge.PollOsu（Z/X/空格→OnFieldInput）+ PlayPage.OnFieldInput + PlayScene.HandleFieldInput + JudgementZone.ReceiveFieldInput + ChartRuntime.ReceiveFieldInput + **PlayState.ReceiveFieldInput**（域权威=NativeTracker.PressField(x,y,hitRadius)——osustd preset 命中语义复刻；镜像=X/Y 最近音符近似回退）；MainScene 游玩页 osustd 模式分支；**X4 鼠标 pos/button 就绪后接入**（当前=中心场简化输入）。
6. **代码精简 pass**：新增模块化（非重构批：设置模型化/菜单数据化/PlayState 场判入口单一——去重于本次实现内完成；PageBase 公共导航已有——进一步抽取待 UX 完成后一并清）。

## 依赖 X2/X3/X4（eng-coder 增量 ABI 落地后接）
- 设置全 UI 绘制=X3（ms_text_*）；- 每模式特效=X2（ms_rnd_* 原语）；- osustd 鼠标定位/按钮=X4（现=Z/X/空格+中心简化）；- 性能建议 UI 显示=X3。

## 验证
- build 0 错 0 警；--selftest PASS（+fx 断言）；--g1 5/5 PASS（判定链 1:1 保持）；--abi-check 22/22 PASS（23 函数代理无损）；--bench 5s 实测（native）→中档建议；两树 34 文件 MD5=0（含新增 2 文件：PerfBench/FxCatalog）。

## 待续（依赖侧推进）
- X2/X3/X4 ABI 增量（eng-coder）→设置/特效/osustd 全 UI 全链；osustd 末段显示 bug（试玩原版取证→域/游戏层修复——C# 侧渲染挂接位已留：LaneRenderer.OnRender osustd=域 render_field 候补）；主菜单三项最终裁定（eng-design）；设置项与 AppConfig 全映射细化。