# t43 实现报告：t41 P1×5 修复（编辑器说明行/AI 标题/结算底部/键位展开）

- 日期：2026-08-27 · 承办 coder-max · 约束：只写代码不构建（captain 构建 + 试玩复验）
- 依据：trunc-sweep2.md（t41-01~07，bin 21:17:07）

---

## 逐点修复

### 1/2 编辑器·事件/判定线 Tab 长说明行裁切穿插（P1-1、P1-2）
- 根因：右栏说明标签 `L()` 为 AutoSize 无 MaximumSize——长文案（「（数量无上限；放置的音符/新事件归属此线…）」等 12 处）
  单行延伸至面板右缘被裁，行间观感穿插。
- 修复：`L()` 统一加 `MaximumSize = new Size(Ui.P(255), 0)`——右栏窄面板（424 物理宽）内按 255 逻辑宽换行全文，
  流式布局自动撑高不重叠；字号不变。
- 双保险：新增共享 `ToolTip _tipTooltip`（AutoPopDelay 30s），对 11 个长说明行
  （easeTip/lineTip/splitTip/parentTip/speedTip/alphaTip/aiTip/keyTip/propTip/aiHead/_propLbl）悬停显示全串。

### 3 编辑器·AI Tab 标题右端截断（P1-3）
- `aiHead` MaximumSize 240→**255**（同 L() 口径）——「…规则检查仍可用）」换行全文不裁切 + tooltip 全串。
- `_propLbl`（属性速览）同口径 240→255。

### 4 结算页底部条目截断（P1-4）
- 取证：t41-06（2560×1600）逐像素带扫描——面板内仅 标题/评级/分数/ACC/判定条/按钮/底部提示行，
  **全帧无任何日期绘制路径**（GamePanel 结算画面源码亦无日期绘制）；扫掠「2026-09-06 最高 50」系底部提示行
  「[R/回车] 重新开始 · [退格] 返回选歌 · [ESC] 主菜单」的 OCR 误读。
- 修复（按任务「日期行宽度自适应」口径作用到底部文本）：提示行 `宽 panelW-60→panelW-40、高 18→20、字号 11→10.5`
  ——任意面板宽下完整渲染，不再被裁。

### 5 键位展开态（P1-5）
- 待补（需实际按键触发展开态核验；t33 两行独立已覆盖常态）——如实标注，本轮无代码面。

## 变更文件
- `源码/Charting/ChartEditorPanel.cs`（L() 换行宽、_tipTooltip 字段/创建/挂接、aiHead/_propLbl 255）
- `源码/Play/GamePanel.cs`（结算底部提示行宽高字号，giant-line 内单处替换）

## 试玩复验口径（captain 构建后）
1. 编辑器事件/判定线 Tab：长说明行换行全文、行间无穿插；悬停出全串 tooltip；
2. 编辑器 AI Tab 标题「…规则检查仍可用）」完整可见；
3. 结算页底部提示行完整；
4. 键位展开态（实际按键触发后）键名/退格/Del 提示全文。
