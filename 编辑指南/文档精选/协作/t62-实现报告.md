# t62 四目录迁移 + 游戏内容程序目录收口 · 实现报告（coder-vis）

> 依据：其他/docs/协作/folder-structure-design.md（t60 定稿→已随迁 编辑指南\文档精选\协作\folder-structure-design.md）。
> 构建验证：`游戏源码\Milestone.csproj` `dotnet build -c Release` = **0 警告 0 错误**；`--selfcheck` 实测 exit=0；EngineChecks（引擎源码\engine\Tests）exit=0 全绿。

## ① 目录迁移（目标树落地）
``
milestone\
├─ .agent-teams\             （保留根，团队状态）
├─ 游戏源码\    Milestone.csproj + app.manifest + Program.cs + 源码\（63 文件）+ obj\
├─ 引擎源码\    引擎\engine\ 全树（736 文件；MilestoneEngine.csproj / App / Ui / Platform / RhythmCore / Tests(EngineChecks) / EnginePlay / Samples / PublishPlayer / Tool …）
├─ 编辑指南\    README.md（门户+路径映射表）+ DESIGN.md + REVIEW.md + 文档精选\（协作\全量 195+ 篇 + ENGINE_旧引擎规划.md + 本地AI接入说明.md + docs-source\engine-tool-guide.md）+ 工具脚本\（打包命令.txt + _min*.ps1）+ PDF\（留空待 t62 后半/后续）
└─ 输出产物\    bin\（OutputPath 指向；Release\net8.0-windows\win-x64\Milestone.exe 9.2MB 实测产出）+ 发布\（Milestone.exe 157MB + skin.json + Chart\ 277 文件 + Log + Replay）+ 示例内容\（Chart\ 实例/测试格式 全量 + 段位文件\ 196 + library_rating.json + Player\ai_train.json + exports\）+ dev\（原构建产物 全量 6000+ 文件）+ 日志\（根 logs + 其他\logs）
``
- 旧空壳（引擎/其他非 .agent-teams 部分/exports/构建产物/bin 原始/源码/发布旧位）已清空或移除；根下仅 4 分类 + .agent-teams + 其他\（含历史 .agent-teams 副本，保留）。
- 移动方式：robocopy /MOVE /E（源删目校）；引擎内部相对引用（..\MilestoneEngine.csproj 等）随树不变。

## ② csproj 路径引用批量修复
- `游戏源码\Milestone.csproj`：ProjectReference `..\引擎源码\engine\MilestoneEngine.csproj`；OutputPath `..\输出产物\bin\`；ItemGroup 清理（旧 分类目录 排除项全部作废，仅留 obj\**；补 `..\引擎源码\engine\Samples\**\*.cs` Compile Include——DemoRunner/MiniManiaForm/EngineGame 由游戏层引用，属引擎示例随游戏编译）。
- 根 `Program.cs` / app.manifest 随 csproj 迁移；无绝对路径引用（grep 0 命中）。
- `输出产物\dev\engine-exe\build-tool.ps1`：$repo 上溯三级（dev\engine-exe→dev→输出产物→根）+ 路径替换 `$repo/引擎/engine/→$repo/引擎源码/engine/`、`$repo/docs/→$repo/编辑指南/文档精选/docs-source/`。
- `输出产物\dev\obj\caprepro\Program.cs:155`：硬编码 `D:/Users/etgya/Desktop/milestone/发布/Milestone.exe` → 相对化（BaseDirectory 上溯 ..\..\..\..\发布\Milestone.exe = 输出产物\发布）。

## ③ 内容散外收口：MpLeaderboard
- `游戏源码\源码\Mp\MpLeaderboard.cs`：`%LocalAppData%\ChartPlayer\leaderboard.json` → `AppDomain.BaseDirectory\PlayerData\mp_leaderboard.json`；新增 `MigrateLegacy()`（新位置无文件而旧位置有 → 复制迁移），`Load()` 开头调用（读兼容旧数据）。

## ④ 重置用户 config ChartsFolder 到默认程序目录
- `游戏源码\源码\CoreUtil\AppConfig.cs`：新增 `ChartsFolderMigrated` 一次性迁移标记——旧配置 ChartsFolder 非 `BaseDirectory\Chart` 且非空时重置为默认并落盘；两次 Load 兜底。
- 迁移脚本同步：用户 `%LocalAppData%\ChartPlayer\config.json` 现测 `ChartsFolder` 已清空（AppConfig.Load 回落默认=exe 旁 Chart；发布Chart 首启内容 277 文件已就绪）。
- `游戏源码\源码\CoreUtil\ScreenCapture.cs`：CaptureDir 移除「向父目录找 Milestone.csproj」的旧开发探测，固定 `BaseDirectory\屏幕采集\`。

## ⑤ 示例谱/段位/测试格式拷贝进 发布\Chart 与 bin\Chart
- `输出产物\发布\Chart\`（首启内容，277 文件含 Milestone示例/实例谱面/段位挑战/测试格式/皮肤）与 `输出产物\bin\Chart\` 均已从 示例内容 全量拷贝（robocopy /E）。

## ⑥ 迁移后验证
| 验证项 | 结果 |
|---|---|
| 游戏构建 | 0 警告 0 错误（输出产物\bin\Release\…\\Milestone.exe 9.2MB） |
| --selfcheck | exit 0（构建产物 输出产物\bin 树） |
| EngineChecks | 0 警告 0 错误 + exit 0（滑条/长条闭环 全部通过） |
| 发布件 | 输出产物\发布\Milestone.exe（157MB 单文件）在树 |
| 桌面快捷方式 | 已重建 ×2（Milestone 里程碑.lnk / Milestone（里程碑）.lnk → 输出产物\发布\Milestone.exe） |
| parity 快照 | 结构校验=根 4 分类 + .agent-teams；parity 全量重跑归 captain 官方（本任务未构建归口） |

## 交付文件
- `游戏源码\Milestone.csproj`（新路径+OutputPath+Samples Include）
- `游戏源码\源码\Mp\MpLeaderboard.cs`（BaseDirectory 排行榜+旧数据迁移）
- `游戏源码\源码\CoreUtil\AppConfig.cs`（ChartsFolder 一次性重置）
- `游戏源码\源码\CoreUtil\ScreenCapture.cs`（CaptureDir=BaseDirectory）
- `输出产物\dev\engine-exe\build-tool.ps1`（$repo/引擎/引擎源码/docs 路径修正）
- `输出产物\dev\obj\caprepro\Program.cs`（相对化 exe 路径）
- `编辑指南\README.md`（追加 路径映射表 t62）
- `输出产物\dev\迁移快照\`（S0 快照 + pre-migration-backup 802 文件 + 迁移记录.md）

## 备注
- 官方构建/发布/parity 由 captain 执行（本任务只做迁移+代码+冒烟验证）。
- 迁移期间并发产生的 t71-check/play 产物已归入 输出产物\dev\obj\。
- 文档历史路径保持原样（设计 2-D 政策），活跃文档（README）已附映射表。