# t122 实现报告：v3 M2.6 Python 绑定层（rhygemaker 包——ctypes 经同一 C ABI）——eng-coder-vis

## 一、交付（按 t121-m26-python-design.md + P-1..P-7；RhygeMaker 正式名下实施）

### python/rhygemaker/（纯 Python 零第三方——ctypes/unittest 仅）
- **_interop.py**：cdll.LoadLibrary（RHYGEMAKER_DLL 环境变量/同目录搜索 + **os.add_dll_directory**（Win10+ 安全加载——PATH 不再兜底依赖；RHYGEMAKER_MINGW 登记 mingw 依赖目录））——47 个 ms_* 函数签名（43 ABI+4 脚本桥；ms_ 前缀=稳定 ABI——品牌改名不换）+ 错误码常量 + BindException（MS_ERR 消息）+ ComponentSpec 结构 + CALLBACK_VOID/DT + c_* 别名
- **component.py**：PyComponentBase（八回调 snake_case：awake/on_enable/start/update/fixed_update/late_update/on_disable/on_destroy——Unity 顺序 C++ 侧）+ _ComponentRegistry（userData=id(inst) 强引用保活=GCHandle 对称；instanceKey=FullName#n 多实例安全；_thunks 保活 CFUNCTYPE 防 GC）+ 静态 thunk 桥
- **engine.py**：GameEngine（title/width/height/root/assets/run_frame/run/save_scene/load_scene/root_count/render_hash/last_frame_ms/live_engine_count/resize/dispose/__enter__/__exit__）
- **gameobject.py**：GameObject（id/name/transform/add_component(cls)（registry 注册+八回调立即 Awake/OnEnable）/get_component/add_child/set_active/destroy（延迟帧尾））
- **transform.py**：Transform（position/rotation（quat wxyz 列表）/scale/parent/set_parent(keep_world)/detach——ms_transform_* ABI）
- **assetstore.py**：AssetStore（set_root/load→Asset/type/guid/unref——引用计数/save_binary/save_text）
- **scene.py**：Scene（build/update——与 C# IScene 对称）；**__init__.py**：公开面+KeyCode（IntEnum）
- **__main__.py**（py_demo）：--renderhash（120 帧→ms_engine_render_hash 直读——不重复实现哈希）==4634E387E024BE90（**三语言 parity：C++/C#/Python**）；--pf1；--pf2；--run-frames N
- **cookbook/**：PF1_Minimal（Score 计数器）+ PF2_Lifecycle（八回调序断言==官方）

### tests/test_py_bind.py（unittest 零依赖——退出码=失败数）
9 用例：①句柄配对（×5）②八回调序==Unity 官方（精确）③Transform pos/rot quat/scale/parent 往返④组件注册多实例（FullName#n+registry 长度+dispose 释放）⑤资产（bmp load/type/guid 16/unref）⑥场景 save/load round-trip⑦跨线程→BindException(MS_ERR_THREAD)⑧TraversalDenied→BindException(5)⑨renderhash parity==黄金帧

## 二、验收 10 项对照
| # | 项 | 状态 |
|---|---|---|
| 1 | 导入/加载 | ✅ import rhygemaker（DLL 经 RHYGEMAKER_DLL/RHYGEMAKER_MINGW+add_dll_directory——README 注） |
| 2 | 生命周期 | ✅ py_demo --pf2 序==Unity 官方精确（awake,on_enable,start,fixed_update,update,late_update×2 帧） |
| 3 | 对象/变换 | ✅ add_child/set_active/destroy+Transform quat/parent/keepWorld 往返（test） |
| 4 | 组件注册 | ✅ add_component(cls) 多实例（FullName#n）+registry 长度（+2→dispose 0） |
| 5 | 资产 | ✅ load(bmp)/type/guid/unref（引用计数——C 端缓存） |
| 6 | 场景 | ✅ save/load round-trip（.mscene 容器+校验）经 Python |
| 7 | 线程防御 | ✅ 非主线程→BindException(MS_ERR_THREAD) |
| 8 | 错误码 | ✅ TraversalDenied→BindException(5)（UNKNOWN_FIELD 同理——错误码映射） |
| 9 | parity | ✅ py_demo --renderhash ==4634E387E024BE90（三语言） |
| 10 | cookbook/测试 | ✅ PF1 score=120 PASS/PF2 PASS；test_py_bind.py 9 用例 OK exit=0 |

## 三、验证记录（本机实际运行）
- Python 3.14.7：import ok；py_demo --renderhash PARITY OK / --pf1 PASS / --pf2 PASS / --run-frames 3 ok（hash 稳定）
- test_py_bind.py：Ran 9 tests OK（1.5s）exit=0
- 零依赖核实：仅 ctypes/struct/unittest/math/threading/tempfile（标准库）
- 双源：引擎源码/rhygemaker ↔ D:\Desktop\rhygemaker **111 文件 MD5 逐字节一致**（含 python/）

## 四、偏离/决策记录（评审需知）
1. **DLL 加载=add_dll_directory**（P-6 补强）：ctypes 现代加载语义 PATH 不兜底依赖——DLL 目录+RHYGEMAKER_MINGW 登记（零第三方）。
2. **userData=id(inst)+registry 强引用**（与设计 §3.1 一致——Python 对象指针经 c_void_p）。
3. **回调 snake_case**（设计对称表即 snake_case——C# camelCase ↔ Python snake_case 映射表：awake/on_enable/start/update/fixed_update/late_update/on_disable/on_destroy）。
4. **ms_ 前缀保留**（P-4——稳定 ABI 43+4；品牌改名不换）。
5. **py_demo --renderhash 经 C 端 ms_engine_render_hash 直读**（不重复实现 FNV——设计明确）。
6. **旧 D:\Desktop\milestone-v3 路径废弃**（RhygeMaker 改名后 ASCII 源=D:\Desktop\rhygemaker；本报告仅在新路径验证）。

## 五、边界
- captain 构建：build-v3c.bat（D:\Desktop\rhygemaker）+python 验证命令（README §M2.6：PYTHONPATH=python+RHYGEMAKER_DLL/RHYGEMAKER_MINGW 环境变量）
- 嵌入 CPython=P1；pytest=P2；多线程（PyGILState）=P1（单线程主线程文档明示）
- API 对称维护约定（P-7）：C# 新 API 同步 Python（附录对照表=t121 §2）