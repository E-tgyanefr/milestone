# 引擎树事故报告与恢复指南（2026-08-30 凌晨）

## 事故经过
1. 用户批启动后，被移除代理（eng-coder-vis 末回合 panic 恢复）持续以旧代覆盖 ASCII+镜像树（editor 77 用例代/rhythm 7 用例代/scripts 缺失），并间歇删除文件、文件常被置 ReadOnly。
2. captain 多轮恢复与并发写者冲突；最后一步全量清理 src/include/tests 后快照还原，因快照未含全部文件，树处于部分可建状态。

## 事实基线（未受损）
- 发布物完好：Output产物发布RhygeMaker bin + Desktop RhygeMaker-Setup.exe（23:11:16，21/21）。
- 文档完好：t132-m4-design.md（Y 终态/联签/§17.3）、milestone-game-acceptance.md、GUI-波3-新功能复验协议.md、T-ANIM-UI-游戏层实施.md、部署配方-exit2消除.md、待决策-基线快照.md。
- 代码快照：.agent-teams panic-snap-0030（00:24 全代：rhythm Y-final 全套、ms_bind含X2、text_raster、editor30测试、dotnet、cookbook、data/mil 25）——恢复主源；panic-snap-0031=旧代勿用；wip-snap-1320/1405=editor早代备份。
- X1-X4 引擎增量（ms_rnd_*10+2 / ms_text_* / ms_input_mouse / get_renderer+present / TextRaster / BlitRectAlpha / 输入扩展）实现在本会话完整上下文，可一步重放；msjson 重建版已写盘。

## 恢复步骤（单写者=captain；全员休眠）
1. 增量覆盖还原：robocopy panic-snap-0030 → 双树（按映射：Rhythm/*→src|include|tests|dotnet Bind Rhythm；根级按名映射；data mil→tests data mil；Milestone.Game→dotnet；csdemo→dotnet）。切勿先删后拷。
2. 写四清单：根（含 src/rhythm）、src/platform（+text_raster.cpp）、src/render3d（+demo3d.cpp）、src/bind（链 platform+rhythm）。
3. 补 X 件（缺失时）：input/win32_input X4、renderer BlitRectAlpha+软渲实现、engine SetAutoPresent/PresentWindow、ms_bind.h/cpp X、C# Interop+GameEngine（IRenderer/NativeRenderer/延迟呈现/鼠标）。
4. 全量构建（-G MinGW Makefiles；清 build/；mtime 问题=删 build 下 *.obj）。
5. 验证：6 套件+corpus 25/25+acc 21/21+黄金帧；重锁四件；重打包。

## 最终处置
恢复完成后：双树 MD5 对齐→发布与锁一致→按用户批继续（已有全部游戏层实现=X1-X4 接驳即可：设置 UX/菜单精简/性能测试/大击特效/osustd/BeamTransition 均已就绪）。