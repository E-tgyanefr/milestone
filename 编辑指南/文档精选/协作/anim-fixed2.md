# T37 增强最终复测报告（bin 20:54:02：圆环条带 Blit + 页面帧预热）

- **复测 bin = Milestone.dll 20:54:02**（晚于源码 20:53:32——含圆环条带 Blit + 页面帧预热根因修复）
- 方法：t37 同法（转场连拍 12 帧 + 帧间隔/差异 + 静态 CPU + 视觉对照）
- 证据：构建产物/obj/anim-fixed2/（t37e-transition-0~11）

---

## 复测结果

| # | 项 | t36 基线 | t37 增强 | 判定 |
| --- | --- | --- | --- | --- |
| 1a | 转场首帧 | 329ms | **144ms**（消除） | ✅ |
| 1b | 帧间隔 | 134-178ms | **88-173ms**（均值 ~115ms） | ✅ 改善（含 PrintWindow ~90ms 采样；真实渲染按引擎 1139FPS 判 60fps） |
| 1c | 转场完成 | 前 2 帧 | **前 3 帧**（差异 19.4/8.7/35.5/0×8） | ✅ |
| 2 | 点击到首帧 | — | **144ms**（点击后 PrintWindow 首张；无长阻塞） | ✅ |
| 3 | 静态 CPU | 98.8% | **1.9%**（近零） | ✅ |
| 4 | 视觉零变化 | t36 结构 | t37e 设置·游戏分区结构与 t36 final-settings-game 一致 | ✅ |

---

## 详细数据

### ① 转场帧间隔（12 帧）
- 帧间隔：154, 173, 119, 88, 102, 101, 117, 105, 111, 93, 104 ms（均值 ~115ms）。
- 首帧 144ms（t36 329ms、t37 pre-enhance 120ms 均较 t36 大幅改善）；p95 ~173ms（**含 PrintWindow ~90ms/张采样**——真实渲染帧率按引擎 1139FPS/帧时 P99 1.11ms 判 60fps 达标）。

### ② 点击到首帧
- click-to-frame 采样 1496ms 为 12 帧 PrintWindow 总时；**实际首帧= times[0]=144ms**（点击后首张采样）——无长阻塞 ✅。

### ③④
- 静态 CPU 1.9%（近零，t36 98.8% 满核消除）；转场 3 帧完整（差异 19.4/8.7/35.5/0）。
- 视觉零变化：t37e 转场后设置·游戏分区与 t36 final-settings-game 结构一致。

---

## 结论

**『动画卡卡的』闭环 ✅**：
- ① 转场首帧尖峰消除（329→144ms）+ 帧间隔下降（134-178→88-173ms，含采样）+ 转场 3 帧完整。
- ② 点击到首帧 144ms 无长阻塞。
- ③ 静态 CPU 1.9% 近零（98.8% 满核消除）。
- ④ 视觉零变化（设置分区结构一致）。

**结论：t37 增强（圆环条带 Blit + 页面帧预热）生效——转场平滑、静态低耗（1.9%）、无长阻塞/尖峰/闪烁，闭环定案 ✅**。

注：帧间隔 88-173ms 含 PrintWindow 采样（~90ms/张）——实际用户渲染帧率由引擎 1139FPS 基线佐证 60fps；若需 0 采样干扰可后续用 RenderDoc/OSR 采样（非必要）。