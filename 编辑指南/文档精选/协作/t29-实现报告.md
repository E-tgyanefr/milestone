# t29 实现报告：全文显示修复（省略号截断 → 缩字号完整显示 / 加宽重排）

- 日期：2026-08-27 · 承办 coder-max · 约束：只写代码不构建（captain 构建 + 试玩【全文可见性+零重叠】双验）
- 原则：全文必须可见，禁「…」；优先缩字号（下限 8-9），仍不足才加宽/换行重排；全部为放宽改动，零收紧、零新重叠面。

---

## 逐处记录（位置 / 修法 / 字号）

### 曲库管理页（SecondaryPages.BuildFolderPage）
1. **筛选 10 钮**（原单行固定宽 80/96/130 + Ellipsis）：改**两行 5+5**（y=190/230，行高 34、行距 6），按钮宽=按文本自适应
   （新 UiMeasure.ButtonWidth：文本宽+左右内距 12，min 56）。字号 10 不变、无省略号；两行文本合计实测 ≈490/622px < 1184 可用，不重叠。
2. **路径/统计/列表标题标签**：`Ellipsis=true` → `AutoShrinkFont=true`（引擎下限 9）；14pt 基准，超宽自动缩至全文可见。
3. **底部按钮组 9 钮**（原固定 90~260 宽 + Ellipsis）：改**按文本 AutoSize**（ButtonWidth(文本, 14, 12, min 56)），
   行内间距 12、行 y=536/584；行1 合计 ≈554px、行2 ≈436px < 1184。
4. **文件名行**（原 UiButton+Ellipsis）：改 **ClickableCard + 子 UiLabel**（AutoShrinkFont 下限 9，全宽 1160）——
   长文件名缩字号保全文；点击行为不变（同一 handler，整卡命中）。
5. 页面垂直流同步下移：列表标题 230→270、列表 258→298（高 200 不变）、提示 466→506、按钮 496/544→536/584；底 620 < 800。

### 主菜单（EngineMainShell.BuildMenu）
6. CTA「▶ 开始游戏」：去 Ellipsis（文案 ≈124px ≪ 460 宽，本就是全文）。
7. 统计两行（440 宽）：`Ellipsis` → `AutoShrinkFont`（下限 9；现文案 ≈250px 不触发缩小）。
8. 快捷玻璃卡/功能卡标题：`TitleFont` 改 `UiMeasure.FitFont(文本, 可用宽, 基准)`（快捷卡可用 128、功能卡可用 258；下限 8）
   ——引擎 UiCard 内建标题省略在适配字号后不再触发。

### 设置页（EngineMainShell）
9. **信息行 RowInfo**（评级 3 行 / 判定·界面·存档提示 / **键位「退格→返回」提示（P2 旧记录）**）：弃 Ellipsis，保留
   AutoShrinkFont（下限 9，全卡宽 866）——键位提示 30 字 ≈270px 全文可见。
10. 设置 Row 行标签/值列：t22b 已为 AutoShrinkFont、无 Ellipsis（值列 510/244、标签 350/178）——**无需改动**（记录）。

### 玩家信息页（SecondaryPages.BuildPlayerPage）
11. 「打开设置存档分区」按钮：去 Ellipsis（8 字 ≈112px ≪ 240 宽）。

### 编辑器（ChartEditorPanel）
12. 工具栏/右栏页签/各组按钮：t30/t5 已全量 AutoSize（GrowAndShrink）+ AutoSize 标签——**无截断路径**（记录；页签头另见 t24）。

### 二级明细（legacy WinForms）
13. AI 设置窗谱面路径标签（AiSetupForm）：`AutoEllipsis+MaximumSize(470,60)` → `AutoSize+MaximumSize(470,0)` 换行全文（WinForms 原生行距，无叠）。
14. 制谱助手音频标签（ChartMentor）：`AutoEllipsis` → `AutoSize+MaximumSize(300,0)` 换行全文。
15. legacy 曲库面板路径标签（FolderPanel）：`AutoEllipsis` → `AutoSize+MaximumSize(1000,0)` 换行全文。
16. legacy 选歌卡谱名/作者（SongCardView）：`StringTrimming.EllipsisCharacter` → **FitFont 缩字号**（下限 8pt、NoWrap）全文。
17. 键位 legacy SettingsPanel 提示 Label：本就 AutoSize（记录）。

---

## 新增工具
- `源码/CoreUtil/UiMeasure.cs`：GDI 文本宽（emoji 按引擎 1.15em 口径补足）、FitFont（线性适配、下限 8）、ButtonWidth（文本+内距）。
  构建期一次性调用（非帧内）；与 GdiDrawAdapter 同字体族（Microsoft YaHei UI）。

## 变更文件
- 新增：`源码/CoreUtil/UiMeasure.cs`
- `源码/Play/EngineUi/SecondaryPages.cs`（曲库页重构 + 玩家按钮）
- `源码/Play/EngineUi/EngineMainShell.cs`（CTA/统计/RowInfo/卡标题）
- `源码/Forms/AiSetupForm.cs`、`源码/Charting/ChartMentor.cs`、`源码/Forms/FolderPanel.cs`、`源码/Forms/SongCardView.cs`（legacy 二级明细）

## t29 补（t28 P1，随 t29 批次）：选歌页「🎵 选歌」标题图标-文字间距
- 位置：选歌页标题（x≈64..135, y≈44..70 区域贴叠）。
- 根因：emoji 绘制宽 > 1.15em 推进宽——单标签内 🎵 与「选」首字贴叠。
- 修法：标题拆双标签——图标标签（🎵，x=64，W=36）+ 文字标签（选歌，x=108 = 64+36+**8**，W=126）；
  显式间距 +8，文字尾 148 < 搜索框 232，无碰撞；UiText.SongTitle 常量未改。

## 试玩双验口径（captain 构建后）
1. 全文可见性：曲库筛选 10 钮（两行全文案）、底部按钮 9 钮、长路径/长文件名、设置键位提示、玩家信息按钮、AI 设置窗路径、制谱助手音频名。
2. 零重叠：曲库页新垂直流（筛选两行/列表/按钮）、设置 9 分区行、主菜单统计/卡片——按 r7 基线逐页复查。
