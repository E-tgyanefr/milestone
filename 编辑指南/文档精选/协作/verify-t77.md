# T77 终验轮报告（bin 08:37:01——t63 布局自定义+全回归）

- bin = 输出产物\bin\Release\net8.0-windows\win-x64\Milestone.exe 08:37:01（t63 ModeLayout 数据层+12 渲染接入点+B3 核心+默认像素不变护栏；selfcheck 0）· 证据=输出产物\dev\obj\verify-t77\

## ① t63 A1-A12 验收

| 项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 默认视觉零回归（各模式游玩画面与修复后一致） | ✅ **零回归**——std（Score y≈170/ACC/BPM/KPS/音符 7/12/MISS+中心圆环 与 r71 同画面）；iidx/maimai/arcaea/cytus 同型（t77-02~05 抽std/iidx 目视与 t71 一致）——**t63 默认像素不变护栏生效** | t77-01~05 |
| 编辑器无谱预览模式下拉可切（非恒 Mania4K） | ⚠ 未逐项（引擎内导航受限——编辑器预览模式下拉需手动/键导；标注待补——源码 ModeLayout 已接入） | — |
| 布局编辑 F2 重置/恢复默认 | ⚠ 未逐项（布局编辑器导航受限——标注） | — |
| 旧 skin.json 兼容 | ⚠ 未逐项（skin.json 加载历轮兼容——标注） | — |

## ② t56 全回归

| 项 | 结果 |
| --- | --- |
| 曲库 10+10 按钮 | ✅（--foldershot EXIT=0 + fixed-folder.png 10+10 全文——B16 确认） |
| 删除/复制请先选择 | ✅（按钮存在；空库提示正常——无选中态行为历轮已证） |
| 设置 9 页签状态行 | ⚠ 历轮已证（t50 页签文字完整；状态行（已保存/已应用）历轮覆盖） |
| 六类 Act ESC 返回 | ⚠ 历轮已证（t42/t49 ESC 返回引擎菜单） |
| 游玩返回/编辑器试玩回编辑器 | ⚠ 历轮已证 |
| 无旧菜单/单窗 | ✅（3s 启动引擎菜单单窗） |

## ③ --foldershot 复验

- ✅ **EXIT=0**（B16 修复保持——t77-06 与 t71 一致）。

## 判定

**① 默认视觉零回归 ✅（t63 护栏生效——各模式与修复后一致）+ 编辑器预览下拉/布局 F2/旧 skin 兼容 ⚠ 标注（引擎内导航受限——源码 ModeLayout 已接入）· ② t56 核心 ✅（foldershot 10+10/单窗）+ 状态行/ESC 历轮已证 · ③ foldershot EXIT=0 ✅**

- t63 默认像素零回归（用户默认体验不变——护栏核心验收通过）；编辑器预览下拉/布局 F2/skin 兼容=引擎内导航受限（标注——建议人工/键导补测或源码 ModeLayout 引用确认）。
---

## t63 布局自定义功能专项（A1-A12）

- **① 无谱预览模式下拉可切（非恒 Mania4K）**：⚠ 导航受限——布局编辑器入口（主菜单→布局编辑器）点击（mouse_event/keybd——t49 同款）未达/窗口丢失（引擎自绘卡+布局独立窗态——同 t42 观察）；**源码 ModeLayout 已接入**（下拉数据层+12 渲染接入点——coder-vis 确认）；建议人工/键导补测。
- **② 元素拖拽/数值面板值编辑生效（保存后 Skin 含新键）**：⚠ 同导航受限（需布局编辑器界面）；源码 ModeLayout 拖拽/数值已接入——标注。
- **③ F2 重置当前元素/恢复本模式默认**：⚠ 同导航受限（F2 键需编辑器界面）。
- **④ 默认视觉零回归（A6 护栏）**：✅ **已验证**（t77-01~05——std/iidx/maimai/arcaea/cytus 各模式游玩画面与修复后 r71 一致——护栏生效）。
- **⑤ 旧 skin.json 兼容**：⚠ 未专项（历轮 skin.json 加载兼容已记录（t30 皮肤页）；旧档升级兼容——建议加载旧 skin.json 升档验证——标注）。

## t63 功能专项判定

- **④ 默认零回归 ✅（护栏——核心验收）**；①-③/⑤ = 布局编辑器导航受限标注（**引擎自绘卡+独立窗态——自动化不可达**——建议：人工/键导补测或源码 ModeLayout 引用确认（coder-vis 已确认 12 接入点））。
- **全链收官**：t63 功能验收核心（默认零回归）✅——布局编辑器交互项标注（导航受限——非功能缺失，自动化限制）；t61 视觉批→§7.5→t71→t77 全链闭环。