# T104 实现报告：GamePanel 巨型行拆分（格式化 + partial + 行宽≤200）

## 任务

- 对象：`游戏源码\源码\Play\GamePanel.cs`（拆分前 3650 行 / 18 个超长行；最坏单行 L43 = 42114 字符 ≈ 41KB——一条语句/整块方法被压成单行）。
- 目标：① 全文件格式化（Roslyn）② 拆分 partial（按职责）③ 行宽≤200 ④ parity + selfcheck + 游玩回归（parity 全绿 + SG dense 60s FPS 无回归）。只写代码不构建（scratch 构建仅验证格式/回归，官方 bin 未动）。

## 实施

### 1. 格式化（Roslyn 工具，scratch）

- 工具：`输出产物\dev\t104-scratch\fmt`（引用 gptmp-rp 的 Microsoft.CodeAnalysis DLL）。
- 流程：Roslyn Parse（输入 0 诊断）→ `root.NormalizeWhitespace("    ", \r\n, false)` → 输出再 Parse（输出 0 诊断）→ 写盘。
- 结果：3650 行 → 7850 行，剩余 22 行 >200。

### 2. 行宽≤200（分隔器：括号深度≥1 的逗号；回退深度 0 的 ?/:；跳过字符串/字符/注释；绝不触碰行内 //）

- 作用：22 个 >200 行 → 全部 ≤199（残剩 1 个字符串拼接行手工断行：a.Name + "  " + … 链）。
- 全程仅插入换行+缩进，未删改任何字符；行内 `//` 注释原样保留（跳过逻辑保证分隔点永不在注释/字符串内——规避「行内注释吞码」事故）。

### 3. partial 拆分（按职责，Roslyn 成员级）

工具：`输出产物\dev\t104-scratch\split`（按成员最近的前置 banner 注释归类）：

| 目标文件 | 成员数 | 内容 |
| --- | --- | --- |
| `GamePanel.cs`（Core，partial 主） | 180 | 字段/构造/公共 API/引擎与渲染入口/状态机等 |
| `GamePanel.Play.cs` | 97 | 各模式渲染/多场同屏/NoteStyle/深度排序/多条判定线/BGA/3D/osu!std/ADOFAI/maimai/回环作曲等 |
| `GamePanel.Editor.cs` | 83 | 布局编辑器扩展（t63 B3）+ 布局自定义（t63 B1/B2） |
| `GamePanel.Hud.cs` | 14 | 布局/渲染辅助（PlayLayout、IsHoldNote 等） |
| `GamePanel.Result.cs` | 3 | 结算画面 |

- 主文件 `GamePanel.cs` 声明改为 `public partial class GamePanel : Panel`；各 partial 文件为 `public partial class GamePanel`（基类只出现一次，合法）。
- 每个 partial 文件带完整 using 集 + namespace 包裹（可直接编译）；成员文本 = Roslyn `member.ToFullString()`（含前置 XML 文档注释/属性）。

## 验证（全绿）

| 项 | 结果 |
| --- | --- |
| 成员恒等（Roslyn 语义级） | 原 377 成员 vs 5 文件聚合 377 —— 归一化成员文本全集 **IDENTICAL=True**（无丢失/无重复/无改动） |
| 各文件 Parse | 5/5 —— 0 错误 |
| Scratch 全项目构建（复制源码树+引擎树） | **0 警告 0 错误**（Milestone.dll 产出） |
| parity（scratch exe `--parity`） | 玩法 10/10 `identical`；壳页面 menu/songs/settings `ok`；条目 54：一致 53 + 已知偏差 1（Arcaea 10M 计分口径——基线既有偏差，非本次引入）；divergent=0 |
| SG dense 60s（Mania 4K.mil，目标 120 FPS） | **平均 446.9 FPS ≥ 120 → PASS**；帧时 P99 16.70ms；GC 0.75MB/s（无回归） |
| 行宽 | 5 文件全部 ≤199（maxLen：181/195/159/199/120；0 行 >200） |

> 注：任务「只写代码不构建」条件——官方 `输出产物\bin\Release` **未构建**；以上 regression 均基于 scratch 复制的源码树+引擎树构建（仅用于验证格式与回归），源文件为唯一落盘改动。

## 文件清单

- 替换：`游戏源码\源码\Play\GamePanel.cs`（格式化为 1287 行 partial core）
- 新增：`游戏源码\源码\Play\GamePanel.Play.cs`（3638 行）、`GamePanel.Editor.cs`（2812 行）、`GamePanel.Hud.cs`（527 行）、`GamePanel.Result.cs`（58 行）
- 备份：`输出产物\dev\obj\backup-t104-orig\GamePanel.cs.bak`（293579B 原文件）
- 工具/证据：`输出产物\dev\t104-scratch\`（fmt/split/check/wrap + parity-out + SG-dense + build 树）

## 结论

- 拆分后原巨型文件职责清晰：Core/Play/Editor/Hud/Result 五文件；行宽全部 ≤200；成员语义零变化（IDENTICAL）。
- parity 全绿仅剩基线已知偏差；SG dense 60s FPS 无回归。交付满足「只写代码不构建」约束，scratch 仅用于验证。
