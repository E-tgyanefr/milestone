# T44 复验报告（t43 四点 + t42 八项；bin 21:45:45）

- 复验 bin = Milestone.dll 21:45:45（晚于源码 21:43:26；selfcheck exit 0）· 证据：构建产物/obj/verify-t44/

## A. t43 四点复验

| # | 项 | 结果 | 证据 |
| --- | --- | --- | --- |
| A1 | 编辑器·事件 Tab 长说明行换行全文 + tooltip | **部分 ❌**：说明行「(表格线上)/添加事件时弹文件写入文件件写入」「表格时弹的(弹开写心)」**仍穿插**（与 t41/x43 相同——换行修复未覆盖事件 Tab 说明行）；tooltip 悬停自动未弹出（自绘 UI 自动化难触发——静态未证 tooltip） | t44-a1-event.png + tooltip2 |
| A2 | 编辑器·判定线 Tab 说明行全文无互压 + tooltip | **不通过 ❌**：说明行「轨道(Phigros)数量…编辑线」「数量上限…父链/父事件」「细线(母线)」「跨到一条线(请用时)」「(父移动起始)因链」「滑动(phemos 父+线)」**多行互压**（与 t41-03/t43 相同——未修） | t44-a2-judgeline.png |
| A3 | AI Tab 标题「…规则检查仍可用）」完整 | **通过 ✅**：标题「AI 与校准（本地 AI 未启」+「用，规则检查仍可用）」换行两行完整 | t44-a3-ai.png |
| A4 | 结算页底部提示行完整（panelW-40 自适应） | **通过 ✅**（x43-05 已证「2026-09-06 最高 50」完整；本 bin 同源） | x43-05 |

## B. t42 八项走查

| # | 项 | 结果 | 证据 |
| --- | --- | --- | --- |
| B1 | 默认启动直接引擎化 UI（无旧菜单闪现） | ✅ 启动 3s=引擎菜单；可见窗口=1（m42-01 已证，本 bin 同源） | m42-01 |
| B2 | 联机→返回→引擎菜单 | ✅ ESC 返回=引擎菜单（联机页 WinForms 遗留；ESC 净化） | m42-07 |
| B3 | AI 演示→返回→引擎菜单 | ⚠ **P1 观察**：AI 演示 ESC 后主窗句柄=0（窗口未自动回；m42-08/09 已证） | m42-08/09 |
| B4 | 布局编辑器→返回→引擎菜单 | ⚠ 未逐项（布局编辑器为引擎页——返回同 ② 模式，待补） | — |
| B5 | 回环作曲→返回→引擎菜单 | ⚠ 未逐项（同 B4） | — |
| B6 | 迷你 mania→FormClosed→引擎菜单 | ⚠ 未逐项（独立演示窗同 B3 风险） | — |
| B7 | 引擎 UI 演示→返回→引擎菜单 | ⚠ 未逐项（同 B6 风险） | — |
| B8 | --legacyui 仍显示旧主菜单 + 单进程 | ⚠ 未验证 --legacyui；单进程=默认 1 引擎窗（B1） | — |

---

## 判定

**A：1/4 通过（A3 修、A4 修；A1/A2 事件+判定线说明行仍穿插）**
**B：核心 2/8 走查（B1 默认/B2 联机通过；B3 AI 演示 P1 观察；B4-B7 未逐项；B8 未验证）**

### ❌ 根因分析
- **A1/A2 事件/判定线说明行穿插**：t43 换行修复未覆盖 ChartEditorPanel 右栏属性速览/判定线 Tab 的说明行控件（该行的 WordWrap/Wrap 尺寸未设——多行文本超 240 宽后行间互压，无 AutoShrinkFont 限宽）。
- **B3 AI 演示 ESC 主窗句柄=0**：引擎演示/独立 WinForms 演示窗 FormClosed 后未 Show 主窗（主窗句柄丢失）——演示窗关闭需显式回引擎菜单。

【修复方向】①事件/判定线说明行加 AutoShrinkFont+Wrap（同事件 Tab 已修逻辑复用）②演示窗 FormClosed 回调显示主窗。

注：--legacyui/B4-B7 未逐项（时间/自动化限制）——如实标注；B1/B2 核心净化已证。