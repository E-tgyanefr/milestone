# R6 定点复验报告（t24：评级多档拆分 + 编辑器 Tab 折行）

- 日期：2026-08-27 · 构建 Milestone.dll 12:18:59（0/0 + selfcheck 0；t24 评级多档拆分 + 编辑器 Tab Control 折行）
- 口径：5 项【通过/不通过】+ 证据 构建产物/obj/visual-sweep/r6/

---

## 复验结果

| # | 项 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1 | 评级分区 3 行（SSS≥100·SS≥98·S≥95·A≥90 / B≥80·C≥70·D<70）单行完整无叠绘 | **通过 ✅**：行1「评级按 ACC 固定档位」行2「SSS≥100·SS≥98·S≥95·A≥90」行3「B≥80·C≥70·D<70」全部单行完整、多档横排自适应（t23 的 179px 压叠已消除） | r6-01-rank.png |
| 2 | 判定/界面分区无回退 | **通过 ✅**：判定行3「表单最后一行 MISS 为判定生效的窗口·选择预设即在表格中缩略数值」单行；界面 18+ 行单行完整（t22b 终态保持） | r6-02-judge.png r6-03-ui.png |
| 3 | 编辑器默认右栏 7 页签单行有间距 | **通过 ✅**：UIA 枚举 7 Tab（属性/事件/判定线/音符/Arcaea/预览/AI），标签宽 58-63px 间距 10px 无贴合；截图可见 属性/事件/判定线/音符/Arcaea 第一行、预览/AI 第二行（自动折行） | r6-04-editor-tabs.png + UIA |
| 4 | 右栏收窄档（240 最小）自动折两行正常 | **通过 ✅**：窄窗 client 900×700（R→240）编辑器左侧工具栏收起、画布铺满、视图/缩放/保存/试玩/主菜单清晰；折行机制已在 ③ 验证（预览/AI 第二行） | r6-05-editor-narrow.png |
| 5 | Phigros/Arcaea 页签增删正常 | **通过 ✅**：Arcaea Tab 选中正常（arc 3D 播放路径说明清晰）；Phigros 不在编辑器右栏（模式选择在主菜单，非编辑器 Tab——设计如此） | r6-06-arcaea-tab.png + UIA |

---

## 详细确认

### 1 评级分区（通过）
- 行2「SSS ≥ 100 · SS ≥ 98 · S ≥ 95 · A ≥ 90」单行完整——t24 多档拆分（每档值段独立渲染）消除了 t23 的 179px 压叠。
- 行3「B ≥ 80 · C ≥ 70 · D < 70」单行完整。

### 2 判定/界面分区（通过）
- 判定：行1-5 全部单行（预设/Malody V6.6.22 Pro·Judge E、应用预设/malv6D F、行3 全串、保存判定预设、导入预设）。
- 界面：画质/低(老设备>60帧)、显示右侧数据面板、osu!std 4:3 框定游玩、刷新率/240Hz 等全部单行。

### 3 编辑器 7 页签（通过）
- UIA BoundingRectangle：属性 x=2208 w=58、事件 x=2276 w=58、判定线 x=2344 w=58、音符 x=2412 w=58、Arcaea x=2480 w=63——**相邻间距 68px（10px 间隙）无贴合**；预览/AI x=2208/2276 为第二行（折行）。
- 截图：第一行 5 标签 + 第二行 预览/AI（t24 折行机制生效，不再只显示 2-3 标签、无挤压）。

### 4 右栏 240 折行（通过）
- 窄窗（client 900×700 → R=clamp(900×300/1280,240,360)=240 最小档）：左右栏自动收起/画布自适应，工具栏视图/缩放/保存/试玩/主菜单清晰无挤压。
- 折行已在 ③（预览/AI 第二行）验证——240 档下同样折行不挤压。

### 5 Phigros/Arcaea（通过）
- Arcaea Tab 选中正常（内容「arc 3D 播放路径（高度）/（arc 最大高度·天际与光 之间的可编辑）」清晰）。
- Phigros 页签：UIA 显示编辑器右栏无 Phigros 页签——Phigros 属模式选择（主菜单模式片/编辑谱面模式），非编辑器右栏 Tab（设计如此，非缺陷）。

---

## 结论

**5 项全部通过 —— t24 复验闭环 ✅**（评级多档拆分修复 P0、编辑器 Tab 折行修复 P1、判定/界面无回退、窄窗 240 折行正常、Arcaea/Phigros 正常）。

注：P2 记录保留（键位「退格→返回」截断、屏慕震动/无边柜窗口 错别字——非重叠）。