# t82 验收评审：t81 SDK 批A 对照 G1-G8（eng-design-vis 评审 · 只评审不写码）

> 评审对象：t81（GameEngine 门面/IScene 桥接/PublicAPI.Shipped.txt 157 类型门禁/cookbook 6 篇/dotnet new 模板/README 定位段/EngineChecks 16 断言）
> 对照基线：engine-language-tradeoff.md §3.4 G1-G8 差距清单 + §C 批A 验收（示例可编译/模板可跑/EngineChecks exit=0/PublicAPI 存在/现有代码零改动）
> 方法：源码逐文件核验（GameEngine.cs/IScene.cs/cookbook A-F/templates 三件/README 1-14 行/EngineToolSdkChecks.cs/PublicAPI.Shipped.txt）+ t81 实现报告比对。

---

## 0. 结论

**通过 ✅（附 4 条非阻塞建议项 R1-R4）**——批A 交付与设计完全对齐，零行为变更成立；可进入 captain 官方构建 + dotnet new 模板实测；**批B 可启动**（命名空间部分按 D4 需用户确认后先行 Assets 管线与 XML doc 存量）。

---

## 1. G1-G8 逐项核验

| # | 差距 | 核验结果 | 证据 | 判定 |
|---|---|---|---|---|
| G1 | API 入口门面 | GameEngine 门面（Title/Width/Height/MaxSeconds/FitMode/TargetFrameMs + **LoadScene/UseAudio 链式 + Run() 块式** + Scene 读属性 + LastFrameMs/LastFps 性能观测）；SceneHost(IEngineAppHost) 桥接——**宿主 EngineApp 零改动**（Run 内 new SceneHost → EngineApp.Run 原样） | GameEngine.cs:11-86；IScene.cs:10-46 | ✅ |
| G2 | Assembly 公开面门禁 | PublicAPI.Shipped.txt=157 公开类型基线（159 行=157 条目+2 注释头）；门禁断言=清单存在+5 关键 API 在列（GameEngine/IScene/SceneBase/IEngineAppHost/IEngineRenderer）；XML doc=SDK 新增面全量（存量批B） | PublicAPI.Shipped.txt 头部+断言 line 73-79 | ✅ |
| G3 | 命名空间 | 仍 ChartPlayer（批B 一次性切换+using 兼容——**本批不动**=符合 D4 暂缓决策） | GameEngine/IScene namespace ChartPlayer | ✅（按 D4） |
| G4 | 示例未分层 | README 定位段已显式（examples 列表完备：EnginePresets/ToolApp/CLI/EnginePlay/PublishPlayer/Milestone.exe 游戏）；物理/注释 [Example] 标注=批B 承接（定位已先行） | README.md:3-5 | ✅（定位先行；物理层=批B） |
| G5 | SDK 文档缺失 | README 定位+快速开始（GameEngine 5 步）；cookbook A-F 六篇（每行 API 域标注）；模板工程三件；文档大纲=批B 补 API 参考页 | README.md:9-14；cookbook/*.cs | ✅ |
| G6 | 渲染后端插件化 | 批C（P2）——本批未动（符合拍板） | — | ✅（批C） |
| G7 | 玩法模板化 | cookbook E（自定义判定档+计分 JudgementProfile/ScoreBoard API）+ A/B/C 域展示（Ruleset/PlayfieldLine/LaneRuleset） | CookbookE_Judgement.cs；B/C | ✅ |
| G8 | Assets 管线缺失 | **遗留**（LoadTexture/LoadAudio 骨架）——SceneBase.Engine 上下文已就绪（Build(GameEngine) 注入=为 G8 铺路） | IScene.cs:38-40 | ⏳ 批B 首项/P2（建议） |
| 零行为变更 | — | 新增面=Sdk/ 目录+csproj templates 排除；引擎核心与 t57-t73 API 未动（t81 报告 §3.1 与源码一致） | 文件位 | ✅ |
| 工程验证 | — | EngineChecks SDK 断言 16 项（门面 7/生命周期 5/PublicAPI 5+文件/模板 3）全绿；cookbook 独立探针编译 0 警告 0 错误（t81 报告） | EngineToolSdkChecks.cs；报告 §1 | ✅ |

---

## 2. 建议项（非阻塞）

| # | 项 | 建议 | 承接 |
|---|---|---|---|
| R1 | IScene.Build(GameEngine engine) 与设计草案 Build(Scene) 不同 | **采纳实现**（更优：engine 上下文注入→Assets 管线/观测面直取）——设计文档 v1.2 记差异 | 无（设计已同步） |
| R2 | 世界 API（Scene/GameObject/Component/Transform）在 cookbook A-F 未显式演示 | 建议 cookbook 增补 CookbookG_World（对象组装+组件/Transform+Tween 示例）——域/UI/渲染已覆盖，世界组合是 SDK 完整面最后一块 | 批B 或独立 P2 |
| R3 | cookbook A 判定=演示简化（un 走 JudgementTracker） | 已由 cookbook E（真实判定档 API）覆盖——在 A 文件头注明「判定演示版，完整判定见 E」即可 | 文档微调（批B 顺手） |
| R4 | 模板外部生成路径（ProjectReference 相对路径） | 模板说明文档批B 补（引擎目录外生成需调整） | 批B |

---

## 3. 批B 启动建议（按 D4 用户确认后）

1. **G8 Assets 管线骨架**（建议批B 首项）：GameEngine.LoadTexture/LoadAudio 统一入口（底层复用现有；SceneBase.Engine 已就绪）——SDK 完整面+示例 H 需要；
2. **命名空间 codemod**（D4 待用户确认后）：脚本+全局 using 兼容+回归闸（EngineChecks 全量+游戏层 0 警告+试玩抽查）；PublicAPI.Shipped.txt 重建基线；
3. **XML doc 存量补全**（公开类型全量）+API 参考页（sites 文档）；
4. **examples 显式化**：csproj 注释块+[Example] 标注 Tool/EnginePresets/PublishPlayer/EnginePlay+README 表（定位段已先行）；
5. **cookbook G_World**（R2）与 A 头注（R3）随批B 或 P2；
6. 批B 验收=全链 EngineChecks+游戏层 0 警告+试玩视看 3 抽查+README 定位段引用。

---

## 4. 评审结论（给 captain）

- t81 批A **通过**（G1-G8 对照：7 ✅ + G8 遗留批B/P2 + 批C 项未动），零行为变更确认；
- 建议：①captain 官方构建+templates dotnet new 实测+cookbook 探针复跑（命令见 t81 报告 §2）②批B 可分两段：**段1=G8 Assets 管线+XML doc 存量+examples 显式化**（不涉命名空间，可立即启动）；**段2=命名空间 codemod**（按 D4 等用户确认）——推荐此拆分，风险与进度兼顾；
- R1-R4 非阻塞登记。

---

*t82 验收评审完成*（未写码；结论=通过+批B 分两段启动建议）
