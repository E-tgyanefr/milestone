# t138 实现报告：M4 游戏层预研-骨架（C# Milestone.Game 项目）

- **任务**：M4 游戏层预研-骨架：C# Milestone.Game 项目（dotnet/Milestone.Game——页面栈+组件签名接入——t132 设计对齐）
- **执行**：coder-vis（t138，attempt_id=4652be1c-4768-4e3c-9ed1-86ded9dfa3ef）
- **日期**：M4 阶段（t134 域库/同阶段）

## 交付物（全部落盘）

**项目：** [引擎源码/rhygemaker/dotnet/Milestone.Game/](引擎源码/rhygemaker/dotnet/Milestone.Game/Milestone.Game.csproj)（net8.0；引用 RhygeMaker.Bind；25 文件/1738 行）

### ① 项目骨架 + 运行循环
- `Milestone.Game.csproj`（net8.0 / Nullable enable / LangVersion latest / ProjectReference RhygeMaker.Bind——与 csdemo/CsTestRunner 同模）
- `Program.cs`：GameEngine.Run 循环接入（OnFrame→MainScene.Update 页面栈 / OnRender→渲染通行证）；`--frames N` 脚本化冒烟；`--selftest` 数据平面自检（无 native 依赖）；原生 dll 缺失时友好报错（exit 2）

### ② MainScene 页面栈状态机（曲库→选歌→游玩→结果）
- `App/PageStack.cs`：IPage + PageStack（Push/Pop/Replace/PopTo + Transition(Fade|Slide) 槽——域 Storyboard 复用）
- `App/MainScene.cs`：状态机接线（Menu→SongSelect→Play→Result；▶/✏/🎲/空库示例谱/重试/返回/ESC 回菜单）+ demo 冒烟驱动
- 页面：MainMenuPage（开始/设置/退出）/ SongSelectPage（卡片+分类 chips 签名）/ PlayPage（M10 动作表：Space/P/R/A/S/Esc/V）/ ResultPage（Score/ACC/Rank）/ SettingsPage（9 分区）/ FolderPage（曲库管理）/ CalibrationPage（P1）

### ③ 游玩场景组件签名（§6.2 组件树——IRenderable 通行证）
- `Play/PlayScene.cs`：Root→MusicDriver/ChartRuntime/PlayfieldRoot(LaneRenderer/NoteSpawner/JudgementZone)/HudLayer；RenderOrder 升序收集（B-10：背景1<音符2<判定线3<HUD9）
- `Play/Components.cs`：**MusicDriver**（AudioPath/OffsetMs；虚拟时钟 Advance/Pause/Seek/Restart——X6 后接 WinMM AudioClock）、**NoteSpawner**（Chart/WindowSec=1.2f；NotesInRange 窗口→池）、**JudgementZone**（HitRadius=32f/HitLineY=0.74f；ReceiveInput）、**LaneRenderer**（ModeId="mania"/Keys=4）、**ChartRuntime**（域宿主：Chart+Tracker+ScoreBoard；事件→HUD）
  - 全部组件 = ComponentBase 派生（**8 回调** Awake/OnEnable/Start/Update/FixedUpdate/LateUpdate/OnDisable/OnDestroy 完整覆写）+ Owner.Transform 可及
- `Play/PlayState.cs`：数据平面（ChartData/JudgementProfile/Tracker/ScoreBoard——tl34 域宿主；自动游玩 acc=1.000 目标；判定档 Multiplier 热换 0.25..4；SkipIntro 3000ms 门限）
- `Play/ChartDriver.cs`：时钟桥（虚拟钟≙音频 SongTimeMs；Pause/Resume/Seek/Rate/scroll 事件）
- `Play/ChartCatalog.cs` + `Charting/ChartLoader.cs`：曲库扫描（9 扩展）+ .mil 解析入口（t134 ms_rhythm_chart_load）+ 内置示例谱（T7 引导语义）
- `Ui/Widgets.cs` + `Ui/Theme.cs`：Label/Button/Card/TabBar/Scroll/TextEdit 签名 + 原版配色令牌（主蓝 #2C6CFF/深底/文字白——§5.5/§5.6）
- `Data/AppConfig.cs`：设置 JSON 持久化（ms_assets_save_text 计划）

### ④ Cookbook（用户向：如何从引擎写游戏）
- `README.md`（项目内 cookbook）：csproj→引擎门面→两种页面模型（页面栈 vs 场景组件化）→组件树装配→渲染通行证→数据/实例平面分离→t134 接线表
- 参考：csdemo/CookbookBF1_Minimal（~10 行 hello）+ CookbookBF2（八回调序断言）

### ⑤ API 缺口清单（交 eng-coder——t134 补齐）
- `docs/API_GAPS.md`：X1 渲染完全委托（ms_engine_render ctx=录制器+双面回放+IRenderer 10 原语+IRenderable 移交 DevLayer B-10）｜X2 ms_rnd_* 10 原语｜X3 ms_text_draw/measure（RuntimeFont）｜X4 Input raw vk（GetKeyDownRaw/UpRaw/Raw）+鼠标 3 键+滚轮｜X5 .mil 资产化（AssetDatabase type="mil"+list）｜X6 音频（WinMM AudioClock ABI 桥）｜B-9 ms_component_spec 增 onRender+尺寸协商；建议顺序 X4→X2/X3→X1→X5→ms_rhythm_*→X6

## 验证
- `dotnet build Milestone.Game.csproj -c Release`：**0 错误 0 警告**（含 RhygeMaker.Bind 引用构建）
- `dotnet run -- --selftest`：**SELFTEST PASS**（18/18 断言）——示例谱构建/自动游玩全 Perfect acc=1.000/手动判定八档（8ms Perfect·30ms Great·60ms Good·500ms Miss）/档位热换×0.5/SkipIntro 门限 1200ms<3000 拒绝与 3200ms≥3000 允许/时钟 Pause 冻结·Seek·Rate×2
- 全链路 demo（--frames N）需原生 rhygemaker.dll（M1 平台产物未在本工作区检出——CMake 构建后置于运行目录/PATH 即可）；无 native 时输出友好错误

## 关键边界（与设计一致）
- **IRenderable 暂在游戏层定义**（设计 B-10 令 DevLayer/Bind 定义——Bind 未发布前游戏层自带，t134 移交）
- **IRenderer 仍为空接口**——所有 OnRender/Widgets.Render 为签名占位（X2 后接）；判定为托管镜像（数据平面），t134 后由域 Tracker/Ruleset 句柄替换（公共签名不变）
- **无输入/无音频/无绘制**：页面按键、滚轮、绘制、真实音频均按 t132 §3.3 M4-X 缺口标注（不调不存在的 P/Invoke）

## 与 t134 的关系
骨架不依赖 t134 即可构建+自检；t134（Rhythm.* 域库）落地后按 README §四接线表填充（ms_rhythm_chart_load / tracker_* / render_field / ms_audio_* / ms_rnd_* / ms_text_*），即成为引擎版 Milestone 重制（M4.1..M4.4）项目底子。

---

## 附：t139? 合流接线补充（captain：「游戏层合流启动——ABI 可用」后更新）

> 起由：captain 通知 ms_rhythm_* 15 函数官方验证（chart_load/load_text/save/release/meta/stats+ruleset_create/release+preset_list/run+tracker_create/release/update/press_lane/result——preset_run acc=1.0 PASS），指令「先完成游玩核心接线（数据平面 PlayState 真接 C ABI）」。

### 已接（P0 游玩核心链——数据平面真接 C ABI）
| 位置 | ABI 接法 | 状态 |
|---|---|---|
| `RhygeMaker.Bind/Rhythm/RhythmNative.cs`（新） | ms_rhythm_* 全 15 函数 P/Invoke（Dll=rhygemaker·Cdecl·UTF-8——与 Interop 同口径） | ✅ |
| `RhygeMaker.Bind/Rhythm/RhythmApi.cs`（新） | 代理门面：RhythmChart（Load/LoadText/Meta/Stats/Save/Dispose）+RhythmRuleset（Create）+RhythmTracker（Create/Update/PressLane/Result）+RhythmPresets（List/Run）——命名空间 `RhygeMaker.Engine.Rhythm`（设计 §9.1 代理层；未改既有 47 函数签名——csdemo 复验 0 错 0 警） | ✅ |
| `Charting/ChartLoader.cs` | Load→`ms_rhythm_chart_load`（域 chartio 权威）→meta/stats 回填 ChartData（Title/Artist/Difficulty/OffsetMs/NoteCount/NativeDurationMs→Native 句柄）→MilMirror 渲染镜像（音符列表；X7 缺口：chart_notes 枚举 ABI 待补后去重）；native 缺失/失败=纯镜像回退（DllNotFound 捕获） | ✅ |
| `Play/PlayState.cs` | `ms_rhythm_tracker_create/update/press_lane/result`——**能力握手**：AttachNativeTracker 探针 Result() 返回码；OK→NativeJudgement=true（域权威判定：tier 0=PERFECT..3=MISS·得分=Profile 常量）；ErrNotSupported(P1 占位)→mirror-fallback（现有托管判定零改动——双计数防护：NativeJudgement=true 时镜像判定短路）；SyncResultFromNative→域 ScoreBoard 覆盖（ApplyNative hit/miss/acc/score/maxCombo） | ✅（P1 落地自动切换） |
| `Play/Components.cs` ChartRuntime | AttachNative：`ms_rhythm_ruleset_create`（modeId/keys→四族）+ tracker_create（PresetIds.For：mode/keys→10 预设 id）；OnDestroy→ReleaseNative（句柄先于引擎销毁）；NativeChain 状态（none/ruleset/tracker/ruleset+tracker） | ✅ |
| `Play/PlayScene.cs` | Build 后 AttachNative(engine.Handle)；Destroy→ReleaseNative；RunPresetSmoke=`ms_rhythm_preset_run` 先例（hit/miss/acc 打印）；ChartCatalog.Presets=`ms_rhythm_preset_list` | ✅ |
| `PlayPage` | 曲终 Finished→`ChartRuntime.SyncNativeResult()`（tracker_result 覆盖 Board——P1 后权威） | ✅ |
| `Program` + `AbiCheck.cs`（新） | `--abi-check [mil] [--preset id]`：preset_list→chart_load(meta/stats)→ruleset_create→tracker_create（握手探针）→chart_save(Assets/__abi_check.mil)→preset_run——官方验证口径 C# 复现；无 native=友好退出 2 | ✅（本工作区无 rhygemaker.dll——eng-coder 构建树内置运行目录后执行） |
| `Charting/MilMirror.cs`（新） | .mil 渲染镜像（System.Text.Json 轻量解析：格式/曲名/作者/模式/键数/BPM/偏移/音符 t·c·e·type）+ ToMilText（ABI 冒烟夹具）——与域 chartio 并行；权威=域 | ✅ |

### 验证（本工作区——无 native rhygemaker.dll）
- `dotnet build Milestone.Game.csproj -c Release`：**0 错误 0 警告**（含 RhygeMaker.Bind 新 Rhythm 代理编译；csdemo 复验 0 错 0 警=既有 API 零回归）
- `--selftest`：**SELFTEST PASS 21/21**（新增 mil-mirror 3 断言：title/3 notes/hold kind+duration 500ms、e>t 300ms）——数据平面含镜像解析全绿
- `--abi-check` / demo：无 native 时优雅退出（DllNotFound 友好信息——捕获路径验证）；**native 侧冒烟待 eng-coder 构建树提供 rhygemaker.dll 后执行**（captain 官方验证口径已具：bind 用例 hit=3 miss=0 acc=1.0 PASS）

### 与 t134 的 P1 待接（已列入 docs/API_GAPS.md §5）
- tracker_update/press_lane/result 判定语义（当前=占位：update OK 无操作、press_lane tier=0、result=NOT_SUPPORTED）——C# 侧握手已就位
- ms_rhythm_tracker_press_field（Line/Ring 族场几何）；tracker_release=hold 尾判
- tier→档位名/得分映射契约（当前 C# 常量 0=PERFECT..3=MISS——P1 后以域 Profile.Tiers 为准）
- **X7 新缺口**：ms_rhythm_chart_notes(e,c,outJson,cap) 音符枚举（C# 渲染镜像去重——MilMirror 临时并行）

---

## 附：域库官方状态记录（t134 completed——P1 批就绪）

- **t134（eng-coder-vis）completed**：judgement（16 工厂/四档/Tracker/ScoreBoard）+ temporal（BpmTimeline）+ ruleset 四族（Lane/Line/Ring/Path RunHeadless + 10 预设 acc=1.000）+ field/render（render.hpp DrawField）+ ms_rhythm_* ABI（15 函数+render_field）+ CLI（--preset-list/--preset/--preview/--mil-check）+ cookbook A/B/C exe + mil_asset 登记 + 全键 chartio + M4-a util 零 Core 引用（R-9）。
- **验证**：全链 92 用例绿 + ctest 6/6 + 0 警告 + 黄金帧不变（4634E387E024BE90/2FAF72A928888E25）+ 双源 MD5=0；判定结案 acc100=18/18；cookbook A 20/20·B 18/18·C 19/19。
- **P1 批（eng-coder 随批推进——游戏层按表接）**：tracker 真判定语义（update/press_lane/result 由占位→真判定，域 JudgementTracker 语义已就位）/ tier 契约（Tiers 升序索引 0=PERFECT…、MISS=-1、result tier 细分）/ press_field（Line/Ring 场几何）/ hold 尾判 / X7 chart_notes 枚举 / render_field 完整（依赖 X1 后 C# LaneRenderer 直调）。
- **游戏层准备度（本文件已具备——P1 落地零改动自动切换）**：PlayState 能力握手（Result()=OK→NativeJudgement 域权威；NOT_SUPPORTED→镜像回退；双计数防护）；ChartRuntime.AttachNative（ruleset_create+tracker_create+PresetIds 10 预设）；ChartLoader（chart_load+meta/stats 回填+MilMirror 镜像回退）；AbiCheck（--abi-check 官方验证口径）；SyncResultFromNative（tracker_result→ApplyNative 覆盖）。P1 派单时接线点=仅按 API_GAPS §5 表核对（无需结构性改动）。

---

## 附：20 函数 ABI 对齐记录（P1 批落地——eng-coder ①②-⑨ 分片完成）

- **ABI 升级**：ms_rhythm_* 15→20 函数——chart_notes（X7 音符枚举 [{t,e,l,x,y,type,s}]）/ tracker_update=AutoMiss 过键扫描 / tracker_press_lane=PressLaneAt（图绑定，tier 0=PERFECT..3=BAD、-1=未中）/ tracker_press_field（X/Y+hitRadius——Line/Ring 族）/ tracker_result=board 真值（Hits/Misses/Accuracy/Score/MaxCombo）/ story_load/frame/release（R-7 Fade/Slide/Wipe）。
- **C# 侧对齐（零结构改动——能力握手自动切）**：
  - RhythmNative：20 函数 P/Invoke 全量；RhythmApi：Chart.NotesJson(X7)+Tracker.PressField+RhythmStory 代理；
  - PlayState：域权威判定拉直——tier 契约（TierNames 映射表 0=PERFECT..3=BAD/MISS；KindForTier；BAD=命中断连低分）；press_lane 真值直接入板；SyncResultFromNative=域真值覆盖；
  - HudLayer：域判定 UI=tier 名映射表（PERFECT/GREAT/GOOD/BAD/MISS——off 携带 tier → TierName）；
  - ChartLoader：X7 chart_notes 优先（渲染镜像去重）→失败回退 MilMirror 文本解析；
  - AbiCheck：扩至 20 函数验证（press_lane 真值 0/-1 断言、press_field、update 过键、story_frame——native 在位机执行）。
- **验证**：build 0 错 0 警；--selftest **PASS 25/25**（新增 tier 契约 2 + X7 notes-json 2）。native 冒烟=20 函数全链（AbiCheck 已覆盖断言）待 eng-coder 构建树 dll 在位机执行。
- **tier 名映射表（UI 展示口径）**：PERFECT/GREAT/GOOD/BAD/MISS——域四档族（PERFECT/GREAT/GOOD/BAD）+未中 MISS；BAD=命中但断连（域 BreaksCombo）——ScoreBoard.BadCount 独立计数。

---

## 附：23 函数 ABI 对齐记录（eng-coder P1 ①-⑤ 全落地——⑥ render_field=X1 依赖本批不动）

- **ABI 23 函数**（20 契约 + story_release + release_lane + tiers）：新增 `ms_rhythm_tracker_release_lane(e,t,lane,nowMs,outTier)`（hold 尾判——EndMs 位窗/头判 held 态/-1=无 held 在窗）+ `ms_rhythm_tracker_tiers(e,t,outPerfect,outGreat,outGood,outBad)`（档位细分——P1 建议的 result 扩展）。
- **C# 侧对齐（eng-coder 注：①-⑤ 无需改动——能力握手 Result()=OK 已满足；本侧补接 ④⑤ 两函数）**：
  - RhythmNative：23 函数 P/Invoke（+release_lane +tiers）；RhythmApi：RhythmTracker.ReleaseLane（hold 尾判）+ Tiers（档位细分）；
  - PlayState：域权威双段判定——LanePress.Down=true=头判（press_lane——tap/hold 头）/ Down=false=尾判（release_lane——hold 尾）；SyncResultFromNative→Tiers 细分→ApplyNative（tierP>=0=精确细分计数；-1=合成兜底）；
  - AbiCheck：+release_lane（无 held=-1 断言）+tiers（rc/counts sane 断言）；
  - Beat：tier 按名映射契约沿用（0=PERFECT..3=BAD 按名——eng-coder ② 已按名映射；-1=未中+不消费=本侧 KindForTier(-1)=Miss 一致）。
- **验证**：build 0 错 0 警（csdemo 零回归）；--selftest **PASS 25/25**（托管侧不变——native 断言在 AbiCheck：engine 侧 rhythm 17/17+bind 10/10+全矩阵 96/96+0 警告+双树同步）。
- **待接（注册在 eng-coder 批）**：⑥ render_field 完整=X1 依赖（C# LaneRenderer.OnRender 直调——X1 OnRender 完全委托后按表接）。

---

## 附：Hold 双段接线记录（captain：「hold 尾判 ABI 已出——Hold 池可真接」）

- **NoteSpawner Head/Tail 双段**：`NoteSegment`（NoteIndex/Lane/TimeMs/Kind(Head|Tail)/Source）+ `Segments` 列表——RefreshWindow(nowMs)=NotesInRange（±WindowSec）→ Head 段；hold 源且 EndMs 在窗 → Tail 段（镜 release_lane 尾判窗视觉/驱动）。渲染（X2 后）与输入驱动共用 `Pool`（源音符）+ `Segments`（双段事件）。
- **PlayState release_lane 接线**（此前已对齐——本批复核）：LanePress.Down=true=头判 press_lane（tap/hold 头）/ Down=false=尾判 release_lane（ErrorMs 位窗——-1=无 held 在窗）；域权威双段+tier 细分（Tiers→ApplyNative）保持。
- **验证**：build 0 错 0 警；--selftest **PASS 28/28**（新增 spawner dual-segment 3 断言：window@2400 → 1 pool+2 segments / head(2000)+tail(2800) 双段命中 hold / window@3000 → head+tail 保序）。engine 侧 rhythm 17/17（eng-coder 证）。
- **6 render_field**（X1 依赖）=仍待接（本批不动）。
