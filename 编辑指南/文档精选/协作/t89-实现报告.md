# t89 实现报告：引擎 v2 阶段A+B（独立项目化 + Unity 八回调生命周期）

> 编写：eng-coder-vis（引擎编写）· 任务 t89（dep t88）· 设计：engine-v2-unity-design.md 阶段A+B（captain 拍板 V1/V2/V5/V6/V7）
> 状态：**代码+验证完成**（EngineChecks 0 警告 0 错误 exit=0 含 v2 断言组；v2 包构建+还原探针 0 警告 0 错误；v1 零回归全绿；官方 bin 构建归 captain）

---

## A. 独立项目化

| # | 项 | 实现 | 验证 |
|---|---|---|---|
| A1 | 独立 sln | MilestoneEngineV2.sln（引擎源码/engine/：MilestoneEngine.csproj + Tests/EngineChecks.csproj） | dotnet new sln + sln add 成功 |
| A2 | 版本 2.0.0 | csproj：Version/AssemblyVersion/FileVersion/InformationalVersion=2.0.0；PackageId=MilestoneEngine；PackageVersion=2.0.0 | EngineChecks ③断言 AssemblyVersion≥2.0.0（实测 2.0.0.0）✓ |
| A3 | nuget pack（零依赖） | dotnet pack → 输出产物/engine-v2/packages/MilestoneEngine.2.0.0.nupkg（117,787B）——零 NuGet 依赖红线=包零 dependency | 包创建成功 |
| A4 | 发布流 | 输出产物/engine-v2/：packages/（nupkg）+ docs/（README/PublicAPI.Shipped.txt/MilestoneEngine.xml——XML doc 随包：csproj 加 GenerateDocumentationFile+NoWarn CS1591） | 目录填充成功 |
| A5 | 本地 folder 源消费 | 探针工程 NuGet.config（local-engine→输出产物/engine-v2/packages）+ PackageReference MilestoneEngine 2.0.0 → dotnet build 0 警告 0 错误 | 包可还原构建 ✓ |
| A6 | 游戏程序=examples 单向引用 | 设计口径（v2 包引用；V5 解耦）——代码层=包发布就绪；游戏层引用切换归阶段后续/游戏侧 | 设计标注 |

## B. Unity 八回调生命周期

| # | 项 | 实现 | 验证 |
|---|---|---|---|
| B1 | Component 八回调 | Component.cs 已有 Awake/OnEnable/Start/Update(double)/LateUpdate/OnDisable/OnDestroy + **新增 FixedUpdate(double)** = 八回调齐 | EngineChecks ①顺序 8/8 ✓ |
| B2 | LifecycleDriver | Sdk/LifecycleDriver.cs：组件注册表+Begin（Awake→OnEnable）+Tick（Start→Update→FixedUpdate 固定步长→LateUpdate）+DisableAll（OnDisable）+DestroyAll（OnDestroy）；FixedStep=1/60 可调 | 顺序断言全绿 |
| B3 | 顺序断言 | EngineToolV2Checks：①八回调顺序（LifeProbe log 逐项：Awake/OnEnable/Start/Update/FixedUpdate/LateUpdate/OnDisable/OnDestroy）②兼容层（v1 Scene.Update→Component.Update(double) 仍工作）③版本 2.0.0 | **全部通过**（8/8+兼容+版本） |
| B4 | 兼容层 | Component.Update(double) 保留（driver 视为 OnEnable 后常规 Update 别名）；GameObject/Scene 组合零破坏；v1 SDK GameEngine/IScene 原样 | ②断言+v1 全量组（批1-5+SDK）exit=0 |

## 验证命令（captain 官方构建后）

```powershell
dotnet build 引擎源码\engine\Tests\EngineChecks\EngineChecks.csproj -c Release -o 构建产物\obj\t89-check\bin
构建产物\obj\t89-check\bin\EngineChecks.exe    # exit=0；[引擎v2] 生命周期八回调/兼容层/版本 全部通过
dotnet build 引擎源码\engine\MilestoneEngineV2.sln -c Release    # v2 sln 0 警告 0 错误
dotnet pack 引擎源码\engine\MilestoneEngine.csproj -c Release -o 输出产物\engine-v2\packages   # MilestoneEngine.2.0.0.nupkg
# 本地 folder 消费：NuGet.config 指向 输出产物/engine-v2/packages → PackageReference MilestoneEngine 2.0.0 → build 0/0
```

## 边界/说明

1. **命名空间**：v2 仍 ChartPlayer（V7 解耦 v1 的 MilestoneEngine.* 命名空间切换=阶段设计——本批未动（批B 承接）；LifecycleDriver/Component 新增在 ChartPlayer 下（与 v1 共存=零破坏优先）。
2. **v1 SDK**：GameEngine/IScene（t81）原样——v2 生命周期为叠加驱动（SceneHost 可后续接 LifecycleDriver 可选——阶段 B 后续）。
3. **FixedStep**：LifecycleDriver.FixedStep=1/60 秒；FixedUpdate 独立步长（0=同帧转发——兼容旧 Runtime 组合）。
4. **包零依赖**：nupkg 无 dependency 条目（零 NuGet 红线）；Game程序 v2 引用=包路径（folder 源）——设计 V5/V6 口径。

## 遗留（阶段后续）

- 游戏层引用 v2 包（examples 单向）切换；
- SceneHost/GameEngine 接 LifecycleDriver（可选叠加）；
- SceneAsset/Prefab/AssetManager（阶段 C/D）；Editor-Runtime 双态（阶段 E）。