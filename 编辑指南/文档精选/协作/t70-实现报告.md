# t70 实现报告：EngineApp 输入接线修复（B2 · 引擎窗可交互 P0）

> 编写：eng-coder-vis（引擎编写）· 任务 t70（dep t69）· 根因：eng-design-vis 定位（EngineApp.Run 零订阅 window.Key/Mouse）+ 试玩 t68-07/08 证据
> 状态：**修复已在当前源码（t69 P2 补修落盘），本任务完成源码核验+连带回归验证**（EngineChecks 0 警告 0 错误 exit=0 全部通过含导航断言；EnginePlay 0 警告 0 错误；demo/preview 窗口冒烟自动关闭正常；官方 bin 构建归 captain）

---

## 1. 修复内容（当前源码位置）

| 文件 | 行 | 修复 |
|---|---|---|
| 引擎/engine/App/EngineApp.cs | 91-92 | `window.Key += host.OnKey;` `window.Mouse += host.OnMouse;`（引擎输入桥：IEngineAppHost.OnKey/OnMouse 从死路恢复） |
| 引擎/engine/App/EngineWindow.cs | 243-247 | Show() 补 `SetForegroundWindow`+`SetFocus`（合成输入/真实点击投递前台窗口；此前仅 SW_SHOW 无激活） |

**连带影响全恢复**（输入桥一站全活）：
- --demo ESC 退出 / F11 全屏（DemoHost.OnKey 死路→活）
- --play pack ESC（PackPlayerHost.OnKey 死路→活）
- --preview/--open ESC、键鼠手动游玩（EnginePreviewHost/PreviewController.OnKey/OnMouse 活）
- --tool 4 页鼠标导航（ToolApp.OnMouse→PointerDown/Up→InvokeClickAt→页面切换）

## 2. 验证

### 2.1 引擎内部路由（EngineChecks 无窗口断言，权威）

EngineToolAppChecks ②组扩展（exit=0，全部通过）：
```
✓ 初始页=home
✓ 点击『模式预设』→ 页面=presets（OnMouse→PointerDown/Up→InvokeClickAt 路由）
✓ ESC → 回主页
```
即 WndProc→host.OnMouse→ToolApp.OnMouse→PointerDown/Up→InvokeClickAt→页面切换全链路正确。

### 2.2 全量回归（EngineChecks exit=0）

引擎工具四组摘要：预设注册表/判定档/全命中闭环/零回归/文本度量/适配器桩渲染/RingField语义 全部通过 · 批2 预览无头/工具窗一帧/四族渲染/预览宿主 全部通过 · 批3 导入9样本/外部预览/PackExport/MilExport往返/音频契约 全部通过 · 批4 节拍音轨规格 全部通过。

### 2.3 窗口冒烟（t70-play/bin）

`--demo --secs 4` 与 `--preview mania4 --secs 4` 均启动+自动关闭 exit=0（含输入桥构建窗口生命周期正常）。

### 2.4 SendMessageW 投递（eng-design-vis 判别法）

本机环境受限（浏览器 z-order 反复抢顶 + PowerShell Add-Type struct marshaling 不稳定：多次 GetWindowRect=0x0/句柄空），**未能获得窗口级确证**。引擎内部路由（2.1 断言）已证明 WndProc→host→UI 链路正确；窗口级复验建议干净桌面执行（试玩真实鼠标/SendMessageW 直投 HWND）。

## 3. 已知边界

1. 官方 bin 01:37:05 构建时刻早于本修复落盘 → **官方 bin 不含输入桥**；captain 重建当前源码后启用。
2. --tool Tab 顺序 + Enter/Space 激活（试玩 P2 建议）= 引擎 UI 焦点管理（UiElement 焦点链），后续迭代（本轮范围=指针/键盘常规输入已活）。
3. 客户区 1896×1021 vs 窗口 1920×1080 = Win32 非客户区边框（CreateWindowExW 尺寸含边框），letterbox/ToVirtual 用 WM_SIZE 客户区自洽，非缺陷。


--------------------------------------------------------------------------------
## 5. 生产层实证（试玩 t70 真窗 a/b 未导航——日志证据追溯）

**疑点**：试玩主窗验收 a) SendMessageW 客户区 (247,307) 投递未导航 b) 真实鼠标 ClientToScreen (408,503) 未导航——与 EngineChecks 无窗口断言(全绿)矛盾。

**日志实证**（临时移除后重建，三处诊断：WndProc WM_LBUTTONDOWN/UP、EngineApp 桥、ToolApp.OnMouse）：
```
[07:29:48.050] bridge.Mouse x=164.0 y=204.0 down=False
[07:29:48.058] ToolApp.OnMouse phys=(164,204) virtual=(145,216) down=False lbScale=0.946
```

**结论**：①生产层全链路正常（bridge.Mouse 收到鼠标事件 → ToolApp.OnMouse 入口收到 → letterbox 逆变换 virtual=(145,216)）——**与无窗口断言一致**；②**“未导航”= 测试投递坐标落错目标**：virtual(145,216) 命中『引擎自检』卡片（X 96..556/Y 200..264，RunAppSelfCheck=空实现→无视觉变化）；『模式预设』卡片虚拟 (96..556, 292..366) 中心 (326,329) → **正确物理坐标 = (335,311)**（根据同帧 scale=0.946/ox=26.8/oy≈-0.3 反算）；③试玩两次投递（a 客户区 247,307 → 虚拟(233,325)? 落空或错卡片；b 屏幕 408,503 → 窗口客户区偏移后落『引擎自检』）均未命中『模式预设』。

**复验索引**（试玩/引擎使用者）：SendMessageW 或真实鼠标投递客户区 **(335,311)** → 预期 presets 页；ESC→home。

**诊断日志**：三处 AppendAllText engine-input.log（WndProc DOWN/UP 坐标 + bridge + ToolApp 物理/虚拟）——保留（轻量）作真窗调试，`remove 引擎目录 engine-input.log` 即关闭；若 captain 认为应移除,可一键注释（批5 评估）。

【附】EnginePresets 无窗口断言+此日志=双层证据：窗口表现层=正常（日志收到），UI 命中层=断言证明，测试层=需正确坐标。

--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
## 7. 窗口层深挖追加（试玩含桥复测仍未导航 + eng-design-vis 三层判定框架）

【时间线定案（完整 dll 地图）】含桥(SFW=True)起点=07:10:44（输入桥落盘 EngineApp.cs 07:10:36 后首次编译）；试玩测 07:20:05 bin 引用 dll 07:10:44=含桥；缺桥=bin\Release\win-x64(01:17:59)/t65-rv2-rv3/t67/t68(01:19-01:25)=陈旧产物。

【绝对路径日志实证（t70-play/bin/engine-input.log）】
```
[07:37:33.708] bridge.Mouse x=223.0 y=207.0 down=False     ← 真实光标 MOUSEMOVE
[07:37:33.715] ToolApp.OnMouse phys=(223,207) frame=1264x681 virtual=(208,219) down=False lbScale=0.946
```
frame=1264×681（客户区实际，非 1280×720）；lbScale=0.946（Letterbox 换算正确）。

【关键事实】①SendMessageW(326,329)/(335,311) 的 WM_LBUTTONDOWN 从未出现在 WndProc 日志 → MainWindowHandle 非引擎窗（stub/宿主，标题空）→ SendMessage 投错窗；②真实鼠标点击（SetForegroundWindow+SetCursorPos(340,410)+mouse_event）→ 日志仍无新增 DOWN 行 → 真实点击也未达引擎窗。③同窗 demo ESC 有效（Key 通）；Mouse 从未触发 → 窗口接收层疑点（前台/焦点/遮挡/沙箱 interop 不稳），非引擎逻辑。

【已落地防御/诊断】①ToolApp.OnMouse try/catch 包裹（异常写日志不致死 WndProc——按 eng-design-vis ③）②三层日志全绝对路径（AppContext.BaseDirectory）：WndProc WM_LBUTTONDOWN/UP（含 hwnd）、EngineApp bridge、ToolApp（phys/frame/virtual/scale/down）③frame 尺寸入日志。

【判定法（移交试玩干净桌面）】--tool → 真实鼠标点『模式预设』(客户区≈(331,311))→ 读 engine-input.log：有 WM_LBUTTONDOWN+ToolApp+virtual 命中 y 292..366=B2 全链活；仅有 DOWN 行无 ToolApp=订阅断（引擎桥）我再查；无 DOWN 行=点击未达窗（系统层）。

【边界】本沙箱（浏览器遮挡/焦点管理/部分 Win32 interop marshaling 不稳定：GetWindowRect/EnumWindows 间歇 0/空）无法完成窗口级确定判定——最终以干净桌面复测为准（评估：引擎代码层三层证据已全绿——无窗口断言+日志链路+防御，剩余疑点高概率=环境而非代码）。

【决定性定案（2026-08-28）】合成鼠标限制——非引擎 bug。
**日志铁证**（正确句柄 MainWindowHandle 标题=[Milestone Engine 工具] 验证 + SendMessageW 0x0201/0x0202 客户区 247,307）：
```
[07:40:51.551] bridge.Mouse x=231.0 y=216.0 down=False      ← MOUSEMOVE(真实光标)到达
[07:40:51.560] ToolApp.OnMouse phys=(231,216) frame=1264x681 virtual=(216,228) down=False
(无任何 WM_LBUTTONDOWN 行=SendMessage DOWN 从未达 WndProc)
```
**三层证据全绿**：①MOUSEMOVE 到达（WndProc→bridge→ToolApp→Letterbox 换算正确）②Key 到达（demo ESC 有效）③无窗口导航断言绿（UI 层 ToolApp 点击→presets/ESC→home）。**唯一不达=WM_LBUTTONDOWN 合成投递**——试玩环境+我环境同结果（键盘可达/鼠标合成不可达）=**合成鼠标输入（SendMessage/mouse_event）的系统级限制**（跨进程 SendMessage 需前台+光标窗口；与 t49 定案一致），**非引擎 bug**。

**B2 最终验收标准**：真实物理鼠标点击（人类操作）——--tool 点『模式预设』卡片→presets/ESC→home/--demo ESC。真实鼠标=系统投递到光标下窗口，无合成限制。

**边界**：自动化的合成鼠标（mouse_event/keybd VK_LBUTTON/SendMessage WM_LBUTTONDOWN）在自绘 Win32 窗不可达=通用限制（两环境同证）；引擎代码层三层证据全绿（无窗口断言+MOUSEMOVE/Key 到达+WndProc 完整）。

【B2 最终处置（2026-08-28 · 三方一致）】
**判定：引擎路由 OK（三层证据绿）；合成鼠标不可达=环境/输入限制（非引擎 bug）；最终验收=人类物理鼠标点击。**

- 试玩 a/b（b2-05~09：SendMessageW 客户区/ClientToScreen 真实鼠标/(517,429)/keybd VK_LBUTTON 多轮）=全部未达——其环境**会话隔离**（run_code/pwsh 合成输入不可达 MilestoneEngine 工具窗；demo 键盘偶达=焦点投递）。
- 我环境最后确认（07:42:20）：SetForegroundWindow+SetCursorPos(430,420)+mouse_event DOWN/UP → 日志仅 MOUSEMOVE（bridge x=231 y=216）+ToolApp virtual=(216,228)——**无 WM_LBUTTONDOWN 行**=合成 DOWN 未达 WndProc（与试玩同结果两环境同证）。
- **三层证据全绿**：①EngineChecks 无窗口导航断言（OnMouse(326,329)→presets/ESC→home exit=0）②窗口冒烟（--demo/--preview --secs 4 exit=0）③MOUSEMOVE+Key 真实到达（WndProc→bridge→ToolApp→Letterbox 换算正确）——**引擎代码层已证**。
**B2 关闭条件**：人类真实物理鼠标点击『模式预设』（**客户区 (335,311)**=『模式预设』卡正确中心（实测 virtual(326,329)，非 (247,307)——后者落『引擎自检』空卡，已更正）→presets 页 · ESC→home · --demo ESC——三项即关闭。
--------------------------------------------------------------------------------
## 8. B2 终局证据包（eng-design-vis 三层判定框架采信 · 2026-08-28）

【定案：引擎链已证（bridge 生产+映射+断言+Key 通）；剩余唯一变量=OS 层真实人手点击——自动化环境（试玩 run_code/pwsh + 我环境）均无法投递鼠标 DOWN（系统注入层限制，非引擎）。】

| 证据 | 内容 |
|---|---|
| ① 生产层 | bridge.Mouse 收到真实光标 MOUSEMOVE（coord→virtual 换算正确：frame 1264x681/scale 0.946/ox 26.8） |
| ② 路由层 | EngineChecks 无窗口导航断言绿（OnMouse(326,329)→presets/ESC→home exit=0） |
| ③ 键盘对照 | demo ESC（keybd 焦点链）有效=Key 生产通 |
| ④ 隔离取证 | EnumWindows 'MilestoneEngineWnd' count=0（我环境）；试玩同（winsta 隔离） |
| ⑤ 误点根治 | B13=『引擎自检』卡禁用样式+提示（截图 b2-point.png 实证：灰卡+文案『请在外部运行 MilestoneEngine.exe --check』）——B2 误点根源消除 |
| ⑥ 防御 | ToolApp.OnMouse try/catch（异常不致死 WndProc）+ 三层绝对路径诊断日志（WndProc DOWN/UP hwnd/bridge/ToolApp frame-virtual） |

【人类点睛（唯一权威测试，10 秒）】--tool → 人手点『模式预设』卡 → 见 presets = 全链活；ESC→home；--preview --secs 6 ESC；--demo ESC。

【B14（批5 按例）】诊断日志开关化评估（三层 AppendAllText 轻量；移除或开关以批5 定）。
【不再深挖】自动化环境无法测真实点击（双环境同证）；引擎层已全绿。引擎侧无需再改（路由三层绿+防御与诊断已备）。

## 6. 批4 文档闭环项：--help 命令数口径（eng-design-vis 提示）

2026-08 --help 实测 = **13 命令条目 + 5 子选项**（--size/--secs/--headless/--out/--beats）；试玩报 16=把子选项计入。修正：README §命令表标题/内容、t69 报告 ①②⑤§2 §3、ToolApp 帮助页标题（12→13 条）均已改。原则：README 命令表以 --help 实测为准（13 条目口径）；子选项在 --help 内嵌标注。

## 4. 构建后验证命令（captain 官方构建后）

```powershell
dotnet build 引擎\engine\Tests\EngineChecks\EngineChecks.csproj -c Release -o 构建产物\obj\t70-check\bin
构建产物\obj\t70-check\bin\EngineChecks.exe    # exit=0（含点击→presets/ESC→home 断言）
dotnet build 引擎\engine\EnginePlay\EnginePlay.csproj -c Release -o 构建产物\obj\t70-play\bin
构建产物\obj\t70-play\bin\MilestoneEngine.exe --demo   # ESC 退出/F11 全屏
构建产物\obj\t70-play\bin\MilestoneEngine.exe --tool   # 真实鼠标点击『模式预设』→4 页导航
构建产物\obj\t70-play\bin\MilestoneEngine.exe --play <pack>  # ESC 退出
```

--------------------------------------------------------------------------------
## 9. B2 最终根因修正（试玩 DPI 补偿突破 · 2026-08-28——优于前述会话隔离结论）

【决定性突破】试玩 SendMessageW DPI 反向补偿 (503,467) → 三层日志铁证：
```
WM_LBUTTONDOWN x=335 y=312 → bridge.Mouse(335,312) down=True → ToolApp.OnMouse virtual=(326,330) down=True → 模式预设页切换成功(b2-18) + ESC→home(b2-19)
```
**B2 已实际验证关闭——自动化 DPI 补偿后 a/c(点击/ESC)全过**。

【根因精确定位】发送方(SendMessage/mouse_event)客户区坐标被 Win32 PerMonitorV2 系统缩放 ×0.667（注入 (335,311)→WndProc 收 (223,208)——落『引擎自检』空卡）→ 需 DPI 反向补偿 ×1.5（(503,467)→系统缩回 (335,311)）。**非『合成不可达/会话隔离』**——前述结论以本修正为准（两者实验投递坐标不同致差异；t49 定案一致：合成可达到自绘窗）。

【入库建议（自动化方）】SendMessage/mouse_event 投递客户区坐标前做 DPI 补偿 ×(96/Dpi)（或 DPI 感知程序内 ClientToScreen）——PerMonitorV2 下系统缩放注入坐标；定位卡片用『虚拟坐标×scale+ox 再×1.5』或已验证 (503,467)。

【资产更新】WndProc WM_LBUTTONDOWN 日志加 dpi 字段+ASCII hint（PerMonitorV2 scale 提示）——未来调试者一眼看出缩放；UP 日志绝对路径。

【引擎侧终态】引擎 100% 无 bug（三层日志+同进程自测+(503,467) 导航成功——三项互证）；B2=已验证关闭；批5 待 captain 派。