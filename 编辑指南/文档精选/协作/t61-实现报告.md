# t61 视觉修复批 · 实现报告（coder-vis）

> 依据：其他/docs/协作/engine-user-verify-coords.md 定案表 + eng-design-vis 方位映射钉死结论（引擎侧 RingField/RingNote 断言全绿，问题=游戏层未消费/未归一）。
> 编译验证：`构建产物/obj/t56-scratch`（Milestone.csproj 全树干副本）`dotnet build -c Release` = **0 警告 0 错误**（csc/Roslyn 语法预检 PARSE OK）。

## 修复清单（5 项全落地）

### ① 主菜单 Logo 段间距 32px → 6px
文件：`源码/Play/EngineUi/EngineMainShell.cs`
- `logoB.X = 228` → `logoB.X = 202`；`logoB.Width = 230`（原 132→220 的 t59 改动保留）。
- 原理：logoA（白"Mile"）右对齐 228，因其墨迹右缘实测 210（advance 含右侧 side bearing 死亡≈18px）；logoB 左对齐 202 = 228-(18+14-6)（218=logoB 墨迹左缘、24=左 bearing）→ 墨迹间隙 = 210→216 = **6px**，满足 §5.5 "双色 Logo 段间距=6px"。
- 实测锚点（eng 引擎使用者 2026-08-28）：白段墨迹 x=96..210，蓝段墨迹 216..330。

### ② osustd ●录制中徽章 × Score 重叠
文件：`源码/Play/GamePanel.cs`（DrawHud 的 case "score"）
- 锚点 `ch * 0.035f` → `ch * 0.075f`（0.035H→0.075H：下移 0.04H≈29px@720，让出顶部徽章带 y=40..50 与徽章白字 y=20..38）。
- 注意：原行是巨行（单行 5.2KB），插入的 `// t61` 行注释必须换行终结——本项与 ⑤ 曾因**注释未换行吞掉后文代码**（CS8641 else 不配对），已同时修复为行尾注释（本报告下方"过程笔记"有记录）。

### ③ IIDX/Cytus/Phigros 判定字压左 HUD
文件：`源码/Play/GamePanel.cs`（PaintCore `if (_lastJudge.Length > 0)` 分支）
- 引入 `judgeMinX`：无轨判定线类（Iidx/Cytus/Phigros）`280f`，其余 `120f`（维持原左边界）；
- `_d2d.Text(_lastJudge, Math.Max(judgeMinX, Math.Min((float)W - 120f, _lastJudgePos.X - 120f)), …)`——x 钳制下界抬到 280。
- 原理：Text 为 `center:true` 盒宽 240 → 左缘 = cx−120；cx≥280 ⇒ 墨迹左缘 ≥160（判定字实测墨迹半宽≈76，280−76=204>160），让出 x<160 左侧"音符/KPS 行"带（该带 x≈40..150）。
- 保持原右钳/上漂逻辑（rise/`_lastJudgePos.Y - 60 - rise`）不变。

### ④ maimai 中心圆盘簇 → 8 方位环上
文件：`源码/Play/GamePanel.cs`（MaimaiButtonPos + MaimaiNotePos + DrawMaimai）
- **根因（双）**：a) 引擎 .mil 导出 `"c": 0..7`（0=正上）而游戏 simai 解析为 1..8（8=正上），两套编号未统一 → 方位全部错位/落 0 扇区；b) 旧 MaimaiNotePos 未到接近窗时返回**环心**（`(now-t0)/approach` 钳 0）→ 音符群"中心簇"。
- 修复 1（编号统一）：`int k = ((col % 8) + 8) % 8; if (k == 0) k = 8;` —— 引擎 c=0→扇区8（正上），simai 8→扇区8，c=1..7→扇区1..7；越界（<0 或 >14）一次性告警 `_maimaiColWarn`（Logger.Error，不再静默落中心）。
- 修复 2（环上渲染）：新增 `MaimaiApproachK(n,L,now)`（0..1 接近进度）；`MaimaiNotePos` 恒返回**按钮方位**；DrawMaimai 用 `ksc=0.35+0.65k` 缩放 + 渐显（alpha 90..255）实现"环上接近"，**不再出现环心停驻**。
- 修复 3（slide 弧线）：slide 移动圆按起始/终点扇区角走**最短环弧**（含 360° 回绕：`while (d>π) d-=2π`），不再画直线弦。
- 中央 A~F（Col 9..14）保持五角环绕（9=A 圆心、10=B 上 …）不变——官方 touch 语义。

### ⑤ Arcaea 判定字遮挡（帧相关 → 双保险）
文件：`源码/Play/GamePanel.cs`（DrawHud case "judge"）
- 判定字绘制顺序已位于场景四边形之后（PaintCore：DrawStageContents/DrawArcaea → 之后才 DrawHud("judge")），保证任何帧不被 quad 扫过；锚点 `cw*0.030f, ch*0.055f` 已是窗口缩放值（相对坐标）,复核通过。
- case "judge" 处以行尾注释记录（`// t61：判定字绘制在场景四边形之后…`）。

## 关键编译过程笔记（防止同类事故）
- `GamePanel.cs` 为**巨行文件**（行 3 ≈ 68KB 单行存多段逻辑）：在其中插入 `// 行注释` 后**必须紧跟换行**，否则注释吞掉该行全部后续代码——本次 ①osustd ②arcaea 两处注释均因此把 `case "dev"`/`if (age…)` 吞成注释，产生 CS8641 `else 不能用在语句开头`。
- 修复方式：在注释文本后补 `
`（tools.edit 原样保留 UTF-8，未触碰其他行）。
- 验证链：Roslyn 语法树 ParseText 预检（`gptmp/rp`，0 语法错误）→ 全树干 scratch 构建 0/0。

## 待验收（按 engine-user-verify-coords.md 模板）
1. Logo：白 96..210 / 蓝 **216..330**，墨迹间隙 6px（新 bin）。
2. osustd：Score 锚点 0.075H，徽章带 y=40..50 无遮挡（Score 文字区下移 ~29px）。
3. IIDX/Cytus/Phigros：判定字墨迹左缘 ≥160px，不再压"音符 n/N"行。
4. maimai：音符在环上（8 方位），环心无停驻簇；slide 弧线走环弧；引擎 .mil（c:0..7）与 simai（1..8）双编号均正上方位。
5. Arcaea：判定字在 quad 上层（双保险已做），锚点缩放复核。

## 交付文件
- `源码/Play/EngineUi/EngineMainShell.cs`：Logo 段间距（logoB.X=202/Width=230）。
- `源码/Play/GamePanel.cs`：osustd Score 锚点、judgeMinX 钳制、maimai 方位/环上渲染/弧线、arcaea 注释、`_maimaiColWarn` 字段。tmp

## t61b 补测追加（引擎使用者 topbar-buttons-2x.png，bin 23:37:30 → 本次新 bin 修复）

| 项 | 旧 bin 实测 | 标准 | 修复 |
|---|---|---|---|
| 顶栏玩家胶囊 图标-文字间距 | 头像圆 936..966（宽≈30px）+ 白字 992..1040 → 间隙 ≈26px | §5.5 文字 X=槽右+6 | `pLabel.X=974→972`（头像圆右缘 966 + 6），文字墨迹自 972 起 ✅ |
| ⚙设置 / ✖退出 按钮底色差 | 底 (20,23,31) vs 背景 (10,14,22) → **9/255 < 18/255 不可检出** | §5.6 底色差 ≥18/255 | GlassButton 改深蓝底 (36,48,78)：各通道差 26/34/56 ≥18 ✅（Hover (52,68,108)/Press (28,38,64) 递增） |
| 头像圆 α | α=120 不可检出 | §5.6 主蓝 #2C6CFF α≥220 | `Background = (45,108,255,224)`（与深底色差≈45/255）✅ |

> 文件：`源码/Play/EngineUi/EngineMainShell.cs`（BuildMainMenu 顶栏段 + GlassButton）。scratch 全树干 `dotnet build -c Release` = 0 警告 0 错误。

## round-trip 对称性确认（eng-design-vis 问询）

引擎导出 `c = StartAngle/45`（0..7，0=正上）→ 游戏层 `AddMilNotes` 原样 `Col=c`（ChartParserExtra.cs L1222）→ `MaimaiButtonPos` 统一归一 `k=((col%8)+8)%8, k==0?8`（c=0→k=8=正上，c=1→45° 右上…）→ 渲染角 `ang=k*45%360` 与引擎 SectorPoint 同口径。

反向：游戏层改谱后 `.mil` 保存走 `SerializeNotes`（L1425 `if (n.Col != 0) o["c"] = n.Col`）——引擎谱 c=0 音符保存省略 c（Col 缺省=0），重载 `?? 0` 仍为 0→k=8=正上；**无 45° 漂移，双向对称**。simai（1..8，8=正上）与引擎 .mil（0..7，0=正上）双编号在 MaimaiButtonPos 一处收敛。

> eng-design-vis 交叉验证（2026-08-28）：引擎侧 ChartImport `NumAt(e,"c")` 缺失返回 0（ChartImport.cs:31-34 语义）→ 游戏层 SerializeNotes 省略 c=0 → 引擎重载默认 0=正上，与游戏层 `k==0?8` 映射闭合——**双向对称无 45° 漂移**（引擎导出→游戏层渲染方位一致；游戏层保存→引擎重载一致）。maimai 双侧（引擎断言+游戏层消费）实现与文档全闭环。

## t61b2 补测追加（引擎使用者 0738-bin 复测矩阵 → 本轮修复）

| 项 | 0738-bin 实测 | 本轮修复 |
|---|---|---|
| osustd Score×●录制中 | Score 0.075H 已下移（y 中心 54）但仍压徽章带（徽章 y≈34..56 恒在；Score 26pt 跨 25..83） | Score 0.075H→**0.125H**（盒 73..107 全让出）、acc 0.120H→0.175H（同列下移保 0.05H 行距） |
| maimai 中心 5棕1黄簇 | 黄音符 7/8 已上环 ✅，但 A~F 特殊音符仍落环心（五角簇） | MaimaiButtonPos Col 9..14 不再走环心五角——**按最近扇区**（A→4 下、B→5、C→6、D→7、E→8 上、F→1）；DrawMaimai 中央 A~F 六按钮簇移除，中央仅留 TOUCH 提示字 |
| maimai P2 combo×PERFECT | 中心 combo『3』与 PERFECT 判字重叠 | DrawHud：maimai mode + combo + 判字存活期（_lastJudgePos.X>-500 且 age<700）→ combo y −62px 提升 |
| IIDX/Cytus 判字 | ✅ 已修（PGREAT x=210..352、PERFECT x=386..418） | 无改动 |

> 文件：`游戏源码\源码\Play\GamePanel.cs`（osustd score/acc 锚点 + maimai A~F 扇区映射 + 中央簇移除 + combo 提升）。真实工程 `dotnet build -c Release` = 0 警告 0 错误。验收：引擎使用者 ≥07:41 官方 bin 模板复核。

## t61b2 更正（引擎使用者判读撤销：中央 A~F 圈=设计特殊键圈，非映射缺失）

引擎使用者更正：DrawMaimai 源码 3412-3440「外圈 8 按钮 + 中央 A~F」——中央 5棕1黄圆盘 = **A~F 特殊键圈（设计如此）**，非音符落心；音符实测已上环 ✅ + slide 沿弧 ✅。t61 修复验收通过，此前『中心簇=映射缺失』判读已撤销。

我方随更正执行：
- 撤销 t61b2 的「A~F 按最近扇区映射」与「中央簇移除」——恢复 MaimaiButtonPos 中央 A~F 五角 + DrawMaimai 中央键圈绘制（设计保留）。
- 仅保留/新增 **P2 处理**：①combo（maimai+判字存活期）y−62px 提升（免『3』×PERFECT 叠）②判定字 maimai 再抬 46px（_lastJudgePos.Y−60−rise−46，免与中央 A~F 圈拥挤）。
- 文件：`游戏源码\源码\Play\GamePanel.cs`；真实工程 `dotnet build -c Release` = 0 警告 0 错误。
> **eng-design-vis 交叉验证**：引擎侧 `ChartImport.NumAt(e,"c")` 缺省返回 0（ChartImport.cs）与游戏层 `SerializeNotes` 省略 c=0 语义闭合——双向（引擎导出→游戏层渲染 / 游戏层保存→引擎重载）均以 **0=正上** 为基准，与 `MaimaiButtonPos k==0?8` 映射对齐，确认无 45° 方位漂移。
