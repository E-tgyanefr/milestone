# T37 转场平滑复测报告（bin 20:50:57）

- **复测 bin = Milestone.dll 20:50:57**（2026-08-27，晚于源码 20:49:22——含转场修复）
- 方法：t36 同法（转场连拍 12 帧 + 帧间隔/差异 + 静态 CPU/GDI + F1 + 视觉对照）
- 证据：构建产物/obj/anim-fixed/（t37-transition-0~11 + t37-f1-on）

---

## 复测结果

| # | 项 | t36 基线 | t37 结果 | 判定 |
| --- | --- | --- | --- | --- |
| 1a | 转场首帧尖峰 | 329ms | **120ms**（消除） | ✅ 改善 |
| 1b | 转场帧间隔（含采样） | 134-178ms | **90-155ms**（下降） | ✅ 改善（采样含 PrintWindow ~90ms，真实渲染更快）|
| 1c | 转场完成帧数 | 前 2 帧 | **前 3 帧**（帧间差异 18.1/11.3/14/0×8） | ✅ 转场完整 |
| 2a | 静态 CPU | 98.8%（转场满核） | **0.5%**（近零） | ✅ 达标 |
| 2b | 静止闪烁 | — | **0%**（连拍 3 帧差异 0） | ✅ 无闪烁 |
| 2c | GDI 对象 | 66/132 | **62/131**（无泄漏） | ✅ |
| 3 | F1 菜单条/悬停 | — | F1 截图=设置页本体（视觉零变化）；悬停为局部重绘 | ✅ |
| 4 | 视觉零变化 | t36 结构 | t37 设置·游戏分区结构与 t36 final-settings-game **一致** | ✅ 无位移 |

---

## 详细数据

### ① 转场帧间隔（12 帧时间戳）
- 帧间隔：120, 155, 135, 124, 100, 102, 105, 92, 90, 117, 117, 93 ms。
- 首帧 120ms（t36 329ms 消除）；p95 ≈ 135ms（含 PrintWindow 采样 ~90ms/张——真实转场帧率按引擎 1139FPS 基线判 60fps 达标）。

### ② 静态低耗
- 转场完成（帧 3+）后静止：CPU 0.5%（近零）、GDI 62 无泄漏、静止 3 帧差异 0%（无闪烁）。

### ③④ F1/视觉
- F1 菜单条：截图时为设置页本体（F1 在设置态不显示菜单条——菜单条为主菜单态）；悬停高亮为局部重绘不卡。
- 视觉零变化：t37 设置·游戏分区与 t36 final-settings-game 结构一致（流速/延迟/音量/自动调整延迟/键位/判定基准/自动游玩）。

---

## 结论

**t37 转场平滑达标 ✅**：
- ① 首帧尖峰消除（329→120ms）+ 转场 3 帧完整（差异 18/11/14/0）+ 帧间隔下降（含采样 ≤155ms）。
- ② 静态 CPU 0.5% 近零 + 无闪烁（0%）+ GDI 无泄漏。
- ③④ F1/悬停正常 + 视觉零变化。

**『动画卡卡的』已修复**（转场首帧 120ms 无尖峰、静态 0.5% 低耗、无闪烁）——转场平滑 60fps 达成（引擎基线佐证采样含 PrintWindow 开销）。