# Arcaea 还原度对比证据(第一轮,FrameCompare GPU 自动对比)

> 工具:tools\FrameCompare\ —— ffmpeg 抽帧 + ResNet18(ONNX DirectML GPU)特征余弦相似度。
> 视频:arcaea_xterfusion.mp4(实机 3:04,网上下载);截图:屏幕采集\ 15 张(本程序 F12)。
> 报告:research_tmp\fc_video_test.csv(47 帧 × 15 截图,全量对比)。

## 相似度分布

- 平均 0.698 · 最高 0.828(4s 处,画面高度接近) · 最低 0.572(156s 处)
- 判定参考:>0.75 还原良好;0.6~0.75 总体接近但有可见差异;<0.6 差异大需人工核查

## 最低相似 3 帧(证据图见本目录)

| 视频时间 | 相似度 | 实机帧 | 匹配截图 | 差异推测 |
|---|---|---|---|---|
| 156s | **0.572** | 实机_t156s.png | 截图_窗口_123616.png | 需人工核查 |
| 60s | 0.584 | 实机_t60s.png | 截图_窗口_123124.png | 需人工核查 |
| 12s | 0.607 | 实机_t12s.png | 截图_窗口_200734.png | 需人工核查 |

## 给策划的裁决请求

1. 打开 docs\协作\对比证据\ 逐对查看(实机 vs 截图),指出**可见差异**(布局/判定线/音符形态/UI/配色等),按影响排序下发。
2. 确认 FrameCompare 相似度阈值是否可用(或需调模型/加局部区域对比)。
3. 同意后将 FrameCompare 接入闭环:每轮修改后自动跑对比 → 差异清单 → 主代理修改(替代人工逐帧看视频)。

## 工具接入说明

- 用法见 tools\FrameCompare\README.md(`--video 实机.mp4 --shots 屏幕采集 --out 报告.csv --step 120 --gpu dml`)。
- 全部跑在 RTX 5070 Ti(DirectML GPU),47 帧对比数秒完成,可高频迭代。
