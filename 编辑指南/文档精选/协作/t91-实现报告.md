# t91 实现报告：EngineEditor E1（壳+场景层级树+Inspector+Selection）

> 编写：eng-coder-vis（引擎编写）· 任务 t91（dep t90）· 设计：engine-editor-design.md E1
> 状态：代码+验证完成（EngineChecks exit=0 含 E1 断言组 21 项全绿；E1 范围=壳+树+Inspector+Selection——SceneView/Console/Asset 浏览器=E2/P1；官方 bin 构建归 captain）

---

## 交付（E1 五项）

| # | 项 | 实现 | 验证 |
|---|---|---|---|
| 1 | EditorApp 壳 | Editor/EditorApp.cs：IEngineAppHost 桥接 EngineApp.Run（引擎窗宿主）；1280x800 逻辑布局（左 300 树/右 300 Inspector）；LoadScene 装载+Selection.Changed联动 Inspector.Refresh；OnMouse 树命中 → Selection.Current | 1 断言 2 项（面板就位/IEngineAppHost） |
| 2 | 场景层级树 | Editor/SceneTreePanel.cs：Scene.Roots 枚举+折叠（Collapsed HashSet）+组件徽记行+Hit 命中（行 22px）；Refresh 行缓存 | 2 断言 3 项（行数/Player+Camera/Hit 选中） |
| 3 | Inspector | Editor/InspectorPanel.cs：组件列出+反射可编辑字段（int/float/double/bool/string/RgbaColor/Vec2/Vec3——IsEditable+ParseValue+SetValue 写回+脏标记） | 3 断言 9 项（字段数/int/float/bool/string 写回+实例验证+脏标记） |
| 4 | Selection 单例 | Editor/Selection.cs：Current（GameObject）/CurrentAsset/Changed 事件/Clear；非幂等 | 4 断言 2 项（事件触发 2 次/null） |
| 5 | EngineChecks 断言 | Samples/EngineEditorE1Checks.cs（1-4 共 21 断言）+DemoRunner 挂 E1 组 | 全部通过（21 项） |

## 验证命令（captain 官方构建后）

dotnet build 引擎源码\engine\Tests\EngineChecks\EngineChecks.csproj -c Release -o 构建产物\obj\t91-check\bin
构建产物\obj\t91-check\bin\EngineChecks.exe    # exit=0；[引擎编辑器E1] 全部通过

## 边界/说明

1. E1 范围：壳/树/Inspector/Selection 核心就位；SceneView/Console/Asset 浏览器/AddComponent·Delete 按钮形态=E2（设计 E2 联动）。
2. 命名空间：MilestoneEngine.Editor.*（V7 v2 新代码）；引用 v1 ChartPlayer（Component/GameObject/Scene/RgbaColor/Vec）——using 兼容（零破坏）。
3. 反射编辑：仅公开实例字段；属性/私有/只读不列（E1 口径）；脏标记在 InspectorPanel。
4. 树层级：E1=Scene.Roots+组件徽记；Transform 层级/拖拽=E2（Transform→GameObject 反查需补 owner——E2）。
5. 存量 XML 警告：t89 GenerateDocumentationFile 引出的存量 XML doc 格式警告（JudgementLog 等 CS1570——CleanBuild 才现）——非 E1 引入；批B XML doc 清理时一并修。

## 遗留（后续派单）

- E2：SceneView 渲染+ConsolePanel+AssetBrowser+AddComponent/Delete 按钮+Transform 层级展开；
- E3：CLI --editor 入口+独立 exe MilestoneEngineEditor.exe；
- 阶段 C：SceneAsset/SceneSerializer/AssetManager（Inspector 保存联动）。