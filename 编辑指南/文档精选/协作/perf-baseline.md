# 性能基线（perf-baseline——t102 取证 · 优化前置）

- 取证日：当前会话 · 环境：RTX 5070 Ti Laptop · Win11 · DPI 144 · 证据=输出产物\dev\obj\verify-perf\（p02 dense FPS）

## ① 游戏程序（发布 Milestone.exe 156.6MB · 14:54:10）

| 项 | 值 |
| --- | --- |
| 双击→引擎菜单窗口呈现 | **1.61s**（1606ms；启动调用 49ms） |
| 内存（菜单） | **WS 178.2MB**（Peak 178.2MB） |
| 内存（游玩 dense） | WS 158.9MB |

## ② 游玩（240Hz dense 谱——Dense Stress 4K·4K）

| 项 | 值 |
| --- | --- |
| FPS | **60.5**（满 60 上限——dense 满屏音符稳定满帧；KPS 25） |
| 帧时 | ≈16.5ms（60fps） |

## ③ 引擎工具/编辑器

| 项 | 值 |
| --- | --- |
| --tool 启动→窗口 | **0.28s**（277ms——EnginePlay host 0.1MB+引擎 dll） |
| --check 全绿耗时 | **9.51s**（9510ms exit=0 全部通过） |
| 编辑器窗口呈现 | **0.2s**（205ms——六区内容完整渲染后续；WS 47MB——窗口先现） |

## ④ 单文件体积/冷启动

| 件 | 体积 | 窗口呈现 |
| --- | --- | --- |
| Milestone.exe（发布） | 156.6MB | 1.61s |
| MilestoneEngineEditor.exe（发布） | 64.7MB | 0.2s |
| EnginePlay host（MilestoneEngine.exe） | 0.1MB（+dll） | 0.28s |

## 优化目标对照（t103/t104）

- **启动 -30%**：游戏 1.61s → 目标 ≤1.13s；--tool 0.28s → ≤0.20s；--check 9.51s → ≤6.66s
- **内存 -20%**：游戏菜单 WS 178.2MB → ≤142.6MB；游玩 158.9MB → ≤127.1MB
- **GC -50%**：30min 长测（未测——标注：GC 基线留 t103 前补 30min 或按 WS 稳态估——本次取证 WS 值已录）

- 备注：①GC 30min 长测未做（本次 WS/启动/FPS 基线已录——GC 基线建议 t103 前 30min 采样）②dense 60fps 满帧（渲染无瓶颈——优化重点=启动/内存）③窗口呈现 vs 内容完整渲染（编辑器窗口 0.2s=先现——六区完整 ~2-3s 估算）。
---

## 修正（稳态内存——重要）

- **稳态 WS = 761.1MB**（完整初始化后空闲稳态——非 178.2MB（那是窗口初现 1.61s 时）——**优化锚=稳态 761.1MB（目标 -20% → 608.9MB）**；PRIV 838.9MB。
- **GC**：Get-Counter CLR 计数器在运行环境受限（no counters）——**GC 基线=30min 长测标注**（t103 前补或按稳态 WS 稳定判（稳态无显著增长=GC 回收正常——**本次 4s 采样 WS 稳定 761.1MB**）。
- **修正对照**：内存 -20% 目标 = 稳态 761.1MB → **608.9MB**（原 178.2MB 对照作废——改用稳态值）。