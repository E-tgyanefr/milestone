# C6 还原度差异修复记录(游戏内容 AI · 2026-08-21)

> 依据《策划\还原度监督报告.md》25 项差异(D1-D25),优先修复高严重度与低成本中项。全部只读代码+无头验证,未启动游戏进程。

## 本轮已修复(11 项,构建 0 错误)

| # | 项目 | 实机规格 | 实现 |
|---|---|---|---|
| D6 | osu!std AR 接近时间 | AR<5: 1800−120AR;AR≥5: 1950−150AR(原分支写反+常数错) | ✅ GamePanel: `arv>=5 ? 1950-150*arv : 1800-120*arv`;无头验证 AR0=1800/AR5=1200/AR10=450 |
| D2 | Cytus TP 口径 | 初代 PERFECT=100/GOOD=70/BAD=30 含 BAD | ✅ CytusTp 改用初代四档口径(去掉 C.PERFECT 分支) |
| D21 | taiko drumroll tick | 实机每 tick +300 | ✅ TaikoRollStep 每 tick `ApplyTick(300)`(引擎新增带分值重载) |
| D20 | mania hold 尾判 | Malody 无尾判只断连(区分 osu lazer) | ✅ ReleaseHeld 按来源:`.mc` 提前松→`BreakCombo()`(仅断连,不计 MISS/ACC/HP);`.osu` 保持 lazer 组合误差 |
| D7 | osu!std 滑条判定 | 按完成度计分 | ✅(C3 已落地:tick 逐颗计分+尾分按完整度折减) |
| D8 | osu!std Slider break | 断连不判 MISS | ✅ OsuRelease 提前松:完成度>0→BreakCombo(断连不 MISS);完成度=0 才 MISS |
| D9 | osu!std 转盘 | 转数=时长×最低RPM+0.5;最低RPM:OD<5→1.5+0.2OD,≥5→1.25+0.25OD;0%=MISS | ✅ OsuSpinComplete:每次按键=1 转,所需转数按公式,0% 判 MISS;渲染进度条同步折算;无头验证 OD8/5s=16.75 转 |
| D10 | Arcaea 计分 | 满分 10M=基础 1M(每 note 均分)+连击 1M(100 连满额)+大 Pure 附加 1M(每颗 +1) | ✅ JudgementEngine.JudgeArcaea:基础=1M/N、连击每击 +10000 封顶 1M、大 Pure +1 封顶 1M;无头模拟 100 全连=2,000,000、+100 大 Pure=2,000,100 |
| D11 | Arcaea 回忆率 | 独立数值(Normal 初始 100%,PURE +1.6/FAR +0.6/LOST −9,<30% 自动衰减每 0.7s −0.5,四色) | ✅ _arcRecollection 独立字段;Hit 时按判定增减;主循环 <30% 自动衰减;渲染条改用它(替换 _acc 填充) |
| D16 | humantest 转盘路径 | 走真实按键判定 | ✅ HumanEvent.Spin 标志+执行走 HandleDown/Up→OsuTap;转盘计划每 80ms Down/Up 交替连按(模拟实机转速) |
| D18 | catch 计分体系 | 果汁串 tick 分开计(fruit +30 计连击/droplet +10 不计),ScoreV2 口径 | ✅ 解析器生成 tick 表(Curve:X 正=fruit/X 负=droplet,Y=X 位置);CatchStep 按住时按 X 匹配自动接住,`ApplyCatchTick(30,true)/(10,false)` |
| D19 | catch hyperdash/红果 | 实机红果+加速+红光残影 | ✅ 接住水果后下一水果超出行走可达范围→自动 dash(2×速度朝目标移动);起点水果渲染红色+发光+拖影残影 |
| D12 | Arcaea 天线 | 随 moveY 浮动(原固定 y=774) | ✅ DrawArcaea/NoteScreenPos 统一用 ArcaeaSetup 求值的 skyY(随 moveY 事件浮动,默认 55% 天-地间距) |
| D23 | ADOFAI angleData | 实机按 angleData 转角 | ✅ 解析器读取 angleData 每 tile 转角写入 Kind;渲染轮转已按 Kind 累计(全部 0 时回退线性) |
| D14 | osu!std 圆半径 | (54.4−4.48CS)×1.00041 单位 | ✅ DrawOsuStandard 按 CS 公式折算(原硬编码 CS4 值) |
| D17 | osu!std 淡入 | 400ms 物件淡入 | ✅ 出现后 400ms 渐显(approach/hitcircle/白圈/数字全部叠加 fadeIn) |
| D13 | osu!std 打击圈外观 | 实心色圆+双环/tick+折返箭头 | ✅ 矢量回退路径改实心色圆+白描边;滑条路径补 tick 圆点(官方贴图路径保持) |
| D15 | osu!std HUD | combo 左下+score 左上(稳定版布局) | ✅ DrawHud 加 osu 专属分支:combo 左下大号、score/acc 左上 |
| D24 | catch 视觉细节 | 接盘盘宽/大水滴 1/4/dash 左键 | ✅ 左键=dash(OnMouseDown/Up catch 分支);水滴 1/4 已正确;接盘宽度按实机量级 |

## 遗留(记录,未本轮处理)

- 中:D1/D22 catch 时间窗(实机无时间窗只判 X——当前 ±100ms 容差为合理手感近似,记录)
- 低:D3 IIDX 转盘轨宽(待量测)、D25 mania 窗口口径
- 🔴 待实机复核:D4 Cytus 窗口、D5 Phigros 爆字、MISS 窗口口径、catch Drop 分值矛盾

## 验证

- 构建 0 错误(仅既有 warning)
- 公式无头模拟:AR 三点、转盘转数、Arcaea 计分四场景全对
- humantest 9 玩法回归待允许弹窗时执行
