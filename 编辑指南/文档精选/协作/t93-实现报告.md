# t93 实现报告：主流对齐补丁批（D1/D11/D3/D4）

> 编写：eng-coder-vis（引擎编写）· 任务 t93（dep t92）· 设计：engine-mainstream-alignment.md 差异 D1/D11/D3/D4
> 状态：代码+验证完成（EngineChecks 0 警告 0 错误 exit=0 含主流对齐断言组 14 项全绿；全部新增别名零破坏=批1-5+SDK+v2+Editor E1 全绿；官方 bin 构建归 captain）

---

## 交付（D1/D11/D3/D4）

| # | 项 | 实现 | 验证 |
|---|---|---|---|
| D1 | GetComponent/GetComponents | GetComponent<T>/GetComponent(Type) 已存在（基类匹配 c is T/type.IsInstanceOfType）；**补充** GetComponents(Type)（Component[] 返回基类匹配）；GetComponents<T>（List<T> 已有）——D1 语义齐 | ①断言 4 项（基类命中/派生/GetComponents 含基类+派生/Type 匹配） |
| D3 | Time 别名 | Sdk/Time.cs（MilestoneEngine 命名空间）：Time.deltaTime/unscaledDeltaTime/timeScale(读写)/ElapsedTime/paused/Advance/Reset——包装共用 EngineTime 不改芯 | ②断言 4 项（timeScale 读写/deltaTime 正值/ElapsedTime/缩放影响） |
| D4 | Transform 短名 | Transform.cs 增 position/localRotation/localScale/parent/children 属性别名+GetParent/GetChildren（Godot 式）——现有长名/SetParent/AddChild 保留 | ③断言 5 项（position 写读/localRotation/localScale/parent 层级/children） |
| D11 | InstanceId | GameObject.InstanceId（静态 Interlocked 单调计数）+Component.InstanceId（Attach 分配）——序列化引用=Path+Id 基础 | ④断言 2 项（GameObject 唯一/Component 唯一稳定） |

## 验证命令（captain 官方构建后）

dotnet build 引擎源码\engine\Tests\EngineChecks\EngineChecks.csproj -c Release -o 构建产物\obj\t93-check\bin
构建产物\obj\t93-check\bin\EngineChecks.exe    # exit=0；[主流对齐D1/D11/D3/D4] 全部通过

## 边界/说明

1. **零破坏**：全部新增（GetComponents(Type) 补/Time.cs 新/Transform 别名/InstanceId 属性）——现有 API 未改签名；v1/游戏层零回归（批1-5+SDK+v2+Editor 全绿）。
2. **命名空间**：Time 别名=MilestoneEngine（v2 新代码 D10 V7）；GetComponent/Transform/InstanceId 在 ChartPlayer（存量类原地补——零破坏优先；批B 段2 命名空间切换时随迁）。
3. **InstanceId 序列化**：Path+Id 引用基础就位（阶段 C SceneSerializer 用）。
4. **D2/D5/D6/D7/D8/D9**：SetActive+Destroy/Input 动作轴/AssetManager/Prefab/Inspector 白名单=后续批（E2/阶段 C/D——本批仅 D1/D11/D3/D4 拍板范围）。

## 改动文件

| 文件 | 改动 |
|---|---|
| 引擎源码/engine/GameObject.cs | GetComponents(Type) 补+InstanceId（Interlocked 计数+ctor+Attach） |
| 引擎源码/engine/Component.cs | InstanceId 属性 |
| 引擎源码/engine/Transform.cs | D4 短名别名（position/localRotation/localScale/parent/children+GetParent/GetChildren） |
| 引擎源码/engine/Sdk/Time.cs | 新——Time 别名单例（MilestoneEngine） |
| 引擎源码/engine/Samples/EngineMainstreamChecks.cs | 新——D1/D3/D4/D11 断言 14 项 |
| 引擎源码/engine/Samples/DemoRunner.cs | 挂主流对齐组 |