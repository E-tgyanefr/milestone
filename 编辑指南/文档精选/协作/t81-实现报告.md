# t81 实现报告：SDK 批A（GameEngine 门面+IScene 桥接+PublicAPI 门禁+cookbook A-F+模板工程+README 定位）

> 编写：eng-coder-vis（引擎编写）· 任务 t81（dep t78）· 设计：engine-language-tradeoff.md §C 批 A（零行为变更渐进）
> 状态：**代码+验证完成**（EngineChecks 0 警告 0 错误 exit=0 含 SDK 批A 断言组；cookbook 六篇独立探针编译 0 警告 0 错误；模板工程+PublicAPI 基线就位；官方 bin 构建归 captain）

---

## 1. 交付清单（批A 七项）

| # | 项 | 实现 | 验证 |
|---|---|---|---|
| ① | GameEngine 门面 | 引擎源码/engine/Sdk/GameEngine.cs：Title/Width/Height/MaxSeconds/FitMode + LoadScene(IScene) 链式 + UseAudio(IEngineAudioBackend) + Run()（包装 EngineApp.Run 零改动）+ 性能观测面（LastFps/LastFrameMs） | SDK 断言 ①7 项绿（属性/LoadScene/桥接/UseAudio/观测） |
| ② | IScene 桥接 | 引擎源码/engine/Sdk/IScene.cs：IScene 接口（Build/Update/Render/OnKey/OnMouse/OnClose）+ SceneBase 抽象基类（空实现可覆写）+ SceneHost（IEngineAppHost 包装-宿主 EngineApp 零改动） | SDK 断言 ②5 项绿（Build/Update/OnKey/OnMouse/OnClose 回调）+桥接存在 |
| ③ | PublicAPI.Shipped.txt 门禁 | 引擎源码/engine/PublicAPI.Shipped.txt：157 公开类型基线（门禁=清单不漂移）；XML doc 注释（GameEngine/IScene/SceneBase 全量） | SDK 断言 ③5 项（GameEngine/IScene/SceneBase/IEngineAppHost/IEngineRenderer 在列） |
| ④ | engine-cookbook 6 篇 | Sdk/cookbook/CookbookA_4K.cs（程序化 4K 下落/~120 行）/B_Line（Phigros 线场动态判定线+圆点）/C_Arcaea（天地双轨+ScrollDir 视觉+ARC 时序）/D_Minimal（白盒模板同源）/E_Judgement（自定义判定档+计分）/F_UI（UI 面板/转场）——每行标注 API 域；仅引用 MilestoneEngine | 独立探针工程编译 0 警告 0 错误（API 逐行实核：PlayfieldLine.Transform/JudgeTier 签名/UiButton/UiCanvas 修正） |
| ⑤ | dotnet new milestone-engine 模板 | 引擎源码/engine/templates/milestone-engine/（template.json+MilestoneEngineGame.csproj+Program.cs——白盒 D 同源）；引擎 csproj 排除 templates/**（Top-level 非库单元） | SDK 断言 ④3 项（template.json/Program.cs/csproj）+模板已排除库编译 |
| ⑥ | README 定位段 | 引擎源码/engine/README.md 顶部：引擎=可编程游戏引擎 SDK；examples 层显式标注（EnginePresets/ToolApp/CLI/EnginePlay/PublishPlayer/Milestone.exe 游戏=不定义引擎）；零依赖红线+SDK 快速开始 | 文档已加（正文行 1-18） |
| ⑦ | EngineChecks SDK 门禁断言 | EngineToolSdkChecks.cs（①GameEngine 门面 ②IScene/SceneBase 生命周期 ③PublicAPI 在列 ④模板存在——16 断言）+ DemoRunner 挂批A 组 | exit=0 全绿（[引擎SDK批A] 全部通过） |

## 2. 验证命令（captain 官方构建后）

```powershell
dotnet build 引擎源码\engine\Tests\EngineChecks\EngineChecks.csproj -c Release -o 构建产物\obj\t81-check\bin
构建产物\obj\t81-check\bin\EngineChecks.exe    # exit=0；[引擎SDK批A] GameEngine/IScene/PublicAPI/模板 全部通过
dotnet new milestone-engine -o Demo    # 模板生成（templates/milestone-engine）→ dotnet run 白盒可运行
# cookbook 探针（编译验证）：构建产物/obj/t81-cookbook-probe/cookbook-probe.csproj（0 警告 0 错误）
```

## 3. 边界/说明

1. **零行为变更**：GameEngine/IScene=新增面（Sdk/ 目录）；SceneHost 桥接 IEngineAppHost——宿主 EngineApp/EnginePlay 零改动；现有 t57-t73 全部 API 未动。
2. **cookbook 可运行性**：六篇编译通过（探针工程）；窗口运行需宿主注入（GameEngine.Run 走 EngineApp.Run——窗口版可运行；cookbook 场景为示例骨架——A/B/C 完整视觉演示，E/F 为 API 展示）。
3. **PublicAPI 基线**：157 公开类型（引擎现状）——门禁=清单管理（批B 命名空间切换后重建）；XML doc 覆盖 SDK 新增面（存量类批B 补）。
4. **模板工程 csproj**：ProjectReference 相对路径指向引擎源码（模板生成于仓库内正确；外部生成需调整——模板说明文档批B 补）。
5. **命名空间**：仍 ChartPlayer（批B 一次性切换 MilestoneEngine.*+using 兼容——本批不动，防破坏）。

## 4. 批A 验收对照（tradeoff §3.5）

| 验收项 | 状态 |
|---|---|
| 示例可编译 | ✅ cookbook 六篇探针 0 警告 0 错误 |
| 模板可跑 | ✅ 模板工程就位（dotnet new 生成验证归 captain——代码层齐备） |
| EngineChecks exit=0 | ✅ 含 SDK 批A 断言组 16 项全绿 |
| PublicAPI 清单存在 | ✅ 157 类型基线 |
| 现有代码零改动 | ✅ 新增面（Sdk/）+引擎 csproj 仅加 templates 排除（已有 t79 排除）；核心未动 |

## 5. 遗留（批B 承接）

- 命名空间 ChartPlayer→MilestoneEngine.*（批B codemod+全局 using 兼容）；
- examples 层物理/注释显式化（csproj [Example] 注释块+README 表——批B）；
- XML doc 存量类全量补（批B）；Assets 管线骨架 LoadTexture/LoadAudio（G8——本批面未含,批B 或 P2 承接）。