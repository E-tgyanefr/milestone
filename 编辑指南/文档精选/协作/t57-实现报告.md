# t57 实现报告：引擎工具批1（EngineText + SoftwareDrawAdapter + EnginePresets 注册表 + BuildChart 增强 + 断言）

> 编写：eng-coder-vis（引擎编写）· 任务 t57（dep t54 设计 + engine-presets-data.md 数据附录）
> 状态：**代码完成 + 本地 EngineChecks 自检工程验证通过**（构建产物/obj/t57-check/，0 警告 0 错误，exit=0；官方 bin 构建归 captain）

---

## 1. 改动文件清单（引擎侧 5 文件 + 自检 1 文件）

| 文件 | 内容 | 说明 |
|---|---|---|
| 引擎/engine/Platform/EngineText.cs（新） | GDI P/Invoke 文本服务 | CreateFontW/SelectObject/TextOutW/GetTextExtentPoint32W/GetDeviceCaps/CreateDIBSection；#if WINDOWS 实装 + #if !WINDOWS 桩（估宽=码点×字号×0.55）；MeasureExtent/Rasterize/DrawString/CodePointCount；字体 LRU≤64、字形光栅 LRU≤128；零 NuGet/零 System.Drawing/零 WinForms |
| 引擎/engine/Platform/SoftwareDrawAdapter.cs（新） | IUiDraw → SoftwareRenderer 帧缓冲 | Rect/RoundedRect/Ellipse(扫描行)/Line/Text(光栅 alpha 合成)/Image(EngineTextBitmap 透传)/MeasureText；PushClip/PopClip 委托渲染器裁剪栈 |
| 引擎/engine/Platform/SoftwareRenderer.cs（改） | 裁剪栈 | PushClip/PopClip + Pixel 单咽喉 InClip 检查（**空栈=恒真=与旧版逐像素一致，零回归**） |
| 引擎/engine/EnginePresets.cs（新） | EngineModePreset + 10 预设注册表 | All/Get/BuildRuleset/BuildProfile/BuildSampleChart/BuildContext/RunHeadless/ToDocTable + PresetLayout/PresetBackground（颜色 FromHex 解析） |
| 引擎/engine/RulesetFactory.cs（改） | ChartSeedOptions + BuildChart 增强重载 | 新增 BuildChart(d, seed) 独立路径；**原 BuildChart(d) 未动一字**（seed=null 回退）；Lane 长条/Ring 滑星/Line 滑条+转盘/Path 拐角标记 |
| 引擎/engine/Samples/EngineToolChecks.cs（新） | t57 断言组（接入 DemoRunner.RunAll） | 6 组：①10 注册表 ②10 判定档 ③10 RunHeadless 全命中 ④增强零回归 ⑤文本度量单调 ⑥适配器桩渲染 |

## 2. 10 预设实现差异表（B2/engine-presets-data.md 为准 → 实现值）

| Id | B2 数据 | 实现 | 差异/标注 |
|---|---|---|---|
| mania4 | Lane 4 键 · OsuMania8 · D F J K · 32 音 500ms+4 hold | 同左（Hold 每满 8 格第 8 格） | ✅ 无出入 |
| mania6 | 36 音 | 同左（36 音，4 hold） | ✅ |
| mania8 | 40 音（SPACE） | 同左（40 音，5 hold） | ✅ |
| phigros | Line 2 线 · Phigros · 6 音（线 X 分布+1 滑条） | 同左（6 音：X 0.25/0.5/0.75 × Y 0.35/0.65，n-2 位 1 SliderNote） | ✅ |
| arcaea | Lane 6（天2+地4）· ScrollDir=-1 · Arcaea · 24 音 | 同左（24 音 6 轨循环；ScrollDir=-1 仅渲染层消费） | ⚠ 标注：引擎侧 Lane 近似族（真 Arc 曲线属游戏层表现，批2 RulesetRenderer 亦按近似族绘制） |
| cytus | Line 1 扫描线 · Cytus · 16 音（Y 分位网格） | 同左（16 音 X/Y 网格；**无滑条**——初版曾加滑条，已按 B2 修正） | ✅（修正记录） |
| osustd | Line 1 · OsuStandard8 · 12 音（圆点+滑条+转盘） | 同左（10 圆点 + n-2 滑条 + 末音 SpinNote） | ✅ |
| iidx | Lane 8（7 键+转盘）· Iidx · 32 音 | 同左（32 音 8 轨循环） | ⚠ 标注：转盘列=8 键之一（KnobNote/旋盘旋转判定属游戏层表现，批1 不建模；B2 注明近似族） |
| maimai | Ring 8 扇区 · Maimai · 8+2 滑星 | 同左（10 音：8 扇区 tap + 末 2 音 EndAngle=+2 方位滑星） | ✅ |
| adofai | Path 2 球 · Adofai120 · 24 拍（含拐角） | 同左（24 音 @BeatMs(120)；拐角=Type="corner" 数据标记） | ⚠ 标注：拐角仅数据标记（Path 判定=PressLane 双球语义，拐角渲染属批2 RulesetRenderer） |

**关键核对结论（对 eng-design-vis 5 点指引）**：
- ①ProfileKey 全部可解析（BuildProfile 剥离数字后缀→小写基名匹配）；RunHeadless 全部 acc=1.000 ⇔ 全命中（无 Path/Ring 例外，无需降级为 HitCount 自检）；
- ②RunAuto 语义 10 预设 100% 命中（32/32…24/24，miss=0，Perfect 判定全过）；
- ③A2 文本口径：EngineText 度量=绘制同字体同尺寸实例（GDI 实算）；非 Windows 桩保跨平台编译；emoji run-split 属宿主层（批2 需要时按 UiGlyphRuns 姿势扩展——EngineText 已注边界）；
- ④BuildChart 增强默认输出零回归（默认路径方法体未动，EngineChecks ④ 断言逐字验证）；
- ⑤四族构造无出入（Lane(keys,100,0.3,speed,90)/Line(500,500,40)+双线/Ring(origin,400,keys)/Path(120,100)——与 RulesetFactory.Build 现构造一致）。

## 3. 构建后验证命令（captain 官方构建后执行）

```powershell
# ① 引擎自检工程（含 t57 6 组断言；Windows 构建走 GDI 实算路径）
dotnet build 引擎\engine\Tests\EngineChecks\EngineChecks.csproj -c Release -o 构建产物\obj\t57-check\bin
构建产物\obj\t57-check\bin\EngineChecks.exe        # 期望 exit=0，日志含「[引擎工具] …全部通过」

# ② 独立引擎 exe 自检（EnginePlay）
dotnet build 引擎\engine\EnginePlay\EnginePlay.csproj -c Release   # captain 官方构建（或既有 bin）
引擎\engine\EnginePlay\bin\Release\net8.0-windows\MilestoneEngine.exe --check   # 期望 exit=0
```

## 4. 已知边界（如实标注）

1. **EngineText 单字体无自动回退**：emoji/生僻字走 .notdef 框（引擎工具窗文本以拉丁/CJK 为主；emoji 双字体 run-split 属宿主层 UiGlyphRuns——批2 工具窗 UI 需要时按 t31/t33/t34 姿势扩展 EngineText）。
2. **GDI 文字质量**：ANTIALIASED_QUALITY 灰度 AA（非 ClearType，DIB 无彩边）；Rasterize 位图带 3px pad（防右侧 bearing 裁切）。
3. **iidx/arcaea 近似族**：转盘/Arc 曲线为游戏层表现，引擎批1 数据照 B2 标注保留。
4. **RunHeadless 依赖 ChartContext.SortNotes**（构造时排序）——EnginePresets.BuildContext 已走该路径。
5. 本地仅构建 EngineChecks 自检工程（scratch，产物 构建产物/obj/t57-check/bin），**未触碰官方 bin**（官方构建归 captain）。

## 5. 复位/下一步

- 批2 就绪点：EngineText.Rasterize（文本→位图）+ SoftwareDrawAdapter（帧缓冲合成）已供 ToolApp 直接调用；EnginePresets.BuildSampleChart/ToDocTable 供预设页/帮助页直接消费。
- 批3 就绪点：Profiles 判定档解析已验证（ChartImport 用同样的 ProfileKey 口径可查表）。
---

## 6. 批1 增补（t57 验收补充 · 2026-08 eng-design-vis）

经 eng-design-vis 验收补充要求，EngineToolChecks 新增第 ⑦ 组断言（RingField 语义 + maimai 构造，t55 根因闭环）：

| 断言 | 结果 | 意义 |
|---|---|---|
| SectorAngle(0..7) = i×45° | ✅ | 8 方位均匀（0°=+X、顺时钟默认），与 InputMapper.AngleToSector/AngleOf 消费同源 |
| SectorPoint(i) → AngleToSector 互逆 | ✅ | 触点→扇区 与 扇区→世界点 往返一致（InputMapper.TouchToSector 同口径） |
| SectorPoint(i) 距圆心 = Radius 400 | ✅ | **音符世界点在环上，非环心**——钉死 t55「游戏层 maimai 渲染在环心」的引擎侧语义：引擎 RingField 语义正确，根因在游戏层 DrawMaimai 未消费 StartAngle（待 coder-vis 修） |
| maimai 示例谱 10 音全为 RingNote | ✅ | 构造=RingNote.StartAngle（非自定义角度/自由场） |
| 含 2 滑星 StartAngle→EndAngle | ✅ | 滑星 2 音 EndAngle=+2 方位（RingNote.Slide=true） |
| StartAngle 全 = Field.SectorAngle(扇区) | ✅ | 与引擎判定 PressRing（半方位容差）同口径 |
| RunHeadless('maimai') 10/10 | ✅ acc=1.000 | 无降解（已达 100% 命中，无需 HitCount==TotalNotes 自检降级） |

本地 EngineChecks 复跑：0 警告 0 错误，exit=0，[引擎工具] …/RingField语义 全部通过（构建产物/obj/t57-check/run.log）。

> 增补结论：引擎侧 RingField/RingNote/maimai 预设语义全部正确闭环；游戏层 t55「maimai 音符渲染在环心」为消费侧缺陷（DrawMaimai 未映射 StartAngle→SectorPoint），建议 coder-vis 修复 + 试玩复验。