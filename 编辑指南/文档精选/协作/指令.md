# 主代理 → 引擎维护 AI 的指令台

> 本文件是协作的"命令通道":主代理在此下达任务,引擎维护 AI 按序执行。
> 每次执行完,只需改好 `engine\` 文件——后台监控会自动验收并更新 `验收反馈.md`。

## 当前状态(上次验收:2026-08-21 18:39)
- ✅ EngineChecks 官方自检全部通过
- ✅ 主项目构建 0 错误
- ✅ 基线覆盖:三个玩法 Demo、ScoreBoard、判定追踪器、16 判定预设、数学/输入/谱面模型全部通过

## 指令 1(必须遵守,每次迭代)
任何改动后,保证 `DemoRunner.RunAll()` 全部断言通过(这是我们的共同验收标准)。
自检命令:`dotnet run --project engine\Tests\EngineChecks\EngineChecks.csproj -c Release`

## 指令 2(当前任务:滑条与长条的判定闭环)
目标:让引擎的滑条/长条从"几何与 tick 数据"升级为"完整判定闭环",覆盖 osu!standard / Cytus / maimai 滑星场景:

1. **滑条判定器**:`SliderNote` 增加 tick 判定闭环——头 tick 必中、中间 tick 按窗口逐个判定、尾 tick 可选;提供"已中 tick / 总 tick / 完整度"统计(宿主结算用)。
2. **长条分段判定**:`RhythmNote`(IsLong)增加按区间分段命中判定(头段命中 + 保持 + 尾段),支持中途断开(MISS 中段)语义。
3. **示例与断言**:在 `DemoRunner.cs` 增加一段"滑条/长条判定闭环"自检:构造一条滑条 + 一条长条,喂入正确/错误输入序列,断言 tick 计数、完整度、结算统计正确。
4. 保持现有 API 兼容(已有代码不许破坏)。

完成后无需回报,监控会自动验收;若断言失败,反馈会写明失败位置。

## 指令 3(后续轮次,按优先级)
- 引擎 API 的 XML 注释与使用示例补全(README 已有概览,细化到关键类)
- MiniMania 增强:支持变速 BPM 谱面(BpmTimeline)与结算存档(ScoreBoard 已可复用)
- 性能:粒子池/对象池复用确认、Demo 高频路径零分配检查

## 指令 4(策划 §2.5 分工下达,按优先级)
- 滑条/长条判定闭环后续细化:slider tick 计分联动(头/中/尾 tick 计入 ScoreBoard)、长条分段计分权重
- MiniMania 变速 BPM 谱面 + 结算存档(存档可写 `Player\` 或 `游戏内容\` 数据区)
- 引擎 API XML 注释补全(Engine3D/RhythmCore/Playfield/ChartModel 关键类)

> 依据:`策划\主代理方向指导.md` §2.4/§2.5(策划 2026-08-21 下达)。

## 协作守则
- 只改 `engine\`;不碰主项目源码(项目根的 .cs 与 csproj 归主代理维护)
- 新增文件放 `engine\` 对应模块目录;测试断言进 `DemoRunner.cs` 或 `Tests\EngineChecks`
- 有疑问或需要主代理配合(如改 csproj 排除规则),通过用户转达,或在 `验收反馈.md` 的"主代理待办"区留言

## 指令 5(2026-08-21,团队协作组织后下达)

目标:引擎侧为"主项目已接入的 maimai/ADOFAI"提供配套验证与样例,并按新玩法补齐引擎 API 覆盖。

1. **maimai RingField/RingNote 对接验证**:主项目 DrawMaimai 已按官方 simai 语义实现(键位 8=上 0°/1=右上 45° 顺时针、角度判定用主项目 JudgeSettings)。请在 DemoRunner 增加断言:RingField 8 分区方位角映射(SectorCount=8、StartAngleDeg、Clockwise)与 RingNote.SlideProgress 在 45°/90°/180° 转角下的进度/方向语义正确(与官方 simai 键位表一致)。
2. **ADOFAI 引擎样例**:在 README 增加"双球轨道玩法(类 ADOFAI)"引擎组合示例——RingNote(固定球/绕旋球)或 PathField 双轨、每拍按键、角度判定(可复用 JudgementProfile)、掉轨重开语义(检查点);无需主项目代码,引擎独立可运行。
3. **判定闭环确认**:确认 JudgementTracker 与主项目 _eng(JudgementEngine) 的 tick 口径一致性说明(ScoreBoard.ApplyTick vs 主项目 ApplyTick(score))写入 README 对照小节。
4. 保持现有 API 兼容;断言失败会由监控自动反馈。

完成后 	ools\协作.ps1 done "指令5完成:..." 留言并触发自动验收。