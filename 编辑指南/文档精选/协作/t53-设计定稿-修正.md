# t53 设计定稿审阅与修正（designer-vis 结单）

> 审阅对象：其他\\docs\\协作\\legacy-removal-design.md（designer-pro 起草，217 行）
> 审阅方式：源码事实核查（grep/read 全量验证引用点）→ 判定
> 结论：**设计整体完备可执行，但 §1.1 对 FolderPanel 的关键判定有事实错误，须修正后方可交 coder-vis 实现；其余批次/引用点/迁移方案通过。**

---

## 一、必须修正项（否则照做即编译失败/引擎曲库页失效）

### ✘ 修正 1：FolderPanel **不能整体删除**（§1.1 判定表 + §2.2 引用性质标注均错）

文档 §1.1 写「引擎曲库页已替代（同实现）→ 删」；§2.2 把 SecondaryPages.cs:87/93/97 标为**「注释/文案对照」**。**事实相反**：引擎曲库页不是"替代" FolderPanel，而是**运行时委托 FolderPanel 实例**执行全部数据/命令：

| 位置 | 实际性质 |
|---|---|
| SecondaryPages.cs:87 | 字段 `FolderPanel _folderLogic;`（运行时引用） |
| SecondaryPages.cs:93-103 | `FolderLogic()` 懒建 `new FolderPanel(AppConfig.Load())` + 接线 GoSong/GoHome/FolderChanged |
| SecondaryPages.cs:138-146 | 引擎页 7 个按钮回调直调 `FolderLogic().PickFolder()/ImportFiles()/ScanZip()/RefreshData()/OpenDir()/DeleteSelected()/CopySelectedPath()` |
| SecondaryPages.cs:100 | `FolderChanged` → 引擎页刷新 + `InvalidateSongsScan()`（t39 同源联动） |

**修正判定：删 UI 不删逻辑 —— FolderPanel 降级为纯逻辑类**：
- 删除：`Build()` 全部 WinForms 控件（_headBar/_title/_pathLabel/_statsLabel/_btnXxx/_filter/_listTitle/_chartList/_selHint + 事件接线 + 底部工具栏），约 300 行。
- 保留：`AppConfig _cfg` 注入、数据 `_items`、`FilteredItems(int)`(L260)、`RefreshData()`(L282)、`PickFolder()`(L388)、`ImportFiles()`(L406)、`ScanZip()`(L432)、`OpenDir()`(L466)、`DeleteSelected()`(L477)、`CopySelectedPath()`(L493)、`PathText/StatsText`(L251/254)、事件 `GoSong/GoHome/FolderChanged`(L37-39)。
- 改造方式二选一（建议 A）：
  - **A. 原地瘦身**：FolderPanel.cs 改为普通类（去 : UserControl、去 SysDrawing/Forms 不必要 using；文件名可保留），SecondaryPages 的 `_folderLogic` 类型同步改。改动面最小、引擎页零行为变化。
  - **B. 移植引擎侧**：新建 `引擎/...` 或 `源码/Play/EngineUi/FolderLogic.cs` 同名类（需 AppConfig 在引擎侧可见性，改动面大）。
- 删除量重估：批2 删 ≈ 300 行（FolderPanel UI 半壁）+ 两个整文件（SongCardView/FolderPanel 之外）→ 总净删 ≈ 2400-300=2100 行、删 3 文件改为「删 2 文件 + 瘦身 1 文件」。

### ✘ 修正 2：保留组漏列 2 个活跃窗体类（§1.2 表）

| 类 | 运行时引用 | 结论 |
|---|---|---|
| **MpLobbyForm**（源码\\Forms\\MpLobbyForm.cs） | MpEmbedPanel.cs:41 `new MpLobbyForm(_game)`（联机承载=保留组内），其内部 L123 还 `new AiSetupForm()` | **保留**（§1.2「联机」行应显式列出，防误删） |
| **AiSetupForm**（源码\\Forms\\AiSetupForm.cs） | MainForm.cs:1062 `AiSetupForm.PickLevel(this)`（AI 演示选谱，§3.1 保留职责）+ MpLobbyForm.cs:123 | **保留**；顺带核查：t12 禁用本地 AI HTTP 后，其内若仍有 Ollama 地址/模型字段=死 UI，可留可清（不阻塞本设计） |

---

## 二、通过项（复核确认，无异议）

1. **删除组其余判定全部属实**：CliLegacy/--legacyui（MainForm:73/96/242/267/274/279/283/348/1275 + ParityCli:518 + Program.cs:1014 与文档一致）；SongCardView（运行时引用仅 MainForm:17/1023/1108，引擎 SongGrid 为独立实现 ✅ 可删）；CalibEmbedPanel（运行时引用仅 MainForm:25/1138 ✅ 可删）；--menushot（Program.cs:1003-1030，CliLegacy=true 旧菜单截图 ✅ 与 §4.1 描述一致）；CalibrationForm 14 处引用核实为全部保留（SettingsPanel:348 + SecondaryPages:196-229 真实使用 ✅）。
2. **§1.3 待核对项方向正确**（dev CLI 参数 grep tools\\ 后定；parity 基线键联动批4）。
3. **§3 MainForm 最小化方案**（纯宿主 + Act 白名单 + 兜底纯游玩 ≈20 行 + _settingsView/_mpView/_editorView 三容器保留）合理；§3.3 兜底替代旧回退，**可接受**（引擎壳不可用概率低，纯游玩兜底够用）。
4. **§4 parity/--menushot 迁移**：--menushot 合流 --shellshot 管线（Program.cs:1149 RunShellShot 已存在）零新代码 ✅；ParityCli 四段改造（CollectTexts 换 EngineUiDemoForm / legacy-mainmenu.png 删 / actions 直查 / ClickByText 不动）✅；BridgeFeatureNames:641「曲库管理 FolderPanel 全部按钮」改「引擎曲库页」随批4 ✅。
5. **护栏 5 条**（每批构建 0/0 + parity + shellshot 证据；批1 前确认 22 处调用点直连；UiText 18 键保留；基线不静默改；源码\\ 范围）全部合理，增补见下。

---

## 三、实现分批意见（供 coder-vis 执行；与 t54 互锁）

| 批 | 内容 | 依赖 | 备注 |
|---|---|---|---|
| **批1** | 旧主菜单/_menu/_status/CliLegacy/--legacyui（解析改 log 提示弃用）/22 处 ShowMainMenu→ShowEngineShell/_st 写入点改 Logger | 无 | **可立即开工**（引擎零依赖；_redirectToEngine 随 ShowMainMenu 删） |
| **批2** | 删 SongCardView.cs/CalibEmbedPanel.cs 整文件 + FolderPanel 瘦身（▲修正 1）；MainForm 删 _library/_folder/_calib 字段与 Build*/Show* 方法；清 EngineMainShell/SecondaryPages/UiText/ModeSystem 注释 | 批1 | **可立即开工**；注意 SecondaryPages `FolderLogic()` 返回类型同步改 |
| **批3** | 删 ShowView/TransitionTo/_fade/HideAllMenus/HideOthers/7 视图面板（留三容器）；Act 白名单；兜底纯游玩 | 批2 | **与 t54 引擎改动互锁**：actions public 化（§3.4）**归引擎侧 t54 一并做**（避免游戏层改完被引擎侧重排）；其余子项独立 |
| **批4** | --menushot 合流 --shellshot；ParityCli 四段；BridgeFeatureNames/基线复核 | 批3 | parity -1 全绿后收口 |

**批序调整理由**：文档§3.4 已提示 actions public 化需与 eng 对齐——t54（引擎工具化/模式预设/引擎内游戏预览）正由 eng-design-vis/eng-coder 推进，改为：**游戏层只做「只读直查 EngineMainShell 公开动作表」的适配，引擎侧新增 public 属性**；若 t54「引擎内游戏预览」落地（引擎可预览游戏内容），批3 的"游玩承载通路/AI 演示选谱"将有二次简化空间——**作为 t54 后的 t53-后继评估项，不阻塞当前删除**。

**总删除量修正**：净删 ≈2100 行（MainForm ~1500→~900 行宿主）；删 2 文件（SongCardView/CalibEmbedPanel）+ FolderPanel 瘦身 ~300 行；引擎改动仅 actions public 化（归引擎侧）。

---

## 四、回归矩阵增补（原 §5 第 1-15 行后追加）

| # | 场景 | 验证 | 预期 |
|---|---|---|---|
| 16 | **引擎曲库页 10 按钮 + 筛选 10 钮逐一走查**（FolderPanel 瘦身直接影响） | 引擎页点击 + parity | 选择文件夹/导入/扫描解压/刷新/打开目录/删除/复制路径/去选歌/主菜单全部正常；筛选 5+5 两行生效 |
| 17 | 联机大厅 → AI 设置入口（MpLobbyForm:123 → AiSetupForm） | 联机打开 AiSetupForm | 弹窗正常（若字段已死则显示为禁用态，记录） |
| 18 | EngineMainShell actions 直查（批3 后） | parity 桥接覆盖段 | 覆盖判定语义不变（identical/divergent） |

---



---

## 六、补充核查（captain 指定三项 · designer-vis 专项复核）

### 6.1 与 t54 引擎设计联动——引擎内已替代项的删除清单标记

- 删除清单标记总体正确：§1.1 每行均标注"引擎化已覆盖"依据（选歌/曲库/段位/回放/校准/玩家/我的数据/关于/主题/打开日志/设置行），与 MainForm.actions 字典（MainForm.cs:861-891，18 键）实测一致。
- **唯一反例即 §一 修正 1**：曲库页"已替代（同实现）"实为"运行时委托 FolderPanel"——该行标记必须按修正生效。
- **t54 联动闭环缺口**：t54（引擎工具化/模式预设/引擎内游戏预览）design 进行中（eng-design-vis attempt 5，尚未产出文档）。两处预留：①"引擎内游戏预览"落地后，白名单"AI 演示选谱 AiDemoPickChart"+游玩承载通路有二次简化空间（=t53 后继评估项）；②"引擎工具化"若新增引擎 CLI/工具入口，§1.3 待核对项（--legacysettings/--uidebug 等 dev 参数删留）应随 t54 定稿一并裁定。**动作项：t54 设计定稿+实现落地后，需回扫一次本删除清单（coder-vis 在 t56 收尾时核对，或 t54 后追加小审查任务）。**

### 6.2 --legacyui/CliLegacy/--menushot 迁移方案 vs ParityCli 现状——2 处必须修正

**(a) 批序编译断档（硬问题）**：CliLegacy 字段的消费方不止 MainForm——ParityCli.cs:518（MainForm.CliLegacy = true）与 Program.cs:1014（--menushot 段）都是**编译期依赖**。文档批1 计划删 CliLegacy 字段（§2.5"批1/批4 删除"含糊），但批4 才改 ParityCli/--menushot → **批1 后工程无法编译**。
**修正**：**CliLegacy 字段与 --legacyui 参数的删除推迟到批4 最后一步**；批1 只删其消费 UI 路径（BuildMainMenu ctor 分支/ShowMainMenu 分支/F1 分支等 MainForm 内 6 处），保证批1~批3 每批 0 警告 0 错误可编译可验收。ParityCli legacy 块①② 改造完成后，批4 收尾统一删除字段+参数（其余 5 处 MainForm 引用同步清）。

**(b) §4.2① CollectTexts(EngineUiDemoForm) 方案不可行（硬问题）**：EngineUiDemo.BuildMenu（EngineUiDemo.cs:136-161）实际文案='MILESTONE / Unity 式场景运行时·引擎驱动 UI·现代深色玻璃 / ▶  开始游玩 / 🎵  选歌 / ⚙  设置（主题/交互） / 🌀  引擎演示（再演一遍转场）'；而 parity 基线键 LegacyMenuKeys()（ParityCli.cs:626-635）= UiText.Menu* 20 键（'▶ 开始游戏'/'⚙ 设置'/'💡 快捷键：F1 显示/隐藏菜单栏 · Milestone v6.0' 等）。**两者不同源**（t43 注释也明示"演示窗真实按钮文案≠壳的 开始游戏/设置"）——CollectTexts(EngineUiDemoForm) 后 20 基线键几乎全缺 → parity -1 红。
**修正（推荐）**：**①③合并**——"legacy 主菜单文案快照"段退役：LegacyMenuRender 数据源改为 EngineMainShell.Actions（§3.4 public 只读）键全集（18 动作键 + MenuPlayGame + MenuHint），逐键断言在壳动作表/壳页面中覆盖，覆盖判定语义与现 BridgeButtons 一致（identical/divergent）；字段结构可保留（改名 ActionsCoverage）不破坏 parity 输出 schema。**legacy-mainmenu.png（ParityCli.cs:536-543）删除**，图形证据改用 §4.1 的 mainmenu.png（壳渲染；用户已定默认引擎 UI，旧菜单对比证据无保留必要）。

### 6.3 游戏内 Act 白名单 8 项 vs 引擎菜单 18 动作——**完整 ✅ 无遗漏**

actions 字典 18 键（MainForm.cs:861-891）逐键比对：
- **Act 承载 8 项**（=文档 §3.1 白名单，一一对应）：MenuMp→ShowMp、MenuSettings→ShowSettings(0)、MenuAiDemo→AiDemoPickChart、MenuLayoutEditor→EnterLayoutEditor、MenuOpenLog→Logger.OpenLog、MenuLoopComposer→StartLoopComposer、MenuMiniMania→MiniManiaForm、MenuEngineUiDemo→ShowEngineUiDemo。
- **引擎化 9 项**（非白名单正确归组）：MenuEditChart（t50 引擎窗 HostContent(_editor)）、DanChallenge→GoToPage('dan')、Replay→GoToPage('replay')、Folder→GoToPage('folder')、Calibration→GoToPage('calibration')、PlayerInfo→GoToPage('player')、MyData→GoToPage('mydata')、About→GoToPage('about')、Theme→CycleTheme()（t60 引擎内）。
- **1 项**：MenuExit→Application.Exit。
- **特殊 2 键**：MenuPlayGame/MenuHint **不在 actions**（壳内选歌/提示自行处理；ParityCli.cs:568 已识别为 'identical(壳内页面/提示，非动作键)'）——实现时**不要**误判为"缺桥接"；文档 §3.1 建议顺带注明此 2 键。
- 结论：**白名单 8 项 = 引擎菜单遗留的全部非引擎化动作，无遗漏无多余。**

### 6.4 t56 建议（交 captain 决策，实现任务给 coder-vis）

t56 主题：**legacy 删除实现（按四批）**：批1 旧主菜单/_menu/_status/_fade/ShowView 系（**不删 CliLegacy 字段与 --legacyui 参数，留至批4**）+22 处 ShowMainMenu→ShowEngineShell 直连+_st 写入改 Logger；批2 删 SongCardView/CalibEmbedPanel 两文件+FolderPanel 瘦身（§一 修正1 案 A）+SecondaryPages FolderLogic() 返回类型同步+清注释；批3 MainForm 宿主收口+Act 白名单+兜底纯游玩+引擎壳 actions public 化（与 t54 引擎侧互锁：t54 未完成则游戏层先只读适配）；批4 ParityCli ①②③ 改造（①按 6.2(b) 合并/退役）+--menushot 合流 --shellshot+**最后删 CliLegacy 字段与 --legacyui 参数**+基线复核+回归矩阵 1-18 全绿。验收=每批构建 0/0+selfcheck 0+parity -1 全绿+shellshot/menushot 产物正常+人工走查。

## 七、交付结论

- **定稿**：legacy-removal-design.md + 本修正单 = t53 设计定稿；修正 1 为硬性前置（不修不实施批2）。
- **实现承接**：新建实现任务交 **coder-vis**（批1→批2→批3→批4；批1/批2 立即，批3 与 t54 互锁，批4 收口）；引擎侧 actions public 化由 t54 引擎编写承担。
- **验收口径**：每批构建 0 警告 0 错误 + --selfcheck exit 0 + parity -1 全绿 + --shellshot/--menushot 产物正常 + 回归矩阵 1-18。
