# t51 实现报告：最近谱面按钮硬截断去除 + 键位录入框加宽

- 日期：2026-08-27 · 承办 coder-max · 约束：只写代码不构建（captain 构建 + 试玩复验）

---

## ① 编辑器左栏「最近谱面」24 字符硬截断+省略号（禁省略号红线唯一残留）
- 原：`if (name.Length > 24) name = name.Substring(0, 24) + "…";`（Button 单行截断）。
- 修（Label 行全文方案）：
  - 去截断：`Text = "📄 " + name`（完整文件名）；
  - `AutoSize + MaximumSize(Ui.P(168), 0)`——宽内换行全文（左栏窄列不再裁切）；
  - 点击装载（LoadChart 同逻辑，文件缺失提示/移除/刷新不变）；
  - 悬停 tooltip 全名+完整路径（复用 t43 `_tipTooltip`）；
  - 手型光标 + MouseEnter/Leave 悬停提亮（原 Ui.Hover 按钮反馈等效）。

## ② 键位录入框（legacy SettingsPanel）10K 长键名截断候选
- `_keyText = Txt(300)` → **`Txt(380)`**——「D F J K Space L ; ' , . /」类 10K 键名串全文无截断。

## 变更文件
- `源码/Charting/ChartEditorPanel.cs`（RefreshRecentButtons 行渲染）
- `源码/Forms/SettingsPanel.cs`（BuildKeys _keyText 宽）

## 试玩复验口径（captain 构建后）
1. 编辑器左栏最近谱面：长文件名换行全文、点击装载、悬停 tooltip 全名；
2. 设置·键位：10K 键名串录入/显示全文。
