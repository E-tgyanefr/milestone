# 评审 · 复评 2026-08-24（t3：t2 后证据链复评 · 对照 t6 基线）

> 评审员 reviewer · 2026-08-24 10:5x · 工作目录 D:\Users\etgya\Desktop\milestone
> 被评对象：engineer t2 六项 P0 恢复（垂直柱/拍:0:1/音符面板/执行列表/预制/曲线填充+交互适配）
> 方法：**独立重跑**（先 Stop-Process 清理 Milestone → 每命令记录 exit；不采信 t2 自报）。未改任何源码（仅新增证据目录/文档）。

## 1. 命令对比表（t6 基线 vs 复评）

| 命令 | t6 基线 exit | 复评 exit | 复评证据 |
|---|---|---|---|
| `dotnet build -c Release` | 0（0/0） | **0（0 警告 0 错误）** | bin dll 生成 |
| `--selfcheck` | 0 | **0** | 根 selfcheck.log 10:5x 重跑，12 组断言全过 |
| `--modetest` | 0 | **0** | 关闭=仅 Mania/开启=11 模式 ✅ |
| `--edshot phigros_test.json 4` | 0（9 张） | **0（9 张）** | obj\edshot\agent-r1\（1400x860，0.5s 步进） |
| `--edshot phigros_edsim_test.json 4`（补充机位） | — | **0（9 张）** | obj\edshot\agent-r1-edsim\ |
| `--edsim phigros_edsim_test.json` | 0（7/7） | **0（12/12 断言）** | obj\edsim\agent-r1\edsim.log（比 t6 多 5 项：框选/组拖/Delete/面板控件等） |
| `--shotdemo phigros_test.json 4` ×5 | 首跑 -1073741819（1/2） | **4×0 + 1×-1073741819（1/5 崩溃）** | obj\edshot\agent-r1-shot/-shot2/-shot3/-shot4/-shot5 |

**结论**：基础链（build/selfcheck/modetest/edshot/edsim）全部达标；**shotdemo 稳定性 ❌（崩溃仍复现）**。

## 2. t6 基线 8 项 ❌ → 复评对照矩阵

| # | 判据（t6 基线=❌） | 复评 | 证据 |
|---|---|---|---|
| 1 | 垂直事件柱 | ✅ **已实现** | edshot agent-r1：右侧五列垂直柱（x=通道，柱宽=值），柱内黄色缓动曲线，白色键帧柄，播放头横线；左场约 62% |
| 2 | 拍:0/1 时间显示 | ✅ **已实现** | edshot 底部时间条：1:0/1 / 5:0/1 / 9:0/1（播放头 3:0/1、7:0/1 红色） |
| 3 | 音符编辑面板 | ⚠️ 控件存在 ✅；UI 图像未拍到（选中态+滚动区） | edsim 断言："音符编辑（Phigros·选中）: ✅" |
| 4 | 执行列表 | ⚠️ 控件存在 ✅（MirrorY/ToFlick/AttachX）；行为级断言未入 | edsim 控件树断言；BatchEdit 语义 t2 自报实现（无运行时断言） |
| 5 | 预制事件 | ⚠️ 控件存在 ✅（🚀 倍速2x/🌑 淡出）；生成行为未断言 | edsim 控件树断言 |
| 6 | 曲线填充（Ctrl+F/G） | ❌ **未实现**（t2 声明"归 P1"，但任务名含此项） | 全库无 FillCurveNotes；无 C 键分支；edsim 无 D2q 断言 |
| 7 | Bezier 保存（P0 数据保真） | ⛔ 未评（属 t5 Bezier/Next/Decor/tick率，engineer-local 进行中） | t5 完成后复验 |
| 8 | 事件级多选/框选/组拖/批删（D2d） | ✅ **已实现**（行为级断言） | edsim：框选 9/组拖+1 拍全同步/Delete 批删 5→4 |

**汇总**：8 项中 ✅ 3 项（1/2/8）· ⚠️ 3 项（3/4/5，控件存在断言但行为/图像未覆盖）· ❌ 1 项（6 曲线填充）· ⛔ 1 项（7 归 t5）。

## 3. 六项（t2 目标）逐项结论

| 项 | 结论 | 依据 |
|---|---|---|
| ① 垂直事件柱 | ✅ 达标 | edshot 识图（五列+黄曲线+键帧柄）+ edsim 垂直交互（竖拖/横拖/框选/组拖） |
| ② 交互垂直适配（键帧单击/竖拖改时/横拖改值/不增删） | ✅ 达标 | edsim 12/12：DragKind=26 命中/500→1500ms/值 0.5→0.8/计数不变 |
| ③ 拍:0/1 时间显示 | ✅ 达标 | edshot 时间条 1:0/1、5:0/1、9:0/1（对比 t6 分秒 0:00/2.00s） |
| ④ 音符编辑面板 | ⚠️ 部分（控件在，图像未取证） | edsim 控件存在断言；t2 自报 Side/Width/Alpha/VisMs+解析落盘（round-trip 未入断言） |
| ⑤ 执行列表 11 项 BatchEdit | ⚠️ 部分（按钮在，行为无断言） | edsim 控件存在（MirrorY/ToFlick/AttachX）；语义仅 t2 自报 |
| ⑥ 预制事件按钮组 | ⚠️ 部分（按钮在，生成行为无断言） | edsim 控件存在（2x/淡出）；走 AddLineEventAtPlayhead（t2 自报） |

## 4. shotdemo 崩溃复跑 5 跑表（崩溃协议）

| # | 目录 | exit | shotdemo.log | PNG | 结论 |
|---|---|---|---|---|---|
| 1 | obj\edshot\agent-r1-shot | 0 | ✅ | 40 | ✅ |
| 2 | obj\edshot\agent-r1-shot2 | 0 | ✅ | 30 | ✅ |
| 3 | obj\edshot\agent-r1-shot3 | 0 | ✅ | 40 | ✅ |
| 4 | obj\edshot\agent-r1-shot4 | **-1073741819** | ❌（无） | 31 | ❌ **崩溃复现** |
| 5 | obj\edshot\agent-r1-shot5 | 0 | ✅ | 40 | ✅ |

- **❌ 判定：崩溃未修复（复现 1/5 = 20%）**。第 4 跑 stderr 堆栈与 t6 完全一致：`GamePanel.DrawHud → PaintCore → RenderThreadLoop`（System.AccessViolationException）；崩溃前已产出 31 张 PNG、无 log（WriteLog 未达）——与 t6 首跑现象一致。
- 根因诊断已出：obj\agent-shotdemo-crash.md（OnHandleDestroyed 无锁释放 _d2d 且不停渲染线程 + Join 时机过晚）。**GamePanel.cs 本轮未被修改**（t2 仅改 ChartEditorPanel/Models/ChartParserExtra/Program.cs）→ engineer 声称"修复"不成立/未提交。
- 建议：按诊断报告建议（#1 OnHandleDestroyed 先停线程 Join(1500) 再 lock 释放 _d2d；#2 所有 _d2d.Dispose 入锁；#3 渲染循环 +!IsDisposed）修复后重跑 ≥5 次。

## 5. 未验证项（如实标注，不臆造）

1. **贝塞尔手柄（DragKind=30）**：无 edsim 运行时断言（edsim 说明段明示"桩在 t2 交互段验证绘制；edshot 截图人工核验"）——绘制已可见（t2 自报），拖拽交互未覆盖。
2. **BatchEdit 行为断言**：未入 edsim（需先构造音符多选态）。
3. **mil round-trip 断言**（Side/Width/Alpha/VisMs 保存→重载）：未入 edsim。
4. **Bezier 保存链**：归 t5（P0 数据保真，in_progress）——本复评未判定。
5. 曲线填充：t2 任务承诺项，**未实现**（t2 报告声明归 P1）——需队长裁决续做/降级。

## 6. 回归结论

- 基础链 ✅：build 0 警告 0 错误 + selfcheck/modetest exit 0 + edshot exit 0（9+9 张）+ edsim exit 0（12/12）。
- 功能链：8 项❌中 3 ✅ / 3 ⚠️ / 1 ❌（曲线填充）/ 1 ⛔（Bezier 保存归 t5）；六项目标中 3 项全 ✅、3 项部分达标（控件在、行为/图像待补）。
- 稳定性 ❌：--shotdemo 崩溃未修复（1/5 复现，含完整堆栈证据）。
- **总判定：t2 复刻基本达标（可放行继续），但 ①曲线填充需裁决 ②shotdemo 崩溃修复为阻塞项（建议 engineer 处理后再验收）③三项断言缺口（贝塞尔/BatchEdit/round-trip）待后续补。**
