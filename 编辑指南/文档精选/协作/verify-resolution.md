# T80 分辨率问题取证报告（当前 bin）

- bin = 输出产物\bin\Release\net8.0-windows\win-x64\Milestone.exe（08:37:01）· 证据=输出产物\dev\obj\verify-resolution\（r80-01/02/03）

## 取证数据

| 界面 | 窗口 rect | 客户区 | DPI | 问题 |
| --- | --- | --- | --- | --- |
| 引擎菜单（主窗） | **(-32000,-32000,-31763,-31961) 237×39** | 0×0 | 144 (1.5) | ❌ **窗口屏外/最小化态**（-32000 坐标=Win32 隐藏标记+237×39 标题栏残留——engine-shell show 后主窗未正常还原） |
| 游玩（--play std_test） | (0,0,2560,1600) **2560×1600 全屏** | 2538×1544 | 144 (1.5) | ✅ 正常（满屏渲染） |

## 用户反馈『分辨率不对』定位

- **问题界面=引擎菜单（主窗）**——窗口在屏幕外（-32000 坐标）+ 237×39 尺寸（最小化/隐藏态残留）——**主窗未正常显示**（用户看到异常/极小/黑标题栏）。
- **问题类型=窗口管理回归**（非渲染分辨率/模糊——DPI 144 (1.5) 缩放正常；游玩 2560×1600 满屏正常）。
- **t27 基线对比**：t27 引擎菜单窗口=144,33,2415,1495（2271×1462 正常位置）——**当前 -32000 屏外=回归**。

## 根因方向（P1）

- engine-shell 主窗 StartupClientSize/Show 后**窗口位置未还原**（-32000=Win32 最小化/屏外标记——奇偶：主窗创建时位置计算为屏外（多显示器/DPI 1.5 session 下 StartupClientSize=工作区但实际放置屏外——t27 正常位置 144,33 现在 -32000）。
- 日志『engine-shell show → 可见窗口 15』（窗口枚举存在但 show 后 rect 屏外——**show 未还原位置**——StartupClientSize/SetWindowPos 位置逻辑回归）。

## 判定

**P1：引擎菜单主窗窗口管理回归（屏外/最小化 237×39——用户『分辨率不对』根因）**——修复方向：①EngineMainShell StartupClientSize/Show 后窗口还原到主屏工作区（t27 同逻辑 144,33）②检查多显示器/DPI 1.5 下窗口位置计算（-32000=屏外 bug）③修复后主窗应=工作区（2560×1600 内），游玩已正常（2560×1600 满屏）。
---

## 补充客观测量值（captain 要求：窗口 rect/客户区/字符像素高）

- **启动窗口**：rect=(-32000,-32000) 237×39（屏外/最小化标记）；**ShowWindow(3) 还原后仍 0×0**（client=0×0）——**主窗窗口创建/还原失败**（非 0.92 工作区——窗口根本未正常显示）。
- **游玩窗口**：rect=(0,0,2560,1600)=屏幕 100% 满屏；client=2538×1544——渲染缩放=2538/1280=1.983（逻辑 1280×720 → 1.984×——**原生满屏锐利**——PrintWindow 文字清晰无模糊拉伸——游玩分辨率正常）。
- **字符像素**：游玩 Score 行字符高≈16px（1.983 缩放下清晰）——无模糊；菜单 Logo 不可见（窗口 0×0）。
- **设置页**：legacy 独立窗（历轮 t50 正常——非本问题）。

## 问题类型判定（根据客观值）

- **类型=主窗窗口创建/还原失败**（0×0/-32000 屏外——非模糊/过大/过小/裁切/letterbox——是**主窗未显示**）；游玩满屏 1.983 缩放正常（原生锐利——非渲染问题）。
- **用户『分辨率不对』体验**=启动后主菜单窗口不可见/极小屏外。
- **修复方向（P1）**：EngineMainShell 窗口创建/还原——启动 Show 后窗口还原工作区（t27 基线 144,33 2271×1462——当前 0×0/-32000——查 StartupClientSize/Show/SetWindowPos 链——多显示/DPI 1.5 下计算 0 或屏外）。