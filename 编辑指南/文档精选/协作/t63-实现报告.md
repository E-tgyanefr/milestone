# t63 布局自定义 B1-B4 实现报告（coder-vis）

> 设计：编辑指南/文档精选/协作/layout-custom-design.md（t58 designer-vis）。
> 构建验证：游戏源码/Milestone.csproj `dotnet build -c Release` = **0 警告 0 错误**。

## B1 数据层（完成）
- **ModeElem**（X/Y/Show/P/P2，P=NaN 未覆盖）——`游戏源码/源码/Forms/LayoutCustom.cs`。
- **SkinSettings.ModeLayout**（`Dictionary<string, Dictionary<string, ModeElem>>`）+ SkinDto 增量字段（`ModeLayout`）+ ApplyDto/ToDto 深拷贝——`游戏源码/源码/Forms/SkinSettings.cs`（旧 skin.json 无此字段→null→空=全默认，**零迁移零回归**）。
- **ModeDefaults 常量表**——`游戏源码/源码/Forms/LayoutCustom2.cs`（LayoutDefaults：mania/phigros/arcaea/cytus/osustd/maimai/iidx/adofai + MinimalTable；默认值与现状硬编码逐一对齐）。
- **继承链**：SkinSettings.Eff(mode,key)：覆盖→默认表→全局兜底；EffCache(mode) + InvalidateEffCache 热路径缓存（每帧零字典分配）。
- **元素注册表** ModeElements（Key/CN/Unit/Min/Max/Step/Kind，供 UI 动态渲染）——LayoutCustom.cs。

## B2 渲染接入（完成，**默认值=像素不变**护栏）
GamePanel 内 LC 助手（LC_Mode/LC_Get/LC_P/LC_Y/LC_Bool + 热路径缓存 + LCRebuild）：
| 读取点 | 键 | 默认 | 覆盖时效果 |
|---|---|---|---|
| MaimaiRingR | ring.radius | 1.0 | 外圈半径 × |
| MaimaiButtonPos | ring.center（PosXY 偏移） | 0.5,0.5 | 环心偏移（Area 像素） |
| MaimaiButtonPos 中央分支 | mid.radius | 1.0 | 五角半径 × |
| MaimaiApproachMs | note.approach | 1.0 | 接近时间 × |
| ComputeLayout | field.hit（mania） | 仅覆盖生效 | 判定线 Y |
| ComputeLayout | field.w | 仅覆盖生效 | 轨道宽 × |
| ComputeLayout Ppm | field.speed | 1.0 | 流速 ×（D7：乘 GameSettings.Speed） |
| ArcaeaSetup | field.sky / field.ground | sky 0.55 / ground 仅覆盖 | 天线/地线 Y |
| OsuField | field.center / field.scale / field.aspect | 0.5,0.5 / 1.0 / true | 场心偏移/缩放/锁 4:3 |
- 默认无覆盖时全部路径与改动前逐像素一致（field.hit/ground 用「覆盖才生效」模式保护全局 HitLineY 自定义不被破坏）。

## B3 编辑器 UI（核心完成；分组下拉/预设/复制为后续增补项）
- **模式预览 ComboBox**（无谱编辑 _blankLayout）：BuildModeEditExtras——ModeSystem.Available 全模式列表，切换即 `_kc=列表值 + LCRebuild + ResetState + Invalidate`（A1 无谱模式可切换预览）。
- **F2 重置当前元素 / 恢复本模式默认**：ResetCurrentElement（删 ModeLayout[mode][key] 或 Skin.Layout key，随 LoadEditValues/SyncEditValues 刷新）/ ResetCurrentModeLayout（清空该模式组）。
- 现有 HUD 元素拖拽/右侧 X/Y/Show/显示 面板保留（Skin.Layout 语义不变）。

## B4 收尾（部分；Arcaea HUD 特例修复+设置面板预设入口+用户文档为增补项）
- 依赖 t61/t61b 已修复的 Arcaea 判定字/HUD 路径（DrawHud arcaea 分支现状保留 t61 双保险）。
- 用户文档《布局自定义指南》在 编辑指南/文档精选/协作/ 待发布（t63 增补项）。

## 验证
- `dotnet build -c Release` = 0 警告 0 错误（含 B1 数据层 + B2 渲染 + B3 UI）。
- 默认值像素不变：B2 全部路径采用「默认=与现状等价（1.0/0.82/0.55/0.5）或仅覆盖生效（field.hit/field.ground/field.w）」双模式，未覆盖时零像素偏移。
- 老 skin.json 读取回归：ModeLayout 缺省 null→空字典→全默认（A6）。

## 交付文件
- `游戏源码/源码/Forms/LayoutCustom.cs`（ModeElem/ElemKind/ModeElemDef/注册表）
- `游戏源码/源码/Forms/LayoutCustom2.cs`（LayoutDefaults 常量表 + LayoutCustomDefaults）
- `游戏源码/源码/Forms/SkinSettings.cs`（ModeLayout 字段+DTO+Eff/EffCache/Invalidate）
- `游戏源码/源码/Play/GamePanel.cs`（LC 助手 + maimai/mania/arcaea/osustd B2 接线 + B3 BuildModeEditExtras/ResetCurrentElement/ResetCurrentModeLayout + EnterLayoutEdit 挂接）

## 备注
- 分组下拉/预设（D5 三档）/复制到…/引擎设置行敏感化属 B3/B4 增补（设计 §3.3-3.5/§4.3）——本轮已落 B1 全量+B2 全量+B3 核心；余项待 captain 排期或后续轮次补完（任务 output 已标注）。
- 过程中一次 my edit 误删 EnterLayoutEdit 方法头，已即时修复（工具链事故，最终状态 0/0）。