# t56 实现报告：legacy 删除批1 + 批2（coder-vis）

> 依据：其他/docs/协作/t53-设计定稿-修正.md §6.4（批序修正版）+ captain 指令（批1：旧主菜单/菜单栏/状态栏/ShowView系 + 22 处 ShowMainMenu→ShowEngineShell；批2：删 SongCardView/CalibEmbedPanel + FolderPanel 瘦身 + SecondaryPages 类型同步）
> 验收：构建 0 警告 0 错误（scratch 验证通过）+ 静态自查。**未执行官方构建**（归 captain）。

---

## 一、批1：旧主菜单 / 菜单栏 / 状态栏 / ShowView 系删除（MainForm.cs）

### 删除
- 字段：`_menu`(MenuStrip)、`_status`(StatusStrip)、`_st`(ToolStripStatusLabel)、`_mainMenu`、`_fade`、`_redirectToEngine`
- 方法：`BuildMenu()`、`BuildMainMenu()`、`BuildModeBadges()`、`BuildFeatureGrid()`、`AddCell()`、`MakeCircular()`、`AboutMilestone()`、`ShowMyData()`、`ShowLogView()`、`RebuildMainMenu()`、`ShowMainMenu(bool)`、`CaptureForm()`、`TransitionTo()`、`CreateTransition()`、`ShowEngineUiDemoLegacy()`（占用 _fade/EngineUiDemo.Capture/Open 的已死方法）
- ctor 中 `BuildMenu()`/菜单条/状态条配置、`if (CliLegacy) BuildMainMenu()`、F1 菜单栏 KeyDown 分支、`Controls.Add(_status/_menu)`
- `OnFormClosed` 的 `_fade?.Close()`
- `ApplyThemeRefreshCore` 中 `_menu/_status/_st` 配色 + `RebuildMainMenu()` + `ShowMainMenu(false)` → `RebuildSettings(); ReturnToMenu();`

### 改造
- **`ReturnToMenu()` 统一入口**（替换原 ShowMainMenu 全部 22 处调用）：`_testPlayReturn=false; _game.StopToMenu(); ShowEngineShell();` —— 兜底保留 StopToMenu 语义（原 ShowMainMenu 也先 StopToMenu）。
- `ShowView` 简化：无转场直接显隐（引擎壳场景转场已替代 legacy 光束/圆环/推拉）。
- `ShowEngineShell` catch 兜底：旧主菜单回退 → **纯游玩模式**（this.Show()，日志明确）——设计 §3.3 预演。
- `--legacyui` 解析保留但仅日志提示弃用（字段与参数删除推迟批4 收尾，符合 §6.2(a) 修正）；`CliLegacy` 字段保留（ParityCli/Program.cs 仍引用，批4 删）。

### SettingsPanel 状态文本适配
- `SettingsPanel` 构造参数 `ToolStripStatusLabel status` → `Action<string> status`；全部 `_status.Text = X;` → `_status(X);`
- MainForm 调用：`new SettingsPanel(_game, s => Logger.Info("[设置] " + s))`

---

## 二、批2：展示层组件删除 + FolderPanel 瘦身

### 整文件删除
- `源码/Forms/SongCardView.cs`（831 行，引擎 SongGrid 已替代）
- `源码/Forms/CalibEmbedPanel.cs`（46 行，引擎校准页已替代；**CalibrationForm 保留**）

### MainForm 引用清理
- 删除字段 `_libraryView/_library/_folderView/_folder/_calibView/_calib`
- 删除方法 `BuildLibraryView/WireLibrary/ShowLibrary/BuildFolderView/ShowFolder/BuildCalibView/ShowCalib/RebuildLibrary`
- ctor BuildLibraryView/BuildFolderView/BuildCalibView 调用 + Controls.Add 对应面板
- KeyDown ESC/overlay 判定去掉三视图；HideOthers/HideAllMenus 去对应行
- OnShown `--uidebug` 分支：ShowLibrary+DebugOpenFirstChart → Logger 弃用提示（无脚本使用该参数，设计 §1.3）

### FolderPanel 瘦身（改纯逻辑类，修正1 案A 原地瘦身）
- 保留：`_cfg` 注入、`_items`、`FilteredItems`、`RefreshData`、`PickFolder`、`ImportFiles`、`ScanZip`、`OpenDir`、`DeleteSelected`、`CopySelectedPath`、`PathText`、`StatsText`、`Items`、`FolderChanged` 事件、`SelectedIndex`（新，引擎页选择联动）
- 删除：全部 WinForms UI（`_headBar/_title/_pathLabel/_statsLabel/_btnXxx/_filter/_listTitle/_chartList/_selHint`）、`Build()/OpBtn/ExtColor/TypeName/RenderList/UpdateSelHint/OnDrawItem`、`: UserControl` 继承
- 新增：`UiSync (Action<Action>)` 回调（ScanZip 后台解压完成回 UI 线程；引擎窗注入 BeginInvoke）
- **行为增强**：`PickFolder/ImportFiles/ScanZip/OpenDir/Delete/Copy` 的 `ParentForm` 对话框 owner 改 null 宿主（纯逻辑类无控件宿主）；`DeleteSelected/CopySelectedPath` 改用 `SelectedIndex`（引擎页点击行设置）——修复原引擎页删除/复制永远提示"请先选择一个文件"的潜在缺陷（原 _chartList.SelectedIndex 从未被引擎页驱动）

### SecondaryPages.cs 类型同步
- `FolderLogic()`：去 `IsDisposed` 检查（纯类无此属性）；注入 `UiSync = a => BeginInvoke(a)`（引擎窗为 Form）
- 删除 `GoSong/GoHome` 事件接线（引擎页按钮直连 GoTo 页面跳转；事件已从 FolderPanel 删除，避免 CS0067）
- 删除/复制按钮回调：先 `fp.SelectedIndex = _folderSelIdx` 再调用
- 头注释更新（FolderPanel/CalibEmbedPanel → 委托 FolderPanel 纯逻辑类）

### 注释清理（批2 "清注释"）
- EngineMainShell.cs 6 处 SongCardView 文案对照注释 → "引擎选歌/legacy 选歌"；SecondaryPages 头注释；UiText.cs 3 处；ModeSystem.cs 1 处

---

## 三、验证

| 项 | 结果 |
|---|---|
| scratch 构建（构建产物/obj/t56-scratch，批1 后） | 0 警告 0 错误 |
| scratch 构建（批1+批2 后） | 0 警告 0 错误 |
| 全源码 mojibake 扫描（63 个 .cs） | 0 处残留 |
| 交叉引用扫描（ShowMainMenu/BuildMainMenu/SongCardView/CalibEmbedPanel/FolderPanel 误用） | 0 处遗留（注释除外，已清） |

> 说明 1：构建期间发生一次引擎树并发改动（引擎/engine/Tool/ChartImport.cs 短暂引用不存在的 SetBpmAt——引擎团队进行中），与此交互后同步重编通过；最终警告 1 项（ChartImport.cs CS0219 errLine 未用）位于**引擎侧文件，非本任务改动**，建议引擎团队顺手清理。
> 说明 2：ParityCli legacy 块（513-604 段）按设计留在批4 迁移（§4.2/§6.2(b)——LegacyMenuRender 数据源改 EngineMainShell.Actions、legacy-mainmenu.png 删除、BridgeFeatureNames 字符串随批4）。批1/批2 未动 ParityCli（避免半迁移破坏 parity -1 口径）。

---

## 四、遗留/交接

1. **批3 待办**（本轮未做，与 t54 互锁）：ShowView 系已简化但保留（7 视图面板删 _settingsView/_mpView/_editorView 只留三容器）+ Act 白名单 + actions public 化（引擎侧）+ 兜底纯游玩模式已预演（catch 分支）。
2. **批4 待办**：--menushot 合流 --shellshot；ParityCli 四段改造；**最后**删 CliLegacy 字段与 --legacyui 参数；BridgeFeatureNames/基线复核。
3. **新增小观察**：FolderPanel 纯类化后，`FolderBrowserDialog/OpenFileDialog` 无 owner —— 引擎页按钮弹出对话框无父窗（原亦如此：引擎窗托管时不设 ParentForm 的实例从未被加入控件树）。如需居中可后续注入 owner，不阻塞。
4. **建议 captain**：构建后重点回归 = 引擎曲库页 10 按钮 + 筛选 10 钮逐一走查（设计回归矩阵 #16）；`--menushot` 暂仍走旧路径（批4 收口，产物会有变化属预期）。