# T44 最终复验报告（bin 21:58:09：t46 判线排布 + t45/t47 ExitToLibrary 收口）

- 复验 bin = Milestone.dll 21:58:09（晚于源码 21:52:44；selfcheck exit 0）· 证据：构建产物/obj/verify-final/（vf-01~08）

## A. 编辑器右栏

| # | 项 | 结果 | 证据 |
| --- | --- | --- | --- |
| A1 | 判线 Tab：拆线/绑线按钮单行、Cover 单行、5 tip 行距 ≥6px、父线标签单行 | **通过 ✅**：拆线（播放头）/绑线（并上一条）按钮单行完整（宽 160/170）；Cover 遮罩复选框单行（定宽 260）；5 个 tip（数量上限/拆线/绑线/跨线/细线/父线/覆盖/速度/透明度）各行分离 ≥6px 无穿插；父线（原移动母轨）单行 | vf-01-judgeline.png |
| A2 | 事件 Tab：easeTip 不穿插、说明行宽内换行 | **不通过 ❌**：easeTip「（拖判定线/添加事件时写入）」与相邻行**仍穿插**（「(表格线上)/添加事件时弹文件写入…」「表格时弹的(弹开写心)」互压——与 t41-02 相同，t46 修复未覆盖事件 Tab easeTip） | vf-02-event.png |
| A3 | 属性 Tab：_propLbl 多行分开、底部说明换行 | **通过 ✅**：工程/模式/BPM/音符/未选中音符/跳转编辑页 各行分开；底部「选中音符/事件的属性在「音符」「事件」「判定线」页编辑。」换行完整 | vf-03-property.png |
| A4 | 悬停 11 处说明行 tooltip 全串 | ⚠ 受限：自绘 UI tooltip 自动化难触发（悬停未弹）——静态说明行已全串可见（A1/A3）；tooltip 同源文案标注待人工确认 | — |

## B. 返回路径

| # | 项 | 结果 | 证据 |
| --- | --- | --- | --- |
| B5 | AI 演示 ESC→引擎菜单 + 主窗可见 + 单窗 + MainWindowHandle≠0 | **部分 ❌**：MainWindowHandle=3868052（≠0 **t47 句柄修复 ✅**）+ 单窗=1 ✅——但**引擎窗 UI 显示黑屏**（「Milestone：在主菜单选择…」+ FPS 0 预留态——未重绘到引擎菜单 ❌） | vf-04-ai-esc.png |
| B6 | 布局编辑器返回=引擎菜单 | ⚠ 未达（同 B5 黑屏态——点击后黑屏预留态） | vf-05-layout.png |
| B7 | 回环作曲返回=引擎菜单 | ⚠ 未达（同黑屏态） | — |
| B8 | 迷你 mania 关闭=引擎菜单 | ⚠ 未达（同黑屏态） | — |
| B9 | 引擎 UI 演示返回=引擎菜单 | ⚠ 未达（同黑屏态） | — |
| B10 | 联机 ESC=引擎菜单 | **不通过 ❌**：ESC 后**纯黑屏**（11744 字节——未回引擎菜单） | vf-07-online-esc.png |
| B11 | --legacyui 显示旧 WinForms 主菜单 | **通过 ✅**：显示旧主菜单（Mile stone+玩家卡+14 入口 WinForms——仅调试用） | vf-08-legacyui.png |

---

## 判定

**A：2.5/4 通过**（A1 判线 ✅ / A3 属性 ✅；A2 事件 easeTip ❌ 仍穿插；A4 tooltip 受限）
**B：1/7 通过**（B11 --legacyui ✅；B5 句柄恢复 ✅ 但 UI 黑屏 ⚠；B6-B9 未达同黑屏；B10 联机 ESC 纯黑 ❌）

### ❌ 根因分析
- **A2 事件 Tab easeTip 穿插**：t46 判线排布只修了判线 Tab；事件 Tab easeTip（「（拖判定线/添加事件时写入）」）未加 AutoShrinkFont/Wrap 限宽——同 A1 判线已修的根因但未覆盖事件 Tab。
- **B5/B10 返回黑屏**：t45/t47 ExitToLibrary 收口**只恢复主窗句柄（MainWindowHandle≠0 生效）但 UI 未重绘到引擎菜单**——引擎窗显示黑色预留态（「在主菜单选择开始游戏」提示 + FPS 0）——**返回路径缺少 GoTo("menu")/Show 引擎菜单的 UI 重绘调用**；联机 ESC 甚至纯黑（未触发收口）。

【修复方向】①事件 Tab easeTip + AutoShrinkFont/Wrap（复用判线已修逻辑）②ExitToLibrary 恢复主窗后调 GoTo("menu") 重绘引擎菜单（当前仅 Show 句柄未重绘）；联机 ESC 同路径。

注：B6-B9（布局/回环/迷你/引擎演示）因 B5 黑屏态阻塞未逐项成功——同根因（返回路径 UI 未重绘）。