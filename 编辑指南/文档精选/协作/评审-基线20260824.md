# 评审 · 基线取证 2026-08-24（t6 · t2 前）

> 评审员 reviewer · 2026-08-24 10:3x · 工作目录 `D:\Users\etgya\Desktop\milestone`
> **本基线供 t2 完成后复评对比**（t2 恢复后重跑本表全部命令，对比 exit/证据文件/功能判据）。
> 只读+运行取证，未修改任何源码（仅新建证据目录 obj\agent-base\* 与本文档）。

## 0. 环境

| 项 | 值 |
|---|---|
| 构建 | `dotnet build -c Release` → **0 警告 0 错误**，exit 0（2026-08-24 10:3x，增量） |
| 二进制 | `bin\Release\net8.0-windows\win-x64\Milestone.exe`（2026-08-24 10:31:28 构建，晚于 Program.cs 10:31:19，含三入口） |
| 运行前清理 | 已 kill 全部 Milestone 进程（无残留） |
| GPU | NVIDIA GeForce RTX 5070 Ti Laptop GPU（日志确认 Direct2D1 渲染线） |

## 1. 入口取证表（命令 | exit | 证据文件 | 结论）

| # | 命令 | exit | 证据文件 | 结论 |
|---|---|---|---|---|
| 1 | `--edshot "Chart\测试格式\phigros_test.json" 4 "obj\agent-base\edshot"` | **0** | `obj\agent-base\edshot\edshot.log` + 9 张 `phigros_test_0.0s~4.0s.png`（0.5s 步进，1400x860） | ✅ 编辑器截图 9/9 保存，日志逐张 ✅；界面完整（工具栏/模式/部件/动画菜单/事件曲线/事件键帧列表），证据有效 |
| 2 | `--edsim "Chart\测试格式\phigros_edsim_test.json" "obj\agent-base\edsim"` | **0** | `obj\agent-base\edsim\edsim.log` | ✅ **7/7 断言通过**（键帧命中 DragKind=26 / 横拖改时间数量不变 / 纵拖改值 0.5→0.8 / 拖拽不增删 / 音符命中 DragKind=5 / 判定线拖拽 DragKind=20 / 重载不丢事件 5/5）；日志明示 D2d 事件多选（框选≥3/组拖/批删）为丢失功能待 t2 恢复后追加断言 |
| 3 | `--shotdemo "Chart\测试格式\phigros_test.json" 4 "obj\agent-base\shotdemo"` | **第一次 -1073741819（0xC0000005）／第二次 0** | `obj\agent-base\shotdemo\auto_103421578~...858.png`（40 张）；`obj\agent-base\shotdemo2\auto_*.png`（40 张）+ `shotdemo2\shotdemo.log` | ⚠️ **偶发崩溃（复现 1/2）**：首次 `System.AccessViolationException` @ `GamePanel.DrawHud → PaintCore → RenderThreadLoop`（渲染线程与窗体关闭竞态），崩溃前已产出 40 张且无 shotdemo.log（WriteLog 未达）；二次运行 exit 0 + 40 张 + log 正常。**t2 后需复跑≥3 次确认稳定性**；截图内容有效（Score 609/Perfect 判定/音符 2/9 均正常渲染） |
| 4 | `--selfcheck` | **0** | 根 `selfcheck.log`（08-24 10:3x 重跑） | ✅ 引擎自检全通过（mania/maimai/Phigros/ADOFAI 模拟 + 12 组断言块，含滑条/长条闭环） |
| 5 | `--modetest` | **0** | 根 `modetest.log` | ✅ 关闭=仅 Mania（1 项）/开启=11 项，过滤正确 |

补充：`--edshot`：WindowMs 6133ms（取自 canvas.TimeWindowMs）、基场 24,38 872x664（edsim 日志）；edshot 画布 1400x860 全览。

## 2. 当前编辑器功能基线（对照 `docs\协作\D-编辑器对齐差距表-v2.md`，edshot 图片判据）

> 判定方式：edshot phigros_test_2.0s.png 人眼识图 + edsim 交互断言 + 差距表 v2 grep 结果。✅/❌=与差距表 v2 一致（v2 为方法级 grep，本表为运行级复核）。

| 判据 | 基线 | 证据（本基线） |
|---|---|---|
| 垂直事件柱（五柱/柱宽=值） | ❌ 缺失（现为底部水平 5 通道 moveX/moveY/rotate/alpha/speed） | edshot 截图底部 5 行通道；差距表 v2 §2.1 |
| 拍:0/1 时间显示 | ❌ 缺失（事件键帧=秒/ms 数值 + 底部时间条分秒 0:00/2.00s） | edshot 截图顶栏 3000/0.8/0.2、时间条 0:00~0:04 |
| 音符编辑面板（起止/X/朝向/速度/Y偏移/宽度/可视/透明度） | ❌ 缺失 | edshot 截图无此类面板；v2 §2.1 音符面板行 |
| 批量编辑执行列表（MirrorY/AttachX 等） | ❌ 缺失 | edshot 工具栏无 MirrorY/AttachX 下拉；v2 执行列表行 |
| 预制事件按钮组 | ❌ 缺失 | edshot 右侧动画菜单无"预制事件"；v2 预制行 |
| 曲线填充（Ctrl+F/G 锚/密度/种类/曲线） | ❌ 缺失 | edshot 无填充入口；v2 填充行 |
| Bezier 保存/手柄（编辑器绘制+4 点手柄） | ❌ 缺失（模型/解析/游玩端存活，编辑器绘制+保存丢） | v2 贝塞尔行 + edsim 无 Bezier 断言 |
| 事件键帧 5 类 + 缓动曲线视图（右键切缓动） | ✅ 存活（水平 5 通道 + 缓动菜单"Linear 线性"） | edshot 截图 + edsim 键帧交互 7 项通过 |
| 多判定线拆/绑/父子（1~64） | ✅ 存活（数量 1、拆线/绑线/父线 UI 在） | edshot 截图右侧动画菜单 |
| 判定线拖拽/音符拖拽/键帧单击命中 | ✅ 存活（DragKind 20/5/26 均命中） | edsim 6/7 项交互断言 |
| 重载谱面事件不丢 | ✅ 存活（moveY 5/5） | edsim 第 7 项 |
| 事件级多选/框选/组拖/批删（D2d） | ❌ 缺失 | edsim 说明段明示 + v2 事件级多选行 |

**基线汇总**：交互核心（键帧点击/拖改/音符/判定线/重载）全 ✅；"垂直柱/拍:0/1/音符面板/执行列表/预制/曲线填充/Bezier 保存/事件多选"8 项 ❌ 与差距表 v2 一致（其中 Bezier 保存为 P0 数据保真）。待 t2 完成复评：重跑本表 5 条命令 + 对 8 项 ❌ 逐项复测。

## 3. 复评对比清单（t2 完成后执行）

1. 重跑 5 命令，对比 exit（重点 shotdemo ≥3 次稳定性）
2. 对 8 项 ❌ 判据逐项 retest（edshot 同机位截图 + 增加 edsim 断言段）
3. 更新本文件"复评对比"章节（或另出 评审-基线复评-<日期>.md）
