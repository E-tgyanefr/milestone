# P1-1 · 教练文案与降级链 —— 完成报告(T34)

> 2026-08-22 · 主代理 · 验证:构建 0 错误 / `--coachtest` exit 0 / `--reviewtest` exit 0 / `--selfcheck` exit 0

## 交付物
| 文件 | 内容 |
|---|---|
| `Play\CoachCard.cs`（新建） | `Fallback(d)` 规则兜底文案（离线确定）· `BuildPrompt(d)` 结构化提示词 · `AskAsync(d,timeout,base)` 本地 LLM 调用（Ollama :11434,失败/超时/坏址返回 ""）· `CompleteAsync(d)` 兜底+LLM 一键 · `RunCoachTest()`（--coachtest） |
| `ReviewData.CoachText` | 文案字段（序列化入复盘 JSON） |
| `ReviewCard.SaveTo(d,path)` | 同路径重存（LLM 异步升级后刷新） |
| `GamePanel.Finish()` | 生成复盘后立即写入兜底文案并存档;后台 `Task.Run` 异步尝试 LLM 升级（成功才覆盖,失败保留兜底→**不阻塞结算/渲染**） |
| `Program.cs` | CLI `--coachtest` |

## 降级链（实测）
1. 规则兜底 `Fallback`：`本局 {标题}：ACC xx%，UR xx，断连 n 次。最弱段 a~b s 命中率 xx%（漏 n 键），建议单独放慢练习该段；…` ✅
2. LLM `AskAsync`：坏地址 `http://127.0.0.1:1` → **5s 内返回空**（不 hang）✅
3. `CompleteAsync`：LLM 空时保住兜底文案（非空）✅

## 说明
- LLM 文案显示在结算画面（随 T33 渲染一起呈现，`CoachText` 已有数据）；显示层由协助 AI 按铁命令完成（T34b 进行中）
- 提示词 ≤170 字符、输出上限 400 token → 单次 LLM 体验 ~15-40s（后台,不卡结算）
- v1 后续：把"建议→训练计划"（推荐曲目/难度）串起 P1-2 星级数据
